# REQUIRED SOURCES — 03 — UI/UX Design — Gemini + Stitch

Роли этого чата: `UIUX`. Дата: 2026-08-06.

Обязательный контекст — **6 документов из 15**. Читать по мере необходимости,
не загружать все сразу. Каждый путь — от канонического корня
`/Users/danila/Downloads/Trading-Bot-merge-learning-nik`.

> **Общее для всех 15 документов пакета:** каждый в шапке фиксирует HEAD `a54a100`.
> Фактический HEAD — `7f357e3`, пакет отстаёт на один коммит. Отдельно про это не напоминаем;
> столбец «Что перепроверить» содержит расхождения, специфичные для документа.

| Документ | Зачем именно этой роли | Актуальность | Что перепроверить по коду |
|---|---|---|---|
| `docs/source/00_SOURCE_INDEX.md` | Главный вход в базу знаний Quant/QuantFlow: описывает состав Source Pack (15 документов), порядок чтения, приоритет источников истины, систему обозначений… | в основном актуален | 00_SOURCE_INDEX.md:48 и :168 — «27 открытых вопросов»; в 14_OPEN_QUESTIONS_AND_DECISIONS.md фактически определено 37 разделов `### OQ-…` (от OQ-PROD-01 на строке 27… |
| `docs/source/01_CURRENT_PROJECT_CONTEXT.md` | Компактный «единственный вход» в проект Quant/QuantFlow: продукт, стек, архитектура трёх слабо связанных частей (сайт / Flask-dashboard / торговый бот), сущности БД, интеграции,… | в основном актуален | Строка 132: «bot/ Python, 187 файлов» — фактически `find bot -name '*.py'` = 176. Документ противоречит сам себе: его собственная разбивка по модулям (строки… |
| `docs/source/03_WEBSITE_AUDIT.md` | Полный аудит маркетингового сайта `website/` (Next.js 15 + next-intl, локали /ru и /en): карта маршрутов, структура лендинга, адаптивность на 5 разрешениях, a11y-семантика, статические… | в основном актуален | W-02, ссылка на строку: документ пишет «`robots.ts:4`», фактически дефолт домена объявлен в `website/src/app/robots.ts:3` (`const SITE_URL =… |
| `docs/source/04_DASHBOARD_AUDIT.md` | Источник истины по операторскому дашборду QuantFlow (`bot/ui/`): полная карта 12 экранов SPA и API-поверхности (42 маршрута `/api/v2`, 14 legacy `/api/*`, 30 `/api/platform/*`),… | в основном актуален | Executive Summary (строка 40) утверждает: «публичны только `/health` … и `/api/v2/auth/session`». Фактический allow-list в `bot/ui/app_factory.py:247-253` шире:… |
| `docs/source/09_PRODUCT_UX_MARKETING_AND_MONETIZATION_AUDIT.md` | Продуктовый аудит Quant: аудитория и сегментация, позиционирование и УТП, полная воронка от лендинга до activation, состояние монетизации, retention и готовность к beta/публичному… | в основном актуален | [P-02], строка 138: «5 CTA-точек ведут в одно место: … `finalCta.liveCta` … → все на `#access`» — неверно. `finalCta.liveCta` рендерится карточкой с… |
| `docs/source/13_STALE_DOCUMENTS_REGISTER.md` | Реестр состояния всей документации проекта вне `docs/source/`: для каждого файла зафиксированы объём, дата последнего коммита, соответствие коду, что устарело, что полезно и… | в основном актуален | [X-01] и «Top Critical Findings #5» приписывают бейдж `License: MIT` корневому `README.md` (в таблице X-01 прямо указан «Путь: `README.md` (корень)»). Проверка:… |

## Источники истины в коде (авторитетнее любого документа)

| Тема | Файл |
|---|---|
| Схема БД | `bot/qf_platform/schema.py` |
| Дизайн-токены сайта | `website/src/styles/tokens/` |
| Дизайн-токены дашборда | `bot/ui/static/css/tokens.css` |
| Торговые правила | `knowledge/rules/rules.yaml` |
| Переменные окружения | `bot/config.py`, `.env.example` |
| Контент сайта | `website/messages/{ru,en}.json` |
| Контракт API v2 | `bot/ui/api/v2/` |

