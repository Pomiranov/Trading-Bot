# Разовые измерительные инструменты сессии 2026-08-02

Сохранены **дословно, как были запущены**, без причёсывания и без объединения.

## Зачем сохранены

**Правило 12 §8 требует показывать предикат срабатывающим на подсадке.**
`planted_cases.py` — та самая подсадка, на которой `tools/silent_zeros_probe.py`
дал **12 из 12** при **0** на шести отрицательных контролях. Без неё докстринг
инструмента требует демонстрации, которую нечем повторить.

`is_filter_probe*.py` — инструменты, которыми получены числа раздела 2к
`docs/PROJECT_STATE.md`. Число в документе без инструмента невоспроизводимо, а
воспроизводимость в проекте — предмет отдельных долгов (№37, №41).

## Что чем измерено

| файл | что дал |
|---|---|
| `is_filter_probe.py` | состояние фильтра даунтренда по сессиям IS и OOS для наборов 12 и 20: сколько бумаг закрыто, медиана, сессии «закрыт у всех» |
| `is_filter_probe2.py` | перекрёстная сверка метода: тот же предикат дал **19 из 892 (2.1%)** и серию 2026-07-13…07-31, совпав с независимо записанным замером 01.08 |
| `is_filter_probe3.py` | разогрев SMA200: все 12 бумаг достигают бара 200 в одну дату **2024-04-22**; отсюда деление IS на две половины с противоположным поведением фильтра |
| `planted_cases.py` | подсадка правила 12: 12 положительных случаев по два на каждый вид + 6 отрицательных контролей |

## 🚩 Что с ними НЕ сделано — сказано прямо

**Три `is_filter_probe*` не объединены и содержат общий код втрое.** Это
осознанно оставлено как есть: объединение — работа, а не сохранение, и делать её
на закрытии сессии значило бы править то, чем уже получены записанные числа.
**Но это дубли, и они названы здесь, а не спрятаны** — устранять при первом
обращении к ним.

Инструменты **разовые**: ничего не пишут, читают боевую БД через `.env` боевой
папки и жёстко ссылаются на пути `D:\worktrees\qf-notify-p2p1` и
`D:\Trading-Bot-Nik`. На другой машине не пойдут без правки путей.

`silent_zeros.py` из того же каталога **не дублируется** сюда — он уже сохранён
как `tools/silent_zeros_probe.py` с записанной границей инструмента.
