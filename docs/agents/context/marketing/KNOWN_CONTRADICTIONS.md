# KNOWN CONTRADICTIONS — 04 — Marketing & SEO — Quant

Дата: 2026-08-06. Расхождения найдены фактической проверкой документов против кода
на HEAD `7f357e3`. **Это уже найденное — не искать заново, а учитывать.**

Правило: при расхождении документа и кода прав **код** (`AGENTS.md §2`).
О найденном новом расхождении сообщать в handoff.

## `00_SOURCE_INDEX.md`

- Снимок HEAD устарел: строка 10 заявляет `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = 7f357e358e6a5a7b9e067fccfa667caa74fdae15 (a54a100 теперь HEAD~1). Тот же устаревший commit продублирован в §5 (00_SOURCE_INDEX.md:90) и в §11 (00_SOURCE_INDEX.md:276).
- 00_SOURCE_INDEX.md:9 — «на 5 коммитов впереди origin»; фактически `git status -sb` даёт `[ahead 6]` (добавился коммит 7f357e3 с самим Source Pack).
- 00_SOURCE_INDEX.md:48 и :168 — «27 открытых вопросов»; в 14_OPEN_QUESTIONS_AND_DECISIONS.md фактически определено 37 разделов `### OQ-…` (от OQ-PROD-01 на строке 27 до OQ-OPS-07 на строке 476). Ошибка согласована с самим документом 14 (14_OPEN_QUESTIONS_AND_DECISIONS.md:21 тоже пишет «27 вопросов»), то есть пакет внутренне противоречив по отношению к своему же содержимому.
- 00_SOURCE_INDEX.md:168 — «50 findings» (и 11_MASTER_ROADMAP.md:421 «50 из 50 findings»); фактически в 02–09 определено 55 блоков `### [X-nn]`, из которых 7 — записи «Сильные стороны» (A-07, W-10, D-05, T-06, S-07, Q-07, P-07), то есть проблемных findings 48. Ни 55, ни 48 не равны заявленным 50.
- 00_SOURCE_INDEX.md:123 — бейдж `License: MIT` приписан корневому `README.md`; в корневом README.md 0 вхождений «License»/«MIT» (7 бейджей shields.io, лицензионного среди них нет). Бейдж реально находится в docs/README.md:10. Та же атрибуция повторена в 13_STALE_DOCUMENTS_REGISTER.md:58,241 и 14_OPEN_QUESTIONS_AND_DECISIONS.md:380. Отсутствие файла LICENSE подтверждено (`ls LICENSE*` → нет совпадений).
- 00_SOURCE_INDEX.md:219 — «npm audit --omit=dev → 3 high severity (postcss, sharp)»; сейчас установлены postcss 8.5.25 (вложенная копия под next — 8.4.31) и sharp 0.34.5, то есть версии не ниже уровней, на которых закрыты известные high-CVE. Перепроверить нельзя без сети — вывод не опровергнут, но подозрителен на устаревание.

## `01_CURRENT_PROJECT_CONTEXT.md`

- Снимок git устарел: документ (строка 8) фиксирует HEAD `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (a54a100 теперь HEAD~1). Расхождение безопасное: `git diff a54a100 HEAD` затрагивает ТОЛЬКО 15 файлов docs/source (+6032 строк), код не менялся, поэтому все кодовые утверждения документа остаются в силе.
- Строка 7: «на 5 коммитов впереди `origin`» — фактически `git status -sb` даёт `[ahead 6]` после коммита 7f357e3.
- Строка 132: «bot/ Python, 187 файлов» — фактически `find bot -name '*.py'` = 176. Документ противоречит сам себе: его собственная разбивка по модулям (строки 133-148: 37+35+11+9+20+11+8+14+2+3+11+3 = 164, плюс 4 файла в корне bot/ и 8 в market/realtime/gateway/data) даёт ровно 176. Та же цифра 187 повторно использована как доказательство в строке 189.
- Строка 156: «tests/ 11 файлов / 145 тест-функций» — файлов 11 (верно), но тест-функций 146 (`grep -rcE '^\s*(async )?def test_' tests`: 30+34+24+4+35+9+10). Отдельно: 146 функций дают 233 собранных теста pytest (параметризация), а не 161+72=233 — это совпадает, но из документа связь не читается.
- Строка 133-134: `config.py 110 стр` — фактически `wc -l bot/config.py` = 109 (main.py 417 — точно верно).
- Строка 107: «12 команд, ~60 callbacks» для bot/tg — фактически 15 различных `CommandHandler("...")` (balance, cancel, dashboard, game, help, learning, operations, orders, paper, portfolio, positions, signal, start, stats, status) и 72 регистрации `CallbackQueryHandler`. «3 FSM» подтверждается точно (3 × `ConversationHandler(`).
- Строка 308: «Ноль TODO/FIXME/HACK на 258 файлов кода» — сам факт нуля подтверждён (grep по bot/ + website/src = 0 совпадений), но знаменатель 258 не воспроизводится ни одним разумным определением (bot .py = 176, website/src ts/tsx = 71, bot+website/src+tests+scripts всех типов = 277).

## `03_WEBSITE_AUDIT.md`

- Поле «Commit HEAD» устарело: документ фиксирует `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD — `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (`7f357e3`, «docs: capture Source Pack baseline before Sprint 01 decisions»). При этом `git diff --name-only a54a100 HEAD -- website/` = 0 файлов, то есть код сайта не менялся и все технические выводы остаются валидны; расходится только указатель на коммит.
- W-02, ссылка на строку: документ пишет «`robots.ts:4`», фактически дефолт домена объявлен в `website/src/app/robots.ts:3` (`const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://quantflow.example";`). Для `sitemap.ts:4` и `[locale]/layout.tsx:55` номера строк точны.
- W-04 описан неполно и его рекомендация противоречит явному решению в коде: документ говорит только «фокус не заперт — Tab уводит на контент под ней» и советует добавить focus trap + `role="dialog"`/`aria-modal`, но в `website/src/components/sections/nav/mobile-nav.tsx:203-209` зафиксировано обратное проектное решение — «This stays a disclosure, not a `role=dialog`, so there is no focus trap — instead, when Tab carries focus out of the panel into the scrimmed page, close it», и обработчик `onBlur` действительно закрывает панель при уходе фокуса (исключая сам триггер). Механизм авто-закрытия в аудите не упомянут вовсе, из-за чего находка выглядит серьёзнее, чем есть.
- Раздел «Живой сайт»: тот же процесс dev-сервера (PID 93912) всё ещё слушает :3000, но сейчас отдаёт HTTP 500 на ВСЕ пути, включая `/icon.svg` и `/robots.txt`. Это признак зависшего/устаревшего процесса, а не регресса кода (исходники `robots.ts`/`sitemap.ts`/`middleware.ts` на месте, typecheck и 4 гейта зелёные), но таблица HTTP-кодов раздела 1 (307/200/404) сейчас невоспроизводима без чистого перезапуска сборки.
- Мелкая неточность в таблице раздела 2: «Ассистент-орб (fixed) `components/assistant/assistant-launcher.tsx`» перечислен в порядке содержимого `page.tsx`, тогда как `AssistantLauncher` рендерится в `website/src/app/[locale]/layout.tsx:143`, а не в `page.tsx` (в `page.tsx` его нет ни в импортах, ни в разметке).
- Внутреннее расхождение кода и контента, которое аудит не отметил: раздел 5 верно приводит `faq.a9` («Bybit подключён только для чтения… Финам запланирован, но не реализован»), но docstring `website/src/app/[locale]/page.tsx` утверждает, что «Bybit being read-only and Finam being unimplemented are no longer stated anywhere». Заявления живы в FAQ — комментарий в коде неверен.

## `09_PRODUCT_UX_MARKETING_AND_MONETIZATION_AUDIT.md`

- Снапшот HEAD устарел: документ пишет `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (7f357e3). Смягчающее обстоятельство: 7f357e3 — коммит только документации (`git diff --name-only a54a100 HEAD` → 15 файлов, все в docs/source/), поэтому все выводы о коде и контенте остаются применимыми к текущему дереву.
- [P-02], строка 138: «5 CTA-точек ведут в одно место: … `finalCta.liveCta` … → все на `#access`» — неверно. `finalCta.liveCta` рендерится карточкой с `href="#pricing"`: website/src/components/sections/access/access-section.tsx:147-148. Ссылка логично не может вести на `#access`, потому что сама находится внутри секции `#access`.
- [P-02], строка 138: «`#access` встречается 5 раз» в DOM — недосчёт по исходникам. `href="#access"` присутствует в 6 компонентах (site-header.tsx:113, mobile-nav.tsx:313, dashboard-section.tsx:63, telegram-section.tsx:95, pricing-section.tsx:111, footer.tsx:69), причём pricing-section рендерит его по всем трём тарифам → минимум 8 якорей в разметке. Точную цифру DOM без запуска сайта не проверить, но «5» исходникам не соответствует.
- Executive Summary, строка 35: «Telegram-бот с 12 командами» — фактически 14 команд регистрируется безусловно в активном боте: bot/tg/bot.py:125-135 (start, dashboard, portfolio, positions, orders, operations, balance, stats, signal, status, help) + :205 game, :208 paper, :217 learning; плюс `/cancel` внутри трёх ConversationHandler (handlers/signals.py:391, trading.py:256, settings.py:210) → 15 уникальных имён CommandHandler. Ни /help (9 упомянутых команд), ни меню не дают числа 12.
- [P-01], строка 93: «`grep -rniE "subscription|billing|stripe"` по `bot/` → 0» — фактически 1 совпадение: bot/ui/static/app/main.js:7 («…a chart or a subscription behind») — это проза в комментарии, не биллинг. Существо утверждения (реализации биллинга нет) подтверждается; литеральный «0» неточен. Аналогично [P-02], строка 129: `grep onboarding|wizard|getting.?started` по bot/ → не 0, а 1 (bot/ui/static/miniapp/game.js:92, 'Market Wizard').
- Цитирование ключей i18n неточно: `footer.tagline` как единого ключа не существует — есть `footer.tagline1` («Автоматический торговый оператор») и `footer.tagline2` («с проверяемым путём решения.»); аналогично `hero.headline` разбит на `hero.headline1`/`headline2`, а `hero.subline` начинается с фразы «Торговый оператор для MOEX.», опущенной в цитате (website/messages/ru.json). Смысл цитат сохранён, но ключи для правок указаны неверно.
- [P-04], строка 193: «Периодической сводки нет в активном боте» — с уточнением. Функция `notify_daily_learning_summary` СУЩЕСТВУЕТ в активном диспетчере (bot/tg/notifications/dispatcher.py:364), но не имеет ни одного вызова во всём bot/ — то есть вывод верен, а формулировка «нет» скрывает наличие мёртвого кода, который дешевле включить, чем переносить из legacy.
- Строка 167: «Реализация подтверждения автоматических сделок есть только в неиспользуемом `bot/ui/telegram_bot.py`» — модуль не полностью неиспользуемый: bot/run_forward_d1.py:431 лениво импортирует `from ui.telegram_bot import send_notification` (с комментарием на строке 59 о том, что импорт тянет легаси). Для вывода [P-03] это несущественно, но «неиспользуемый» — не буквально.

## `13_STALE_DOCUMENTS_REGISTER.md`

- Шапка документа: `Commit HEAD` = `a54a100b4d542f1d866b5f89336ce0703fea6ced`. Фактический HEAD сейчас — `7f357e358e6a5a7b9e067fccfa667caa74fdae15` («docs: capture Source Pack baseline before Sprint 01 decisions»), а `a54a100` стал HEAD~1. Ветка (`quant-site-approved-reference-redesign`) совпадает.
- [X-01] и «Top Critical Findings #5» приписывают бейдж `License: MIT` корневому `README.md` (в таблице X-01 прямо указан «Путь: `README.md` (корень)»). Проверка: `grep -c MIT README.md` → **0**; бейдж находится в `docs/README.md:10` (`![License](https://img.shields.io/badge/License-MIT-...)`). При этом §2 (строка 110) приписывает тот же бейдж `docs/README.md` — то есть внутри документа одно и то же наблюдение отнесено к двум разным файлам, и для корневого README оно неверно.
- Заголовок «## 2. `docs/` — 15 файлов» (строка 106) не согласуется ни с диском, ни с собственной таблицей: `find docs -name '*.md' -not -path 'docs/source/*'` → **14**, и таблица §2 содержит ровно 14 строк.
- Шапка: «Область: 46 Markdown-документов вне `docs/source/`», раскладка «`docs/` 15, `design/` 13, `website/docs/` 17, корень 2» даёт 47, а не 46. Фактически: docs 14 + design 13 + website/docs 16 .md (17-й файл в §4 — это `design-references/approved-stitch-reference.jpg`, не Markdown) + корень 2 = **45 .md**. Всего .md вне `docs/source/` в репозитории — **67** (не учтены `website/README.md`, `bot/ui/static/miniapp/BOTFATHER.md`, `knowledge/**` — 18 файлов; часть из них упомянута отдельным абзацем на строке 169).
- [X-02], доказательство п.1: «здесь 5 коммитов впереди `origin`». Сейчас `git status -sb` → `[ahead 6]` (добавился `7f357e3`).
- [X-01]: «`grep -rniE "bayes|posterior|prior|likelihood"` по `bot/` → **0**». Фактически по `bot/**/*.py` — 1 совпадение: `bot/security/bootstrap.py:25` (слово «priority», подстрока `prior`). Содержательный вывод (байесовского вывода в коде нет) верен, но литеральное «0» неточно.

## `02_REPOSITORY_AND_SYSTEM_ARCHITECTURE_AUDIT.md`

- Снапшот HEAD устарел: документ фиксирует `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD — `7f357e358e6a5a7b9e067fccfa667caa74fdae15` («docs: capture Source Pack baseline before Sprint 01 decisions»), a54a100 теперь HEAD~1. Ветка не изменилась.
- Счёт коммитов устарел: заявлено «58 коммитов», фактически `git rev-list --count HEAD` = 59.
- ЛОЖНО: строка 48 — «run_forward_d1.py 652 строки». Фактически `wc -l bot/run_forward_d1.py` = 448 строк; файл не менялся с коммита b2b02c4, то есть это ошибка измерения, а не дрейф. При этом ссылки внутри документа на `run_forward_d1.py:59` и `:431` верны.
- ЛОЖНО и самопротиворечиво: таблица качества кода (строка 357) утверждает «Самый большой Python-файл — `bot/ui/api/v2/routes_read.py`, 850 строк». Фактически крупнее два файла: `bot/engine/paper_engine.py` = 1086 строк и `bot/ui/telegram_bot.py` = 1036 строк. Собственная секция 1 того же документа (строка 57: «engine/ 2 файла / 1 086 строк») это опровергает.
- Счёт Markdown устарел: строка 65 — «44 Markdown-документа» в docs/, design/, website/docs. Сейчас там 58 .md (53 отслеживаемых + 5 неотслеживаемых новых промпт-документов); на коммите a54a100 отслеживаемых было 38.
- Мелкое расхождение метрик: «145 тест-функций» → фактически 146 (`grep -rn "def test_" tests/`); «187 Python-файлов» → фактически 188 (176 в bot/ + 11 в tests/ + 1 в data/tinkoff_loader.py). Число TS/TSX (71) и строк (9 185) совпадает точно по website/src.
- Дрейф номеров строк в ссылках-доказательствах (сама суть утверждений подтверждена): `bot/security/guards.py:199` — `def mutating(` фактически на строке 195; `CLAUDE.md:139` («Dashboard auth: JWT-based») — фактически строка 141.
- Занижен счёт команд Telegram: строка 140 — «11 команд». В `bot/tg/bot.py:125-135` действительно ровно 11 команд, но ниже регистрируются ещё три (`/game` :205, `/paper` :208, `/learning` :217), плюс `/cancel` как fallback в трёх ConversationHandler — итого 14 верхнеуровневых команд. «~60 callback-хендлеров» — 68 вызовов CallbackQueryHandler, оценка приемлема.
- Формально: утверждение «`grep -rniE "bayes|posterior|prior|likelihood"` по bot/ → 0» даёт сегодня 1 совпадение — `bot/security/bootstrap.py:25` («highest priority first», подстрока «prior»). Байесовской математики в коде действительно нет, так что содержательно вывод (README оверклеймит «байесовская») подтверждён.
- Процедурный нюанс, не ошибка: в шапке «Что удалось проверить» указан запуск Flask dashboard на порту 5051, тогда как штатный порт продукта — 5001 (`bot/config.py:48`, DASHBOARD_PORT по умолчанию «5001»). Читателю стоит понимать 5051 как порт разового read-only прогона аудитора.

## `04_DASHBOARD_AUDIT.md`

- Устарел снапшот коммита: в шапке `Commit HEAD = a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (7f357e3, «docs: capture Source Pack baseline before Sprint 01 decisions»). Ветка совпадает. Расхождение в один docs-only коммит, кодовые утверждения не затронуты.
- Executive Summary (строка 40) утверждает: «публичны только `/health` … и `/api/v2/auth/session`». Фактический allow-list в `bot/ui/app_factory.py:247-253` шире: пять точных путей (`/health`, `/login`, `/api/v2/auth/session`, `/api/v2/auth/login`, `/api/internal/push`) плюс префиксы `/static/` и `/miniapp`. То есть вся мини-игра `/miniapp` (Quant Hunter) и внутренний push-эндпоинт отдаются без сессии. `/api/internal/push` имеет собственную защиту (shared-token `QF_INTERNAL_TOKEN` либо loopback, `bot/ui/legacy_api.py:344-363`), поэтому «fail-closed» по сути верно, но перечень публичной поверхности в документе неполон.
- Количество legacy-маршрутов `/api/platform/*` указано как «~28» (строка 91), фактически 30 декораторов `@platform_bp.route(` в `bot/ui/api/platform_routes.py`. Приблизительность помечена тильдой, но цифра занижена.
- Мелкий сдвиг якорей строк в D-02: заявлено `bot/ui/dashboard.py:56` — `app.run(...)`, фактически `app.run` на строке 57; заявлено `dashboard.py:47-53` про отказ debug при небезопасном bind — фактически 48-54. Файл действительно 57 строк, содержание совпадает дословно.
- Утверждение D-01 «`grep -rniE "subscription|billing|stripe|payment" bot/` → 0 (кроме поля `payment`)» строго говоря даёт одно попадание на `subscription`: `bot/ui/static/app/main.js:7` — слово в прозе docstring («a chart or a subscription behind»). Аналогично «`onboarding|wizard` по `bot/ui` — совпадений нет» даёт одно попадание: `bot/ui/static/miniapp/game.js:92` — ачивка 'Market Wizard'. По существу вывод верен (клиентских сущностей нет), но литеральные «0» неточны.

## `05_TELEGRAM_BOT_AUDIT.md`

- Снапшот HEAD устарел: документ пишет `Commit HEAD a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD — `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (`docs: capture Source Pack baseline before Sprint 01 decisions`). a54a100 теперь HEAD~1; в коде между этими коммитами изменений нет (коммит документационный), ветка совпадает.
- Ключевая находка о тестах ЛОЖНА. Таблица раздела 5 утверждает «**Ни одного теста на `bot/tg/`.** В `tests/` — только dashboard/platform/security | VERIFIED», и это же вынесено в Top Critical Findings как «[Q-05] Нулевое тестовое покрытие Telegram-бота (4 792 строки)». Фактически `tests/security_tests/test_phase0_security.py:28` делает `from tg.middlewares.auth import is_authorized, resolve_chat_id`, а класс `TelegramAuthTests` (строки 47-62) содержит 3 теста на fail-closed whitelist и `resolve_chat_id`. Покрытие крайне низкое, но НЕ нулевое.
- Утверждение [T-03] «`bot/security/dashboard_auth.py` … Модуль — мёртвый код» и Quick Win «Удалить мёртвый `bot/security/dashboard_auth.py`» неполны. Grep документа был ограничен каталогом `bot/` (в этих границах он верен — 0 совпадений), но модуль импортируется и исполняется тестом: `tests/security_tests/test_phase0_security.py:32` (`from security.dashboard_auth import register_dashboard_security`) и `:114` (`register_dashboard_security(self.app)`); файл `bot/security/dashboard_auth.py:73` эту функцию определяет. Прогон `pytest tests/security_tests/test_phase0_security.py` — 9 passed. Удаление файла сломает проходящий security-тест — этого документ не предупреждает.
- Число команд посчитано неверно и противоречит собственному перечню: раздел 2 пишет «**12 команд:**», после чего перечисляет 14 (`/start`, `/dashboard`, `/portfolio`, `/positions`, `/orders`, `/operations`, `/balance`, `/stats`, `/signal`, `/status`, `/help`, `/game`, `/paper`, `/learning`). В коде ровно 14 регистраций `CommandHandler` в `bot/tg/bot.py:125-135,205,208,217` (плюс `/cancel` внутри трёх ConversationHandler). Тот же счёт «12 команд» повторён в Executive Summary.
- Rate limiting применён в 14, а не в 15 хендлерах. [T-06] п.4 перечисляет 15 модулей, включая `trading_bot`; фактически `grep is_allowed bot/tg/` даёт 14 вызовов в 13 файлах, а `bot/tg/handlers/trading_bot.py:14` только импортирует `rate_limiter` и ни разу не вызывает `is_allowed` — то есть управление ботом (`bot_start`/`bot_stop`) как раз НЕ защищено лимитом. Это усиливает, а не ослабляет находку, но цифра в документе неверна.
- [T-02] относит `cb_signal_execute` к путям исполнения сделок с риском «дублирующей заявки на реальные деньги». Фактически `bot/tg/handlers/signals.py:271-290` отвечает «📋 Исполняю в Paper Trading…» и вызывает `SignalsService(db_engine).execute_signal(signal_id)` — исполнение в paper-аккаунте, а не рыночная заявка брокеру. Отсутствие идемпотентности здесь реально, но денежного риска у этого конкретного колбэка нет; реальные деньги затрагивает только `trade_confirm` (`handlers/trading.py:213`).
- Мелкий дрейф номеров строк в [T-04]/[T-06]: `_allowed_ids()` указан как `:16-27`, фактически `bot/tg/middlewares/auth.py:14-25`; `is_authorized()` указан как `:38-53`, фактически `:37-52`; fail-closed-блок указан как `auth.py:44-49`, фактически `logger.error` на `:47-49` и `return False` на `:50`. Смысл сохранён полностью.
- Мелкое расхождение в счёте импортов: раздел 1 пишет «`from telegram` (25), `from telegram.ext` (23)». В границах `bot/tg/` — 25 и 22; в границах всего `bot/` (с legacy-модулем) — 26 и 23. Цифры взяты из разных областей.

## `06_BACKEND_DATA_AND_INTEGRATIONS_AUDIT.md`

- Снимок HEAD устарел: документ фиксирует `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD — `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (7f357e3, «docs: capture Source Pack baseline before Sprint 01 decisions»). Проверено `git diff --stat a54a100 HEAD`: единственный коммит сверху затронул только 15 файлов в `docs/source/` (+6032 строки), кода он не менял — поэтому все технические утверждения документа оценивались против неизменённого кода.
- ОШИБКА ФАКТА в доказательстве [B-01] (строка 105): утверждается «Ни `trades`, ни `paper_accounts` … не имеют колонки `owner_id` / `user_id` / `tenant_id`. Единственные `user_id` в схеме — `dashboard_sessions.user_id (schema.py:468,478)`». Фактически `bot/qf_platform/schema.py:137` объявляет `paper_accounts.user_id VARCHAR(64) NOT NULL DEFAULT 'default'` с `UNIQUE (user_id, mode)` на `:146`; колонка присутствует и в живой БД (`information_schema.columns` для `paper_accounts` → `user_id`). Суть находки (нет реальной модели владения — это строка со значением по умолчанию 'default', без FK и без принудительной фильтрации в репозиториях) остаётся верной, но перечень «единственных user_id» неполон, и [B-01] нельзя цитировать буквально.
- ОШИБКА ФАКТА в таблице доменных таблиц (строка 54): `trades` описан как «TimescaleDB-гипертаблица по `opened_at`». В актуальной схеме `bot/qf_platform/schema.py` вызова `create_hypertable` нет вообще (grep по `bot/` → 0 совпадений); единственное такое объявление — в устаревшем `quantflow_schema.sql:127` (`SELECT create_hypertable('trades', 'opened_at')`), который сам документ на строке 48 называет неиспользуемым дублем. В живой БД `SELECT hypertable_name FROM timescaledb_information.hypertables` возвращает ровно одну гипертаблицу — `candles`, а НЕ `trades`. Тем самым и утверждение Executive Summary «TimescaleDB-гипертаблица по времени» применительно к сделкам неверно.
- Числа в Executive Summary (строка 32) и в разделе «Что удалось проверить» (строка 16) не сходятся с репозиторием: «20 таблиц» → в `schema.py` 21 `CREATE TABLE` (и 21 таблица в схеме `public` живой БД); «все 18 репозиториев и 20 сервисов qf_platform» → в `bot/qf_platform/repositories/` 12 `.py` файлов и 11 классов `*Repository` (из них 10 конкретных + `BaseRepository`), в `bot/qf_platform/services/` 18 `.py` файлов и 18 классов `*Service` (20 классов `*Service` набирается только при поиске по всему `bot/`, за пределами `qf_platform`).
- Мелкие расхождения в номерах строк при верных фактах: `REQUIRED_COLUMNS` объявлен на `schema.py:551` (dict тянется до ~568), а документ ссылается на `schema.py:564-567` — это лишь хвост словаря; `SecretBox.from_env()` возвращает `None` на `encryption.py:45-48` (документ пишет 46-48). Формулировка о `trades` «~40 колонок» занижена: в живой БД у `trades` 47 колонок.

## `07_SECURITY_PRIVACY_AND_COMPLIANCE_AUDIT.md`

- Шапка фиксирует HEAD `a54a100b4d542f1d866b5f89336ce0703fea6ced` — устарел. Текущий HEAD = 7f357e358e6a5a7b9e067fccfa667caa74fdae15 (7f357e3, 'docs: capture Source Pack baseline before Sprint 01 decisions'); a54a100 находится ровно на один коммит ниже, и этот коммит только docs — ни один код-факт документа не затронут. Ветка совпадает.
- S-06 утверждает «`CLAUDE.md:139` при этом утверждает "Dashboard auth: JWT-based"». Утверждение по сути верно, но строка другая: реально CLAUDE.md:141 (`- **Dashboard auth**: JWT-based; credentials stored in bot/data/credential_vault.json`), файл всего 141 строка. Номер строки не сходится, факт сходится.
- S-07 говорит «Dashboard на 5051». Штатный порт — 5001: `bot/config.py:48` (`DASHBOARD_PORT`, default "5001"), `.env:20` `DASHBOARD_PORT=5001`, `.env.example:20` то же, CLAUDE.md таблица портов — 5001. Пробы, судя по всему, делались на нештатном порту; это не меняет вывода о 401, но цифра в документе вводит в заблуждение.
- §3 помечает открытый редирект как «Требует проверки / UNVERIFIED: не установлено, валидируется ли `next` как относительный путь», и это же вынесено в Quick Wins. Проверено: валидация ЕСТЬ — `bot/ui/templates/login.html:150-153` берёт `params.get('next')` и переходит только если `next.charAt(0) === '/' && next.charAt(1) !== '/'`, иначе '/' (т.е. protocol-relative `//evil.com` отклоняется). Серверный код `next` вообще не читает: `views.py:73` только записывает его в url_for, `grep args.get("next")` по bot/ui = 0. Пункт Quick Win фактически закрыт.
- S-06 доказывает мёртвость через «`grep -rn "dashboard_auth" bot/ --include=*.py` (исключая сам файл) → 0 совпадений» — это подтверждено (0). Но модуль НЕ полностью изолирован: `tests/security_tests/test_phase0_security.py:32` импортирует `security.dashboard_auth.register_dashboard_security` и патчит `security.dashboard_auth.config` (:127,:137); эти тесты сейчас зелёные (19 passed). Рекомендация «удалить» без правки теста сломает suite. Дополнительно: `docs/PROJECT_ARCHITECTURE.md:127,179` и `design/DASHBOARD_APPROVED_REFERENCE_AUDIT.md:304,1888,2084` до сих пор описывают `dashboard_auth.py` как активный механизм безопасности — ложное впечатление шире, чем описано.
- Executive Summary: «публичны ровно пять маршрутов». Точных публичных путей действительно 5 (`app_factory.py:247-253`), но в том же allow-list есть ещё два публичных ПРЕФИКСА — `/static/` и `/miniapp` (`app_factory.py:254`), т.е. публичная поверхность шире пяти маршрутов. Сам S-07 их перечисляет корректно, противоречие внутри документа.
- §4: «Circuit-breaker существует (`bot/reliability.py`), но к брокеру не применён» — пробел шире: `grep -rn "reliability" bot/ --include=*.py` (кроме самого файла) даёт 0 совпадений, т.е. `CircuitBreaker` (`bot/reliability.py:124`), `retry_sync`/`retry_async` не подключены НИГДЕ, а не только к брокеру.

## `10_CURRENT_DEVELOPMENT_STATUS.md`

- Снимок HEAD устарел на один коммит: документ пишет `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` («docs: capture Source Pack baseline before Sprint 01 decisions»), a54a100 теперь HEAD~1. Ветка `quant-site-approved-reference-redesign` совпадает.
- §12 п.4: «есть отдельный `run_forward_d1.py`, 652 строки» — фактически `bot/run_forward_d1.py` содержит 448 строк (`wc -l bot/run_forward_d1.py` → 448); файл не менялся с коммита b2b02c4, то есть 652 — ошибка исходного подсчёта, а не дрейф.
- §26 п.1: «44 Markdown-документа (`docs/` 15, `design/` 13, `website/docs/` 17)» не воспроизводится. Фактически: `find website/docs -name '*.md'` → 16 (не 17); `find docs -maxdepth 1 -name '*.md'` → 12; `find design -name '*.md'` → 13 (совпало). Всего .md в репозитории (без node_modules/.git) → 83, из них tracked 77. На коммите a54a100 tracked-.md в `docs/` было 11, в `design/` 11, всего 62.
- §21 п.1: «145 тест-функций в 7 файлах» — фактически 146 (`grep -c 'def test_'` по 7 файлам: 34+24+30+9+10+35+4 = 146). Число файлов (7) верно; pytest собирает 233 теста за счёт параметризации.
- §26 п.4: «актуальное описание архитектуры (создаётся этим Source Pack)» — уже создано: `docs/source/` содержит 15 .md (00…14) и зафиксирован коммитом 7f357e3; на момент a54a100 каталога `docs/source/` не существовало вовсе (`git ls-tree -r a54a100 | grep '^docs/source/'` → 0).
- §7 п.1: «python-telegram-bot 22.8» — это версия установленного пакета (`python3 -c 'import telegram; print(telegram.__version__)'` → 22.8), но `requirements.txt:17` фиксирует лишь `python-telegram-bot>=20.0`, то есть версия 22.8 не воспроизводима из манифеста. Документ не отмечает этот разрыв.
- §4 п.2: «`trading_db` Up 3 days» — рантайм-снимок, уже неверен: `docker ps` сейчас показывает `trading_db Up 4 days`. Привязка порта к `127.0.0.1:5432` подтверждена.
- §21 п.2: «161 passed, 72 skipped, exit 0, 1.44 s» — состав прогона совпадает точно, но время прогона отличается (базовый замер: 1.66 s). Это нормальный шум, отмечено для полноты.

## `11_MASTER_ROADMAP.md`

- Снимок HEAD устарел на один коммит: документ фиксирует `a54a100b4d542f1d866b5f89336ce0703fea6ced`, фактический HEAD = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (`7f357e3 docs: capture Source Pack baseline before Sprint 01 decisions`), a54a100 стал HEAD~1. Ветка совпадает. Коммит docs-only, поэтому ни один технический вывод Roadmap не сдвинулся.
- RM-P0-001 (строка 51): «`grep` по `tenant|subscription|billing|stripe` в `bot/` → 0 **на 187 файлов**». Результат grep подтверждён (0 совпадений), но число файлов неверно: `find bot -type f -name '*.py' | wc -l` = 176, `git ls-files bot | wc -l` = 213, всех файлов без __pycache__ = 217. Ни одно из измерений не даёт 187.
- RM-P0-005, DoD (строка 131): «`grep -rn "dashboard_auth" bot/` пуст либо модуль подключён». Этот DoD **уже формально выполнен** — grep по bot/ возвращает 0 совпадений (exit 1), при том что сам файл `bot/security/dashboard_auth.py` (2908 байт, `register_dashboard_security` на строке 73) существует и не удалён. Критерий не отличает «удалено» от «мёртвое, но лежит». Кроме того утверждение «не импортирован нигде» (строка 123) неточно: модуль импортируется из `tests/security_tests/test_phase0_security.py:32` и вызывается на строке 114 — удаление по RM-P1-013 сломает или обнулит этот тест-файл, о чём Roadmap не предупреждает.
- Матрица покрытия (строка 384): finding W-09 «Токен 11 px» отнесён к задаче **RM-P3-018**, которой в документе не существует — RM-P3-018 не описан ни в одной фазовой таблице (в Phase 3 есть только RM-P1-023, RM-P3-023, RM-P3-024, RM-P3-025, RM-P3-026). Заявление «Покрытие: 50 из 50 findings связаны минимум с одной задачей» (строка 421) для W-09 не выполняется: ссылка ведёт в пустоту. Сам токен подтверждён: `website/src/styles/tokens/typography.css:25` → `--text-label: 11px`.
- RM-P0-003, DoD «Node-job: `typecheck`, `lint`, `check` (4 проверки), `build`, `npm audit`, `format.test.mjs`, `check-dashboard-tokens.mjs`» (строка 97) смешивает два подпроекта: `check-dashboard-tokens.mjs` лежит в `bot/ui/static/check-dashboard-tokens.mjs`, а `format.test.mjs` — в `bot/ui/static/app/format.test.mjs`, то есть это гейты Flask-дашборда, а не Next.js-сайта; в `website/scripts/` их нет (там `check-design-tokens.mjs`). Node-job, собранный буквально по этому списку, придётся запускать из двух разных каталогов, чего DoD не оговаривает.
- RM-P0-008 (строки 171–172): ссылки на строки дают лёгкий дрейф и неполный путь. `bot/security/guards.py:199` — это строка `require_reason: bool = False` внутри сигнатуры, сам `def mutating(` начинается на 195. «`routes_actions.py:447`» и «`:514`» указывают на строки `idempotent=True`, тогда как декораторы `@mutating(` начинаются на 444 и 511; полный путь файла — `bot/ui/api/v2/routes_actions.py`, а не корневой `routes_actions.py`.
- Частная неточность RM-P0-009 (строка 192): «Раскрытие есть, но только в `faq.a10`». По коду `website/messages/ru.json` фраза об отсутствии оплаты действительно только в `faq.a10` («сейчас оплата вообще не подключена»), но `pricing.lead` уже содержит «Идёт закрытое тестирование, доступ открывается вручную» — то есть частичное раскрытие в самом блоке `#pricing` есть, и формулировку задачи стоит уточнить, а не писать пометку с нуля.

## `12_AGENT_WORKSTREAMS_AND_RESPONSIBILITIES.md`

- Шапка документа фиксирует `Commit HEAD = a54a100b4d542f1d866b5f89336ce0703fea6ced`, но фактический HEAD теперь `7f357e358e6a5a7b9e067fccfa667caa74fdae15` («docs: capture Source Pack baseline before Sprint 01 decisions»); a54a100 стал HEAD~1. Ветка совпадает. Это единственная реально устаревшая цифра документа.
- Направление «Website and Marketing» получает право менять `website/src/lib/seo/**` — каталог существует, но ПУСТ (`ls -la website/src/lib/seo` → total 0). Фактически SEO-метаданные (`generateMetadata`, openGraph, JSON-LD) живут в `website/src/app/[locale]/layout.tsx`. Агент, следующий карте владения буквально, будет искать SEO не там, где он есть.
- §6 требует оформлять ADR в `docs/adr/` — этого каталога в репозитории НЕ существует (единственный ADR — `website/docs/adr/0001-monogram.md`, он же указан как образец формата и действительно есть). Первый ADR придётся создавать вместе с каталогом; направление Documentation при этом заявлено владельцем `docs/adr/**`.
- Направление «DevOps and Infrastructure» указано владельцем `.github/**` — каталога `.github/` в репозитории нет, CI отсутствует (что согласуется с задачей RM-P0-003, но путь в карте владения описывает будущее, а не факт).
- §3 определяет «Движок» как `bot/main.py` + `services/bot_engine.py`; фактический путь — `bot/services/bot_engine.py` (файл существует, 3907 байт). Префикс `bot/` опущен, тогда как в остальных строках таблицы пути даны от корня репозитория.
- Особое правило §7 (Trading Engine) утверждает, что `place_market_order` глотает исключения и возвращает `None`. Это верно только для legacy-клиента `bot/broker/tinkoff_client.py:149-193` (`-> Optional[str]`, `except Exception: ... return None`). Новый адаптер `bot/broker/providers/tinkoff.py:320` (через `_do_market_order`) наоборот ПОДНИМА�Т `BrokerConnectionError`/`BrokerNotConfigured`. Формулировка «глотает» без указания файла может привести к неверной правке пути исполнения в новом адаптере.
- §1 предписывает сообщать владельцу о незакоммиченных чужих правках. На момент проверки рабочее дерево не чистое: 5 untracked файлов (design/DASHBOARD_UIUX_AUDIT.md, design/UIUX_DESIGNER_META_PROMPT.md, docs/BA_GPT_SYSTEM_PROMPT.md, docs/MARKETING_PROMPT_ENGINEER_GPT.md, docs/PROJECT_MANAGER_PROMPT_ENGINEER_GPT.md). Отслеживаемых модификаций нет — правило §1 сработает у каждого нового агента.

## `14_OPEN_QUESTIONS_AND_DECISIONS.md`

- Шапка: `Commit HEAD = a54a100b4d542f1d866b5f89336ce0703fea6ced`. Фактически `git rev-parse HEAD` = `7f357e358e6a5a7b9e067fccfa667caa74fdae15` (7f357e3, «docs: capture Source Pack baseline before Sprint 01 decisions»). a54a100 теперь на 1 коммит ниже HEAD. Ветка совпадает.
- Строка 21: «**Сводка:** 27 вопросов». Фактически в документе **37** заголовков `### OQ-` (`grep -c '^### OQ-'` = 37, полный перечень: PROD 1-4, BIZ 1-3, UX 1-4, ARCH 1-3, BACK 1-2, TRADE 1-4, AI 1-2, SEC 1-2, LEGAL 1-4, OPS 1-7, DOC 1-2). Расхождение на 10 вопросов.
- Внутреннее противоречие по числу блокеров: 🔴 стоит только в 4 заголовках (строки 179 OQ-ARCH-01, 242 OQ-TRADE-01, 315 OQ-SEC-01, 347 OQ-LEGAL-01), тогда как сводная таблица «блокирующих планирование» (строки 490-495) перечисляет 6 — добавлены OQ-LEGAL-03 и OQ-BIZ-01, у которых маркера 🔴 в заголовке нет.
- OQ-ARCH-01 (строка 182): «все 18 репозиториев». Фактически `grep -rn '^class .*Repository' bot/` = **12** классов, из них 10 доменных в `bot/qf_platform/repositories/` (auth, backtest, equity, events, market, paper, signals, signals_gate, strategies, trades) + `BaseRepository` + `UserRepository` в мёртвом `bot/auth/`. Оценка объёма multi-tenancy опирается на завышенное почти вдвое число.
- OQ-LEGAL-04 (строка 380): «`README.md` содержит бейдж `License: MIT`». Ложная посылка: `grep -ni 'licen\|лиценз' README.md` (1312 строк) — **0 совпадений**; в README 7 бейджей shields.io (Python, Next.js, Flask, PostgreSQL, Docker, Telegram, TypeScript), лицензионного среди них нет. Вторая половина утверждения верна: файла LICENSE в корне нет. То есть вопрос надо переформулировать: лицензия не заявлена вовсе, а не «заявлена MIT без файла».
- OQ-ARCH-02 (строка 196-198): «Срок удаления `/api/v1` … ~30 legacy-маршрутов». Blueprint с префиксом `/api/v1` в коде отсутствует: есть `legacy_bp` без префикса (`bot/ui/legacy_api.py:34`, 13 маршрутов `/health`, `/api/tinkoff/*`, `/api/stats`, `/api/settings*`, `/api/candles`, `/api/portfolio`, `/api/equity`, `/api/positions`, `/api/metrics`, `/api/log`, `/api/internal/push`) и `platform_bp` с `url_prefix='/api/platform'` (`bot/ui/api/platform_routes.py:22`, 30 маршрутов). По определению самого кода (`bot/ui/api/v2/__init__.py:8`: «`/api/v1` (the legacy `/api/*` and `/api/platform/*` routes)») legacy-семейство = 13+30 = **43** маршрута, а не ~30.
- OQ-BACK-02 (строка 230-233) поставлен как открытый: «Не установлено, отвергается ли абсолютный URL». Ответ уже есть в коде: `bot/ui/templates/login.html:150-153` — `window.location.assign(next && next.charAt(0)==='/' && next.charAt(1)!=='/' ? next : '/')` с комментарием «Only a same-origin relative path — an open redirect on a login form is a phishing primitive». Серверная сторона (`bot/ui/views.py:73`) только записывает `next=request.path`, то есть значение всегда относительное. Вопрос фактически закрыт и должен быть снят или переформулирован (защита только клиентская).
- OQ-OPS-07 (строка 478): «5 коммитов впереди `origin`». Фактически `git status -sb` → `[ahead 6]`; `git rev-list --count origin/quant-site-approved-reference-redesign..HEAD` = 6.
- Процессное указание строки 19 («Принятое решение оформляется как ADR в `docs/adr/`») не поддержано инфраструктурой: каталога `docs/adr` в репозитории нет (`ls -d docs/adr` → No such file or directory), ADR не заведено ни одного. Ни один из 37 вопросов формально не закрыт.
- OQ-LEGAL-02 (строка 360) говорит про таблицу `audit_events` — таблица определена в `bot/qf_platform/schema.py`, а НЕ в корневом `quantflow_schema.sql` (`grep audit_events quantflow_schema.sql` → 0). Мелкая, но существенная неточность для того, кто пойдёт искать схему по корню репозитория.

