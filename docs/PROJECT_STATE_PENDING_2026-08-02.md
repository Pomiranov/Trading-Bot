# Правки в PROJECT_STATE, подготовленные 02.08 и НЕ ВНЕСЁННЫЕ

**Почему не внесены.** Действует запрет трогать боевую папку
`D:\Trading-Bot-Nik` — её читает прогон 00:15. Изменения PROJECT_STATE от 02.08
лежат там **незакоммиченными**, поэтому в этом worktree их нет и внести правки
поверх них нельзя. Документ — очередь на применение, когда запрет снимут.

Порядок применения: сверху вниз. Разделы 1–2 **отменяют** ранее записанное, и
применять их надо раньше остальных.

---

## 1. ⛔ ОТОЗВАНО: «П3 + 0.7 одним заходом» на основании «ложь по расписанию»

**Отзывает Ник, 02.08 вечером. Посылка была неверна, и неверна была моя.**

**Что было записано (в разделе «Заведено 2026-08-02» и в порядке работ):**

> 🚨 «Прогон отчитался успехом без сессии» будет приходить каждые выходные и
> каждый биржевой праздник, то есть станет ложью по расписанию.

**Почему отозвано.** Замер по ISS 02.08: MOEX **торгует по выходным**. За
01.05–02.08 бары есть у **22 выходных дней из 28**. Неторговых выходных дней
за три месяца — **шесть** (три пары). Тревога придёт не еженедельно, а
**примерно раз в месяц**.

**Что из этого следует — и это важнее самой ошибки:**

> **Сторож был ПРАВ, а П3 в задуманной форме гасил бы правдивый редкий сигнал.**

Отсутствие торгов 01–02.08 — редкое событие (третий случай за три месяца), и
сообщить о нём человеку **правильно**. Я предложил его заглушить, опираясь на
неверную посылку «это рутина». Правило 9 §8 сработало в обратную сторону:
расписанием биржи нельзя ни защищаться, ни **объяснять**.

**Что остаётся в силе:** шаг **0.7 (календарь MOEX от ISS) по-прежнему нужен**,
но **основание другое**:

> Не «погасить частую ложную тревогу», а **отличить редкий неторговый день от
> сломанной загрузки свечей**. Сегодня сторож честно говорит «либо рынок
> закрыт, либо сломалась загрузка» и различить не может. Календарь нужен, чтобы
> различал — то есть чтобы сигнал стал ТОЧНЕЕ, а не тише.

**П3 в прежней форме — снят с очереди.** Если к нему возвращаться, то как к
«прогон сообщает, что торгов не было, и это подтверждено календарём», а не как
к глушителю.

---

## 2. 🔢 ИСПРАВЛЕНИЕ АРИФМЕТИКИ: было «12 из 14», верно «22 из 28»

**Моя ошибка, найдена Ником.** В отчёте 02.08 стояло «12 выходных из 14 —
торговые». Пересчитано:

| единица счёта | всего | торговых | неторговых |
|---|---|---|---|
| **выходных ДНЕЙ** | **28** | **22** (78.6%) | 6 |
| выходных ПАР (сб+вс) | 14 | 11 | 3 |
| **расколотых пар** (сб торгуют, вс нет или наоборот) | — | — | **0** |

«12 из 14» не соответствует ни одной из единиц — это просто просчёт.
Обе верные записи: **22 из 28 дней** = **11 из 14 пар**.

### Единица счёта — ДЕНЬ, и от этого зависит форма календаря 0.7

Расколотых пар в выборке нет: все три неторговых случая — целые пары
(09–10.05, 20–21.06, 01–02.08). Соблазн считать парами велик, и его надо
назвать и отвергнуть:

> **Календарь 0.7 обязан быть ПОДЕНЬНЫМ.** «Ноль расколотых пар» — свойство
> выборки из 14 пар, а не закон биржи. Календарь, устроенный парами, при первом
> же расколотом выходном соврёт про один из двух дней, и соврёт молча. Это
> ровно правило 8 §8: список вместо предиката.
>
> Форма остаётся прежней: **множество торговых дат, полученное от ISS**
> (`.../candles.json?...&interval=24`), где наличие бара и означает сессию.
> Праздники попадают в него сами, отдельного списка не нужно.

---

## 3. 📌 ДОЛГ №43 и ШАГ 2 «решение по выходным» — ПОДНЯТЬ, основание переформулировать

### Прежняя рамка

№43 записан как «выходные сессии в опорных тройках, 56% PnL», а крупнейшая
сделка `TATN +88 366.01` — вход на **субботней** сессии. Рамка читалась как
«аномалия, попавшая в тройку».

### Новая рамка: выходные — не аномалия, а регулярная часть рынка

Замер 02.08 по всей истории набора (951 сессия, 2023-04-17 → 2026-07-31):

| год | сессий | из них выходных | доля |
|---|---|---|---|
| 2023 | 182 | 0 | **0.0%** |
| 2024 | 256 | 3 | 1.2% |
| 2025 | 318 | 65 | **20.4%** |
| 2026 | 195 | 46 | **23.6%** |
| **всего** | **951** | **114** | **12.0%** |

**Три вывода, каждый меняет постановку:**

1. **Выходные сессии — регулярная черта рынка с 2025 года**, а не выброс.
   Сейчас это **примерно каждая четвёртая сессия**. Оценка «~2 из 7» (28.6%)
   близка к режиму 2026 года, но выше измеренных 23.6%; для опорного окна
   целиком она завышена вдвое.
2. **Опорное окно и боевой режим РАЗЛИЧАЮТСЯ ПО СОСТАВУ.** Тройки посчитаны на
   трёх годах, где выходных **12.0%**, а форвард работает в режиме, где их
   **23.6%** — вдвое больше. То есть бэктест недопредставляет ровно тот тип
   сессии, для которого проскальзывание не смоделировано. Это самостоятельный
   довод, отдельный от концентрации на одной сделке.
3. **56% PnL при 12.0% сессий — перепредставленность примерно в 4.7 раза**,
   а не в 2. Но читать это как «выходные прибыльны» нельзя: 69% разреза с
   фильтром даёт **одна сделка** TATN. Концентрация и календарь здесь смешаны,
   и разделить их может только перемер.

### 🚩 Формулировка, которую записать прямо

> **Вопрос «эти деньги настоящие» НЕ решается ни календарём, ни перемером
> троек. Он решается ТОЛЬКО моделью проскальзывания — долг №44, форма которой
> ждёт выбора Ника.**
>
> Пока модели нет, любая тройка, содержащая касания на тонких барах, остаётся
> **предварительной по построению**, и ни один довод «за» или «против» выходных
> сессий на неё опереться не может. Замер объёмов 31.07 это уже показал: три из
> пяти касаний тройки с фильтром стоят на барах 3–10-го перцентиля, их
> суммарный вклад **−17 392.67**.
>
> Это не «ещё одна причина сделать №44», а **условие осмысленности шага 2**:
> решение по выходным, принятое до модели, будет принято по числам, о которых
> заранее известно, что они смещены.

### Что делать с шагом 2

Шаг 2 остаётся разделённым на два вопроса, как и был (исполнение / данные), но
основание вопроса **(а) вход на выходной сессии** усиливается и меняется:

* **было:** «пять касаний в тройке пришлись на тонкие бары»;
* **стало:** «выходные — четверть текущих сессий, они структурно тоньше будних
  примерно в 15 раз (замер 31.07 по 2 200+ барам), и доля их в боевом режиме
  вдвое выше, чем в окне, на котором считались тройки».

---

## 4. 🔍 ЗАМЕР ОБЪЁМА СУББОТНЕГО БАРА TATN (31.07) — ПЕРЕЧИТАН В НОВОМ СВЕТЕ

**Не пересчитывался.** Ниже — цитата из PROJECT_STATE и что она значит теперь.

### Что было замерено 31.07 (дословно)

> Ждали, что тонкая суббота обнулит `TATN +88 366.01`. Замерено обратное:
>
> | бумага | сессия | роль | объём бара | медиана бумаги | доля | перцентиль | pnl |
> |---|---|---|---|---|---|---|---|
> | **TATN** | 2026-02-28 | вход | **1 873 167** | 1 890 479 | **99.1%** | **49.2%** | +88 366.01 |
>
> **Бар TATN 2026-02-28 ликвиден: 99.1% медианы бумаги, 49-й перцентиль —
> обычный день.** Условие «объём малая доля обычного» НЕ выполнено, значит
> `+88 366.01` — **не число, которого не было**.
>
> Тонкость, которую замер вскрыл попутно: **для СУББОТЫ этот бар аномален**.
> Медианный выходной объём TATN — 95 586 (4.5% от буднего), а тут 1 873 167,
> то есть **в 20 раз больше типичной субботы**. Почему — не выяснено; читать
> как «в этот выходной торги шли как в будни», не как «выходные у TATN
> ликвидны».

### Что этот результат значит ТЕПЕРЬ

**Вывод замера устоял полностью.** Запрет цитирования снят правильно, сделка
реальна, число не фантомное. Новых оснований его пересматривать нет.

**Но одна фраза из него теперь читается иначе.** «В этот выходной торги шли как
в будни» в июле выглядело как объяснение единичной странности. Сегодня известно,
что выходные торги — регулярный режим MOEX, и вопрос переворачивается:

> Замер 31.07 показал два числа по TATN, и они несовместимы как описание одного
> режима: медиана выходного бара **95 586** и конкретная суббота **1 873 167**,
> в 20 раз больше. Пока выходные считались редкой аномалией, разброс списывался
> на саму аномальность. Теперь, когда выходные — четверть сессий, разброс в
> 20 раз внутри одного типа сессии становится **самостоятельным вопросом к
> данным**: либо выходная ликвидность растёт со временем и медиана по всей
> истории смешивает два разных режима, либо 2026-02-28 действительно особая
> дата.

**Проверка дешёвая, но СЕГОДНЯ НЕ ДЕЛАЛАСЬ и в очередь не ставится без Ника:**
медиана выходного объёма TATN **по годам** (2025 против 2026) вместо медианы по
всей истории. Если медиана 2026 года на порядок выше медианы 2025 — «в 20 раз
больше типичной субботы» посчитано против устаревшей базы, и системный замер
«выходные тоньше будних в 15 раз» тоже надо разложить по годам.

**Что НЕ меняется:** концентрация. TATN даёт 69% разреза с фильтром и 167% без.
Тройка по-прежнему измеряет одну сделку сильнее, чем стратегию.

---

## 5. 🕯 КАНДИДАТ В ДОЛГ: роль БД бота — суперпользователь. Номер за Ником

> **РОЛЬ, КОТОРОЙ ХОДИТ ВЕСЬ КОД, ИМЕЕТ ПРАВА АДМИНИСТРАТОРА НАД ВСЕМ
> КЛАСТЕРОМ.**
>
> **Область — предикатом:** «процесс не должен иметь прав на действия, которых
> он по назначению не совершает».
>
> **Замер, 02.08, `SELECT rolname, rolsuper, rolcreatedb, rolcreaterole FROM
> pg_roles WHERE rolcanlogin`:**
>
> | роль | rolsuper | rolcreatedb | rolcreaterole |
> |---|---|---|---|
> | **`trader`** | **t** | **t** | **t** |
> | `mcp_ro` | f | f | f |
>
> `trader` — это `DB_USER` из `.env`, то есть роль, которой ходят форвард,
> сторож, бэктест, дашборд и все измерительные скрипты.
>
> **Чем это опасно.** Суперпользователь в Postgres не ограничен ни владением, ни
> GRANT-ами: `DROP DATABASE`, `DROP TABLE`, `TRUNCATE` по любой базе кластера
> проходят без препятствий. Между «ошибка в коде» и «данных нет» не стоит
> НИЧЕГО. Резервных копий боевой БД в проекте не записано.
>
> **Класс тот же, что у №60.** №60: барьер против живой торговли должен падать
> при импорте, а не проверяться дисциплиной. Здесь: барьер против разрушения
> данных должен стоять в правах роли, а не в аккуратности запросов. В обоих
> случаях защита существует только как намерение автора.
>
> **Почему это всплыло именно сейчас.** При разборе плана репетиционного слота:
> разделение «боевая БД / клон» по имени базы **не является гарантией**, пока
> роль суперпользовательская — она дотянется до любой базы кластера. То есть
> дефект уже блокирует конкретную работу.
>
> **Предлагаемая форма (обсуждаемая, не решённая):**
> 1. рабочая роль `quantflow_app` — **без** `SUPERUSER`/`CREATEDB`/`CREATEROLE`,
>    с `GRANT` только на нужные таблицы своей базы;
> 2. `trader` остаётся ролью администратора и **из `.env` уходит** — им
>    пользуется человек при миграциях, а не процессы;
> 3. отдельная read-only роль для сторожа и измерительных скриптов: они и так
>    открывают сессию `readonly=True`, и право на запись им не нужно вовсе
>    (образец уже есть — `mcp_ro`);
> 4. проверка **предикатом**, а не памятью: тест падает, если `DB_USER` из
>    окружения имеет `rolsuper = t`. Без него правка держится до первой
>    переустановки контейнера.
>
> **Оговорка о цене:** пункты 1–3 меняют боевую БД и требуют миграции прав; это
> не однострочная правка и в текущий заход не входит. Пункт 4 дёшев и может
> идти первым — он хотя бы делает состояние видимым.

---

## 6. 🔬 ГИПОТЕЗА (ЗАПИСАТЬ, НЕ ПРОВЕРЯТЬ): один драйвер как причина ДВУХ проблем

> **Г3. `VBoxNetLwf` — кандидат в общую причину `0x9F` при засыпании И медленной
> публикации порта Docker после сна (§10б).**
>
> **Что измерено 02.08:**
> * VirtualBox 7.2.10 установлен 24.06.2026, **виртуальных машин ноль**, файлов
>   `.vbox`/`.vdi` нет — используется по назначению **никак**;
> * драйвер `VBoxNetLwf` (lightweight filter) — `Running`, тип запуска `System`;
> * привязан и **включён на ПЯТИ адаптерах из пяти**: `Ethernet`,
>   `vEthernet (WSL)`, `vEthernet (Default Switch)`, `Ethernet 2`, `Ethernet 3`;
> * при каждой загрузке пишет в System по две ошибки
>   «The driver detected an internal driver error»;
> * bugcheck `0x9F` с `Parameter1 = 0x3` — «устройство не завершило переход по
>   питанию», `SleepInProgress = 6`.
>
> **Почему это может быть ОДНА причина двух разных симптомов.** `vEthernet (WSL)`
> — коммутатор, через который работает Docker Desktop. Фильтр `VBoxNetLwf` стоит
> **в его сетевом пути**. Тогда:
> * при **засыпании** фильтр не отдаёт переход по питанию → `0x9F`;
> * при **пробуждении** тот же фильтр задерживает подъём коммутатора → Docker
>   публикует порт минутами, а не секундами (§10б), и гейт `.bat`
>   (`DAEMON_WAIT_S` = 180, `DBWAIT_TIMEOUT` = 450) существует именно под это.
>
> Обе проблемы до сих пор считались независимыми и лечились по отдельности:
> первая — переносом слота (0.6), вторая — бюджетами ожидания в `.bat`.
>
> **⚠ Это ГИПОТЕЗА, и ни одного её звена не проверено.** Связь «фильтр →
> задержка публикации порта» не измерена вовсе; связь «фильтр → `0x9F`» опирается
> на класс bugcheck и на ошибки драйвера в журнале, но не на разбор дампа.
> Совпадение по времени (первый `0x9F` серии 06.07, установка 24.06) — корреляция.
>
> **Проверка ОДНА и обратимая:** отвязать `VBoxNetLwf` от адаптеров (или удалить
> неиспользуемый VirtualBox) и наблюдать **оба** эффекта — прекратились ли
> `0x9F` и сколько времени Docker публикует порт после сна. Один опыт закрывает
> или отвергает сразу две гипотезы, и это делает его самым дешёвым из
> оставшихся.
>
> **🚫 НЕ СЕГОДНЯ.** Опыт меняет настройки машины, а прогон 03.08 00:15 обязан
> пойти в тех же условиях, в которых сделано предсказание §2з. Ставить после
> сверки и приёмки блока A.
>
> **Что понадобится, чтобы опыт что-то значил:** до отвязки — снять базовый
> замер времени публикации порта Docker после сна (сейчас его нет, есть только
> качественное «минутами» из §10б). Без базы «стало быстрее» будет впечатлением.

---

## 7. Правки, подготовленные ранее 02.08 (перенесены сюда целиком)

### 7.1 Признак Р4 в таблице различающих признаков

**Было:** «биржа не торговала 01–02.08, состояние не двигалось».

**Стало:** «сессий 01.08 и 02.08 нет **в самом ISS** (проверено 02.08 в 21:00,
спустя 44 ч после закрытия сессии 01.08) — состояние двигаться не могло».
Расхождение означало бы, что 02.08 баров не было по задержке публикации, а не
по отсутствию торгов.

**Почему.** Прежняя формулировка выводила отсутствие баров из дня недели.
Измерено: MOEX торговал 22 выходных дня из 28.

### 7.2 Результат узкого замера дыр (№41)

> #### ✅ УЗКИЙ ЗАМЕР ДЫР (№41) ВЫПОЛНЕН 02.08: НОЛЬ
>
> Окно 12.07 → 02.08, все 20 бумаг, сверка с MOEX ISS потикерно.
> **Дыр 0, фантомов 0**; 20 сессий × 20 бумаг = 400 бар-слотов на месте.
> Сырые данные — локально, `measurements/2026-08-02_blockA_part2/`.
>
> **⇒ №41 выше П2/П1 не поднимается.** Дефект не закрыт — `DELETE` + `INSERT`
> по-прежнему в разных транзакциях, — но основания «уже потеряли бары» нет.
> Окно накрывает **10 восстановлений БД** после нечистой остановки, и все
> десять данные пережили. Это подтверждает «пока везло», а не «не может
> случиться».

### 7.0 ЗАМЕР 02.08 (поздний): выходные сессии отдельно в IS и в OOS

**Граница IS/OOS:** `IS_END = pd.Timestamp("2025-01-01")`,
`bot/backtest/run_osc_oos_debug.py:47`. Разметка — по **дате входа**:
`"IS" if t.entry_date < IS_END else "OOS"`. Окно разреза
`2023-07-12 → 2026-07-29`.

**Доля выходных сессий** (набор 12, на котором считались разрезы):

| половина | сессий | выходных | доля |
|---|---|---|---|
| IS `2023-07-12 … 2024-12-31` | 379 | **3** | **0.8%** |
| OOS `2025-01-01 … 2026-07-29` | 511 | **111** | **21.7%** |

Три «выходные» сессии IS — `2024-04-27`, `2024-11-02`, `2024-12-28`, все
**субботы без воскресений**. Воскресных сессий до 2025 года нет ни одной. Это
рабочие субботы производственного календаря, а **не** выходные торги.

**Настоящие выходные торги начинаются `2025-03-01` (сб) + `2025-03-02` (вс)** и
дальше идут регулярными парами.

**Сделки, у которых вход ИЛИ выход на выходной сессии** (московская сессия, а
не UTC-дата — см. предупреждение ниже):

| разрез | половина | сделок | из них выходных | PnL половины | PnL выходных | доля |
|---|---|---|---|---|---|---|
| с фильтром | IS | 4 | **0** | +547.26 | 0 | **0%** |
| с фильтром | **OOS** | 18 | **4** (22%) | +127 747.97 | **+70 973.34** | **55.6%** |
| без фильтра | IS | 5 | **0** | +43 310.36 | 0 | **0%** |
| без фильтра | OOS | 29 | **7** (24%) | +52 937.30 | +28 664.98 | 54.1% |

55.6% — это и есть «56% PnL» долга №43; замер воспроизведён независимо, и те же
четыре сделки дают ровно пять касаний из таблицы объёмов 31.07 (TATN 28.02 вход,
MOEX 17.01 вход, MOEX 24.01 выход, SBER 24.01 выход, ALRS 02.03 вход).

> ⚠ **Ловушка, в которую я попал при первом счёте, — долг №16.** Метки в
> разрезе стоят в UTC (`2026-02-27 21:00`), и `.date()` по ним даёт сессию
> МИНУС ОДИН ДЕНЬ: суббота 28.02 читается как пятница 27.02. Первый прогон
> замера дал из-за этого −26.7% вместо +55.6% и другой список сделок.
> Считать только через `session_date` (метка + 3 ч). Это третий случай долга
> №16 в проекте.

#### Ответ на вопрос: граница НЕ разрезает перелом — она лежит ДО него

Перелом (`2025-03-01`) целиком **внутри OOS**, через два месяца после его
начала. IS не содержит режима выходных торгов вовсе: 0.8% его «выходных» — это
рабочие субботы.

**Что это означает для тройки `18 / 72.2% / 1.638 / +127 748`:**

1. **IS и OOS различаются не временем, а микроструктурой рынка.** В OOS есть
   тип сессии, которого в IS нет физически. «Настроено на IS, проверено на OOS»
   здесь не выполняется в обычном смысле: OOS проверяет правила на рынке,
   которого в IS не было.
2. **Больше половины PnL тройки — с сессий, отсутствующих в IS.** 55.6% при 22%
   сделок. Проверить это на IS нечем: там таких сессий ноль.
3. **Без касаний выходных тройка другая** — пересчитано на том же разрезе:

   | | n | WR | PF | PnL |
   |---|---|---|---|---|
   | опорная тройка | 18 | 72.2% | **1.638** | +127 747.97 |
   | только касания выходных | 4 | 75.0% | 2.276 | +70 973.34 |
   | **без касаний выходных** | **14** | **71.4%** | **1.392** | **+56 774.63** |

   Доля попаданий почти не меняется (72.2% → 71.4%), а **PF падает
   1.638 → 1.392 и PnL до 44% исходного**. То есть вклад выходных — в РАЗМЕРЕ
   сделок, а не в частоте угадывания.
4. **Проскальзывание на этих сессиях не смоделировано (№44).** Оценка знака
   поправки не даётся: она и есть предмет №44.

*Замер, не решение. В очередь ничего не ставится.*

### 7.0-бис ПРЕДУСЛОВИЕ ВЫБОРА ФОРМЫ №44: перцентиль зависит от популяции

Второй замер — медиана объёма выходного бара **TATN по годам** вместо медианы
по всей истории. Гипотеза была: медиана по истории смешивает два режима, и
«в 20 раз больше типичной субботы» посчитано против устаревшей базы.

**ГИПОТЕЗА ОПРОВЕРГНУТА.**

| год | будних баров | медиана будних | выходных баров | медиана выходных | % от буднего |
|---|---|---|---|---|---|
| 2023 | 123 | 2 296 515 | 0 | — | — |
| 2024 | 253 | 2 160 929 | 3 | 1 669 188 | 77.2% |
| 2025 | 253 | 2 036 546 | 63 | **90 569** | **4.4%** |
| 2026 | 149 | 1 954 266 | 46 | **106 939** | **5.5%** |
| вся история | — | 2 107 269 | 112 | 95 586 | 4.5% |

Между 2025 и 2026 медиана выходного объёма **не уехала** (4.4% → 5.5%).
Строка 2024 (77.2%) — это три рабочие субботы, обычные торговые дни; три бара из
112 медиану по истории не сдвигают. Значит база сравнения 31.07 была корректной,
и «в 20 раз больше типичной субботы» остаётся верным.

**Но замер дал другое, и оно и есть предусловие:**

> **Один и тот же бар TATN `2026-02-28` = 1 873 167:**
> * **49.2-й перцентиль** против всех баров бумаги (замер 31.07) — обычный день;
> * **98.2-й перцентиль** против всех выходных баров бумаги;
> * **97.8-й перцентиль** против выходных баров 2026 года.
>
> **Перцентиль объёма — не свойство бара, а свойство ПАРЫ «бар + популяция
> сравнения».** Пока популяция не названа, число «перцентиль 49.2» и число
> «перцентиль 98.2» одинаково истинны и ведут к противоположным решениям.

**⇒ ПРЕДУСЛОВИЕ ДЛЯ №44.** Если форма М3 считает перцентили объёма, выбор формы
**обязан начинаться с назначения популяции сравнения**, и назначение это надо
записать как часть определения модели, а не как деталь реализации. Пулинг
будних и выходных баров в одну популяцию делает модель слепой внутри выходных:
при медианах 2 036 546 и 90 569 (разница ~22×) почти любой выходной бар
проваливается в нижние проценты общего распределения, и различить внутри
выходных тонкий бар от аномально ликвидного станет нечем — а именно это
различие и стоит за `TATN +88 366.01`, крупнейшей сделкой обеих троек.

*Записано как предусловие. Форма №44 не выбирается, в очередь не ставится.*

### 7.1 ЗАМЕР: сведение разреза БЕЗ ФИЛЬТРА — сошлось, но не с тем эталоном

**Постановка была: «с фильтром сошлось до копейки, без фильтра — нет».
Проверено: без фильтра тоже сошлось до копейки — с ДЕЙСТВУЮЩИМ эталоном.**

| | n | WR | PF | PnL |
|---|---|---|---|---|
| замер по архивному разрезу (`post25_off`, as-of 29.07) | 29 | 58.6% | 1.141 | **+52 937.30** |
| записано в PROJECT_STATE как опорная тройка | 29 | 58.6% | 1.14 | **+52 937.30** |
| число из постановки | 30 | 60.0% | 1.20 | +74 055 |

`30 / 60.0% / 1.20 / +74 055` — **не действующий эталон**. PROJECT_STATE уже
запретил его цитировать, дважды и явно:

> «Число `30 / 60.0% / 1.20 / +74 055` больше цитировать нельзя как результат:
> замерено, что тот же код на трёх соседних окнах давал `+74 054.52`,
> `+67 747.03`, `+77 851.57`, и весь разброс создавала одна незакрытая позиция»
> (раздел «ГДЕ МЫ СЕЙЧАС»)

> 🌀 «`+74 055` — СНИМОК на данных по сессию 28.07, не измерение. Разрез без
> фильтра содержит открытую на краю данных позицию SBER и потому дрейфует
> каждый день: на данных по сессию 29.07 то же число равно **+77 852**
> (PF 1.21). Долг №25.»

#### Причина названа: долг №25, и он ЗАКРЫТ 30.07

Не иной состав набора и не иная конвенция комиссии — **открытая на краю данных
позиция SBER**, попадавшая в счётчики до закрытия №25. Разложение по двум
шагам, оба взяты из уже записанных замеров:

| шаг | n | PnL | что изменилось |
|---|---|---|---|
| as-of 28.07, до №25 | 30 | +74 055 | исходный снимок |
| as-of 29.07, до №25 | 30 | +77 852 | **+3 797** — дрейф той же открытой позиции за один день |
| as-of 29.07, после №25 | **29** | **+52 937.30** | **−24 915** — позиция исключена из `n/WR/PF/PnL` |

Обе дельты порождены **одной и той же** позицией.

**Догадка из постановки — «расхождение живёт целиком в сделках, которые фильтр
отсекает» — ПОДТВЕРЖДЕНА.** Дрейфующая позиция существовала только в разрезе
без фильтра; фильтр её отвергает. Именно поэтому разрез с фильтром был стабилен
и сошёлся сразу, а без фильтра — нет. Это же зафиксировано в PROJECT_STATE:
«Разрез **с** фильтром сегодня стабилен, но это свойство данных, а не
конструкции».

**⇒ Запрет цитирования нужен не тройке без фильтра, а числу `30 / 60.0% / 1.20 /
+74 055`** — и он уже стоит. Тройка `29 / 58.6% / 1.14 / +52 937.30` сведена и
цитируема, с провенансом из пункта (а) ниже.

### 7.2 ЗАМЕР: вложенность наборов на IS — наборы ВЛОЖЕНЫ ТОЧНО

Сопоставление по ключу «тикер | сессия входа | сессия выхода», D1 IS:

| | тикер | вход | выход | pnl с фильтром | pnl без фильтра | дельта |
|---|---|---|---|---|---|---|
| 1 | NVTK | 2023-09-27 | 2023-10-04 | +30 226.44 | +30 226.44 | 0.00 |
| 2 | NVTK | 2023-12-05 | 2023-12-14 | −55 428.22 | −55 428.22 | 0.00 |
| 3 | CHMF | 2023-12-26 | 2024-01-04 | +31 523.90 | +31 523.90 | 0.00 |
| 4 | GAZP | 2024-04-02 | 2024-04-15 | −5 774.86 | −5 774.86 | 0.00 |
| **5** | **LKOH** | **2024-09-09** | **2024-09-19** | **отсечена фильтром** | **+42 763.10** | — |

Совпадающих ключей **4 из 4**; сделок «только с фильтром» — **ноль**; PnL
общих сделок совпадает **до копейки**. Набор с фильтром — строгое подмножество
набора без фильтра. **Сравнение допустимо.**

#### 🚩 ВЫВОД О ДОВЕРИИ К ФИЛЬТРУ, сформулированный явно

> **На всём in-sample фильтр сделал ровно одно действие: отсёк единственную
> сделку, которая принесла почти всю прибыль половины.**
>
> * IS без фильтра: 5 сделок, **+43 310.36**;
> * отсечённая LKOH: **+42 763.10** — это **98.7%** прибыли IS;
> * IS с фильтром: 4 сделки, **+547.26** — то есть практически ноль.
>
> Формулировка «фильтр на IS стоит −43k» в проекте уже есть (`yaml:129`,
> 1 756 − 44 810 = −43 054 по старой конвенции комиссии). Здесь она уточнена по
> механизму: это **не диффузная цена по многим сделкам, а одна отсечённая
> сделка**. Четыре сделки, которые фильтр пропустил, дают в сумме +547.26 —
> статистически неотличимо от нуля при разбросе отдельных сделок в ±30–55 тысяч.
>
> **Что отсюда следует и чего НЕ следует.** Не следует, что фильтр плох: n = 5
> — не выборка, и одна сделка ничего не доказывает ни в одну сторону. Следует
> другое: **in-sample не содержит свидетельства в пользу фильтра вообще**. Всё
> основание фильтра лежит в OOS, где он даёт 18 сделок против 29. То есть
> конструкция «настроено на IS, проверено на OOS» здесь вывернута: на IS
> проверять нечем, а OOS — единственный источник и настройки, и проверки
> одновременно. Вместе с пунктом 7.0 (в IS нет режима выходных торгов) это
> второе независимое основание для провенанса из пункта (а).

### 7.3 ЗАМЕР: граница IS/OOS задана ТРИЖДЫ. Класс долга №24

| файл | строка | как задано |
|---|---|---|
| `bot/backtest/run_osc_oos_debug.py` | 47 | `IS_END = pd.Timestamp("2025-01-01")` |
| `bot/backtest/run_wrd_backtest.py` | 45 | `IS_END = pd.Timestamp("2025-01-01")` |
| `bot/backtest/run_ab_swing_stop.py` | 48 | `IS_END = pd.Timestamp("2025-01-01")` |

Три **независимых литерала** одной константы. Плюс ещё **девять текстовых
копий** той же даты в подписях и докстрингах, которые ни с чем не сверяются:
`run_ab_swing_stop.py:14,152,153`, `run_ab_wrd_sar.py:128,129`,
`run_osc_oos_debug.py:9,10`, `run_wrd_backtest.py:151,152`.

Правильное поведение в проекте **уже есть, рядом**: `run_ab_wrd_sar.py:32`
делает `from backtest.run_wrd_backtest import metrics, IS_END` — переиспользует,
а не копирует. И начало окна устроено как надо: `SAMPLE_START_2026_07 =
date(2023, 7, 12)` живёт в `bot/universe.py:163` в ОДНОМ месте, имеет отпечаток
`SAMPLE_START_2026_07_VERSION` (`universe.py:199`) и стережётся тестом
`test_universe.py:301`.

**Тест, стерегущий `IS_END`, — НЕ СУЩЕСТВУЕТ НИ ОДНОГО.**

> **Чем это опасно конкретно.** Конец окна приколочен и проверяется, начало
> окна приколочено и проверяется, а **граница, делящая окно на настройку и
> проверку, — нет**. Сдвиг её в одном из трёх файлов не уронит ничего: каждый
> скрипт продолжит печатать «IS (до 2025-01-01)» из своей текстовой копии, и
> два разреза, посчитанные разными скриптами, разъедутся молча. Это ровно тот
> класс, ради которого заведён №24, и ровно то, что уже случилось с ставкой
> комиссии в восьми местах (раздел 2а).
>
> Дополнительный оттенок, найденный замером 7.0: `IS_END` — **не просто число,
> а граница режима рынка**. Выходные торги начинаются 2025-03-01, то есть
> смещение `IS_END` на два месяца вперёд перенесло бы часть нового режима в IS
> и изменило бы смысл обеих половин, а не только их размер.

*Замеры. Ничего не решено, в очередь не поставлено.*

### 7.4 Записи по итогам замеров (а–г)

#### (а) Провенанс опорной тройки `18 / 72.2% / 1.64 / +127 748`

Помечать всюду, где она цитируется:

> **OOS-проверкой в обычном смысле НЕ является.** Два независимых основания,
> оба измерены 02.08:
> 1. **В IS всего 4 сделки** (без фильтра 5), и фильтр отсёк там ровно одну —
>    ту, что несла 98.7% прибыли половины. Настраивать и проверять на таком IS
>    нечем; OOS оказывается единственным источником и настройки, и проверки.
> 2. **Режим выходных торгов в IS отсутствует физически** (0.8% «выходных» IS —
>    рабочие субботы), а в OOS это 21.7% сессий и **55.6% PnL тройки**.
>    Половины различаются микроструктурой рынка, а не только временем.

#### (б) Архив истории MOEX проблему режима НЕ решает

Углубление истории до 2023 и раньше **не даёт** ни одной выходной сессии:
первая — 2025-03-01, до неё за всю глубину набора только три рабочие субботы
2024 года. Режиму выходных торгов на 02.08.2026 — **17 месяцев**, и удлинение
окна назад увеличивает долю бумаг и лет, но **уменьшает** долю нового режима.

⇒ Данные о поведении стратегии на выходных сессиях **даёт только время**.
Единственный способ их набрать — наблюдать вперёд. Это меняет цену форварда: он
не дублирует бэктест, а измеряет то, чего в бэктесте нет по построению.

#### (в) Кандидат в долг: дату сессии — ТОЛЬКО через `session_date`

> **Область — предикатом:** «московская дата сессии не выводится из UTC-метки
> иначе как через `market_time.session_date`».
>
> **Три случая одной природы, все с неверным выводом:**
>
> | когда | где | что дал `.date()` по UTC |
> |---|---|---|
> | до 28.07 | данные `candles` | сессия минус один день у 96% баров D1 (долг №16) |
> | 28.07 | `forward_healthcheck._bar_date` | «обработано до 2026-07-26» при сессии 27.07; возраст 2 при пороге 2 вместо 1 |
> | 02.08 | мой замер выходных сессий | −26.7% вместо +55.6% и другой список сделок; суббота 28.02 прочиталась как пятница 27.02 |
>
> Третий случай показателен тем, что произошёл **у того, кто знал про первые
> два и только что про них писал**. Значит дисциплиной класс не закрывается.
>
> **Предлагаемая форма:** предикат по AST — запретить `.date()` на выражении,
> происходящем из колонки времени/метки бара, вне `market_time`. Образец —
> «второй тест №60 — предикатом по AST». Грепом не ловится: `.date()`
> встречается законно на уже-локальных датах.
>
> Номер и приоритет — за Ником.

#### (г) Шаг 0.7: календарь обязан различать рабочую субботу и выходную сессию

Измерено: в IS есть три субботние сессии — `2024-04-27`, `2024-11-02`,
`2024-12-28`, **все без воскресений**, и медиана их объёма у TATN 1 669 188 =
**77.2% от буднего**, тогда как у настоящих выходных сессий 2025–2026 это
**4.4–5.5%**. Это рабочие субботы производственного календаря: обычные торговые
дни, случайно попавшие на субботу.

> **По дню недели они неотличимы от выходной торговой сессии, а по смыслу
> противоположны:** у рабочей субботы ликвидность будняя, у выходной — в 20 раз
> ниже. Календарь, отвечающий только «торговали / не торговали», для №44
> недостаточен: модель проскальзывания получит одинаковый ответ для сессий с
> двадцатикратно разной ликвидностью.
>
> ⇒ Календарь 0.7 обязан давать **три состояния**, а не два: неторговый день /
> обычная торговая сессия / **выходная торговая сессия**. Различитель —
> не день недели, а факт: рабочая суббота идёт **без парного воскресенья** и
> объявляется производственным календарём РФ. Оба признака проверяемы
> данными ISS, но первый — прямо, без внешнего справочника.

### 7.6 ЗАМЕР: разложение пустоты IS по бумагам. Ответ — НЕ ОДИН ИЗ ТРЁХ, А ДВА РАЗНЫХ В ДВУХ ПОЛОВИНАХ IS

Метод: покрытие — запросом к `candles`; сделки — по архивным разрезам
`osc_debug_post25_{on,off}` (run 31.07, as-of 29.07); состояние фильтра —
**переиспользованием предиката проекта** `signals.indicators.
structural_downtrend_series` с параметрами из `rules_osc_range.yaml`
(`sma_short=50, sma_long=200, lower_low_lookback=120, lower_low_window=20`).

> **Метод проверен воспроизведением независимо записанного числа.** Тот же
> предикат на той же глубине даёт «фильтр закрыт у всех 12» в **19 сессиях из
> 892 (2.1%)** и непрерывной серией **2026-07-13 … 2026-07-31** — совпадает с
> замером 01.08 (§2и) точно, включая дату начала серии.

#### ⚠ Ограничение, которое надо назвать первым

Разрезы, на которых стоят обе опорные тройки, посчитаны на
`MEASUREMENT_UNIVERSE_2026_07` — **12 бумаг**, а не 20. Разложение сделок «по
20 бумагам» из существующих артефактов **получить нельзя**: их нет. Покрытие
данными дано по 20, сделки — по 12, и смешивать эти таблицы нельзя.

#### 1. Данные в IS: полны

| | результат |
|---|---|
| сессий в окне IS | 379 |
| бумаг набора 12 с полным покрытием | **12 из 12**, по 379 баров, дыр 0 |
| бумаг набора 20 с полным покрытием | 19 из 20; **VKCO 356 баров, 23 дыры** |

VKCO в набор 12 не входит. **Объяснение (а) — ОТВЕРГНУТО.**

#### 2–4. Сделки потикерно (набор 12; «отс.» = отсечено фильтром)

| тикер | IS без | IS с | IS отс. | OOS без | OOS с | OOS отс. |
|---|---|---|---|---|---|---|
| ALRS | 0 | 0 | 0 | 4 | 2 | 2 |
| CHMF | 1 | 1 | 0 | 1 | 0 | 1 |
| GAZP | 1 | 1 | 0 | 1 | 1 | 0 |
| LKOH | **1** | **0** | **1** | 1 | 1 | 0 |
| MGNT | 0 | 0 | 0 | 3 | 1 | 2 |
| MOEX | 0 | 0 | 0 | 5 | 3 | 2 |
| NVTK | 2 | 2 | 0 | 1 | 1 | 0 |
| PLZL | 0 | 0 | 0 | 0 | 0 | 0 |
| ROSN | 0 | 0 | 0 | 2 | 2 | 0 |
| SBER | 0 | 0 | 0 | 5 | 3 | 2 |
| SNGS | 0 | 0 | 0 | 2 | 0 | 2 |
| TATN | 0 | 0 | 0 | 4 | 4 | 0 |
| **итого** | **5** | **4** | **1** | **29** | **18** | **11** |

Бумаг, давших хотя бы одну сделку: **в IS 4 из 12**, в OOS **11 из 12**.
Частота без фильтра: IS **0.283**, OOS **1.538** сделки на бумагу в год —
разница **5.4 раза**, и она возникает при ВЫКЛЮЧЕННОМ фильтре.

**🚩 Столбец «сколько сигналов ДО фильтра» отсутствует, и подменять его я не
стал.** Число сделок без фильтра — это НИЖНЯЯ ГРАНИЦА числа сигналов: сигнал мог
не стать сделкой из-за капитала, открытой позиции или разогрева. Прямой счёт
сигналов из существующих артефактов не извлекается; `skipped_signals` — таблица
форварда, в ней 3 строки.

#### 5. Состояние фильтра по сессиям IS — и здесь ответ переворачивается

Все 12 бумаг достигают бара №200 в **одну дату: 2024-04-22**. До неё
`structural_downtrend_series` возвращает `False` по строке `mask.fillna(False)`
— **«истории не хватило» неотличимо от «даунтренда нет»**.

| часть окна | сессий | закрытых бумаг: медиана | среднее | макс | сессий «закрыт у всех 12» |
|---|---|---|---|---|---|
| **IS до 2024-04-22** | **199 (53% IS)** | **0** | **0.0** | **0** | 0 |
| **IS с 2024-04-22** | 180 (47% IS) | **9 / 12** | 6.8 | 11 | 0 |
| **OOS целиком** | 511 | 6 / 12 | 5.2 | 12 | 17 |

**Сессий, где фильтр закрыт у всех 12 сразу, в IS — НОЛЬ.** Все 19 таких сессий
за всю глубину данных лежат в июле 2026.

#### 🚩 ПРЯМОЙ ОТВЕТ: верны (б) и (в), но в РАЗНЫХ половинах IS

Пустота IS имеет **две разные причины**, и граница между ними измерена —
2024-04-22:

**Первая половина IS (199 сессий, 53%) — объяснение (б).** Фильтр здесь
**инертен по построению**: 0 закрытых бумаг во всех 199 сессиях. Он не может
ничего гасить. И тем не менее за 199 сессий на 12 бумагах возникло **4 сделки**.
Это чистая пустота потока сигналов, к фильтру отношения не имеющая.

**Вторая половина IS (180 сессий, 47%) — объяснение (в).** Фильтр здесь
**жёстче, чем в OOS**: медиана 9 закрытых бумаг из 12 против 6 в OOS. За эти
180 сессий возникла ровно **одна** сделка — LKOH 2024-09-09, — и фильтр
**отверг именно её**. То есть на той части IS, где фильтр работал, он отсёк
**1 из 1**.

**Объяснение (а) отвергнуто** обоими половинами: данных полный комплект.

> **Формулировка, которую просили дать прямо, и она подтверждается:
> «улучшение OOS» и «уничтожение IS» есть ОДНО поведение фильтра на разных
> рынках.** Правило одно и то же. В OOS оно отсекает 11 сделок из 29 и поднимает
> PF 1.141 → 1.638. В работающей половине IS то же правило стоит закрытым на
> 9 бумагах из 12 (в полтора раза плотнее, чем в OOS) и отсекает единственную
> появившуюся сделку, унося 98.7% прибыли половины. Разница не в фильтре, а в
> рынке: 2024 год для этого набора — структурный даунтренд, 2025–2026 — нет.
>
> **Оговорка о силе вывода, обязательная:** n = 1. «Отсёк 1 из 1» и «отсекает
> всё» — разные утверждения, и второе этим замером не доказано.

#### 🕯 Находка, найденная этим замером (не долг, кандидат в наблюдение)

`structural_downtrend_series` заканчивается `mask.fillna(False)`, и докстринг
называет это прямо: «NaN (недостаточно истории) трактуется как False — фильтр не
активен». Следствие, которого в проекте не записано:

> **53% окна IS — период, в котором A/B «с фильтром против без фильтра» не
> является A/B вообще:** обе ветви там тождественны по построению. Сравнение
> `IS с фильтром` (4 / +547) против `IS без фильтра` (5 / +43 310) молча
> смешивает 199 сессий, где фильтр отсутствует, со 180, где он работает.
>
> Это класс «молчаливый ноль»: отсутствие данных и отсутствие сигнала дают
> одинаковый `False`. Живой путь `is_structural_downtrend` ведёт себя так же
> (`len(df) < sma_long → False`), то есть расхождения между контурами здесь нет
> — есть общая слепота обоих.

*Замер. Ничего не решено, в очередь не поставлено.*

### 7.7 Две записи по итогам предыдущих замеров

#### Различитель рабочей субботы — измерен на n=3 и правилом быть НЕ МОЖЕТ

В разделе (г) выше предложено отличать рабочую субботу от выходной торговой
сессии по **отсутствию парного воскресенья**. Основание — три случая:
`2024-04-27`, `2024-11-02`, `2024-12-28`.

> 🚩 **n = 3 — это не основание для правила календаря.** Признак «нет парного
> воскресенья» получен на трёх наблюдениях одного года и не проверен ни на
> будущем, ни на других режимах биржи. Правилом шага 0.7 он быть не может:
> первая же рабочая суббота, попавшая рядом с торговым воскресеньем, сломает
> его молча — ровно правило 8 §8.
>
> **Источник календаря — ISS, и только он.** Календарь строится из множества
> дат, которые ISS отдаёт как сессии. Парность (суббота без воскресенья)
> остаётся **перекрёстной проверкой** — способом заметить странность и пойти
> посмотреть, — но не определением и не различителем.
>
> Третье состояние календаря («выходная торговая сессия») по-прежнему нужно —
> см. (г), ликвидность различается в 20 раз. Но выводиться оно обязано из
> данных ISS плюс внешнего производственного календаря РФ, а не из парности.

#### Дефект `IS_END`: форма починки названа

> **Дефект.** Граница IS/OOS задана **тремя независимыми литералами**
> (`run_osc_oos_debug.py:47`, `run_wrd_backtest.py:45`,
> `run_ab_swing_stop.py:48`) плюс **девятью текстовыми копиями** даты в подписях
> и докстрингах. **Теста нет ни одного.** Класс долга №24.
>
> **Форма — по готовому образцу, который в проекте уже работает:**
> 1. одна константа в `bot/universe.py` рядом с `SAMPLE_START_2026_07`;
> 2. **отпечаток** по идиоме `sample_version(...)`, как
>    `SAMPLE_START_2026_07_VERSION` (`universe.py:199`);
> 3. **тест** по образцу `test_universe.py:301`
>    (`self.assertEqual(SAMPLE_START_2026_07, date(2023, 7, 12))`);
> 4. три литерала заменяются импортом; текстовые подписи собираются из
>    константы, а не пишутся руками.
>
> **Довод, поднимающий цену дефекта:** `IS_END` — не просто число, а **граница
> режима рынка**. Замер 7.6 показал, что 2024-04-22 делит IS на две части с
> противоположным поведением фильтра, а 2025-03-01 вводит выходные торги.
> Сдвиг `IS_END` меняет не размер половин, а их СМЫСЛ, и молчаливый разъезд трёх
> копий означал бы, что два скрипта считают разные рынки, называя их одинаково.

### 7.9 ОПИСЬ МОЛЧАЛИВЫХ НУЛЕЙ НА ПУТИ РЕШЕНИЯ (предикат по AST)

#### Правило 12 §8 выполнено ДО прогона по проекту

Подсадка: 12 заведомо положительных случаев (по два на каждый вид) и **6
отрицательных контролей** (`fillna(True)`, обязательный ключ без умолчания,
`raise` вместо `return`, проброс исключения, непустые умолчания `"strict"`).

**Результат: поймано 12 из 12, на контролях — 0 срабатываний.**

| вид | поймано |
|---|---|
| `.fillna(<умолчание>)` | 2/2 |
| `.get(k, <умолчание>)` | 2/2 |
| `getattr(o, a, <умолчание>)` | 2/2 |
| `... or <умолчание>` | 2/2 |
| охрана по нехватке данных → `return <умолчание>` | 2/2 |
| `except → return <умолчание>` / `except → pass` | 2/2 |

#### 🚩 Чего предикат НЕ ловит — названо, потому что ноль без этого читается шире, чем есть

1. **Разрешение, выраженное непустой структурой.** `return {"approved": True, …}`
   не является «умолчанием» синтаксически. Самая опасная находка описи (Ф4 ниже)
   найдена **чтением, а не предикатом**.
2. **Нелитеральные нейтральные умолчания:** `or Decimal("0.5")` — это вызов, а не
   константа (Ф5).
3. **Отсутствующую проверку.** Пропущенного условия в AST нет.
4. **Направление.** Предикат находит КЛАСС конструкции. Разрешительный `False`
   фильтра и запретительный `False` условия для него неразличимы. Поэтому 529
   срабатываний — это кандидаты, а не находки.

#### Масштаб и честная оговорка про мой собственный инструмент

Просканировано **155 файлов**, срабатываний **529**. Замыкание импортов от
`bot/run_forward_d1.py` — **27 файлов**; на них лежит **137** срабатываний.

> 🚩 **Моя карта областей сама подпала под правило 8 §8.** Я перечислил области
> списком (движок правил, индикаторы, риск, бэктест, живой прогон, загрузка), и
> `bot/learning/trading_orchestrator.py` в него не попал — уехал в «вне области
> решения» вместе с **63 живыми срабатываниями**. А именно этот файл содержит
> ворота, через которые проходит решение открыть позицию. Предикат его нашёл;
> спрятал его мой список. Область надо было задать предикатом «всё, что
> достижимо импортом от `run_forward_d1`», а не перечислением.

#### НАХОДКИ: разрешительный ответ на пути решения

«Живой» = файл в замыкании импортов от `run_forward_d1.py`.
«Открыть позицию» = может ли ответ повлиять на решение открыть длинную.

| # | место | что означает разрешительный ответ | живой | открыть позицию |
|---|---|---|---|---|
| **Ф1** | `bot/learning/trading_orchestrator.py:416` | `if len(rows) < sma_long: return None  # мало истории — фильтр не применяем`. По докстрингу `:393` **`None` = «торговать можно»** | **ДА** | **ДА, напрямую** |
| **Ф2** | `trading_orchestrator.py:402` | `if not cfg or not ticker: return None` — конфиг фильтра пуст ⇒ разрешено. Цепочка: `:379 data.get("filters") or {}` → `:381 filters.get("structural_downtrend") or {}` → `{}` → `:402` | **ДА** | **ДА, напрямую** |
| **Ф3** | `bot/signals/indicators.py:120` `mask.fillna(False)`; `:126` `if empty or len<sma_long: return False` | «истории мало» = «даунтренда нет» = не блокировать. Образец класса | **ДА** | **ДА** |
| **Ф4** | `trading_orchestrator.py:191-205` | `if not strategy_stats:` → `return {"approved": True, "confidence": 0.5}`. **Неизвестная стратегия РАЗРЕШАЕТСЯ** | **ДА** | **ДА, напрямую** |
| **Ф5** | `trading_orchestrator.py:207` | `confidence = strategy_stats["confidence"] or Decimal("0.5")` — NULL в БД ⇒ нейтральные 0.5 вместо отказа | **ДА** | **ДА** |
| **Ф6** | `bot/risk/risk_manager.py:275-276` | `float(info.get("avg_price") or 0)`, `int(info.get("lots") or 0)` — брокер не ответил ⇒ позиции «нет» ⇒ экспозиция невидима | **ДА** | ДА, если живая торговля включена (сейчас заперта №60) |
| **Ф7** | `risk_manager.py:196` | `if data is None: return 0.0` в `register_close` — незарегистрированное закрытие даёт нулевой реализованный убыток ⇒ дневной лимит потерь недосчитан | **ДА** | косвенно: разрешает СЛЕДУЮЩУЮ сделку |
| **Ф8** | `bot/backtest/engine.py:432,434,447` | `getattr(rules,"structural_downtrend_filter",{}) or {}` → `if not cfg: return None` (гейт выключен); `.fillna(False)` при reindex | нет | нет, но **мерящий**: этим механизмом P3-тест со `StubRules` молча гонялся без фильтра |
| **Ф9** | `rules_engine.py:221` против `trading_orchestrator.py:379` | **две независимые читалки одного ключа yaml**, ожидающие РАЗНУЮ форму: `or []` (список) и `or {}` (словарь). В файле `filters:` — словарь. Совпадение поведения при отсутствии ключа случайно: обе дают пустоту | **ДА** | латентно; класс долга №24 |

#### Что на пути решения ведёт себя ПРАВИЛЬНО — названо, чтобы опись не читалась как «всё сломано»

Эти отказывают, а не разрешают (fail-closed):

* `run_forward_d1.py:534` `len(rows) < MIN_HISTORY_BARS → пропуск тикера`;
  `:541` нет закрытого бара → пропуск; `:579` данные протухли → пропуск;
* `risk_manager.py:80,91` — не посчитать размер позиции ⇒ позиции нет;
* `rules_engine.py:602,606,626` — условие не вычислилось ⇒ правило не сработало;
* `indicators.py:433,438,440,463,470` — нет дивергенции или триггера ⇒ нет сигнала.

#### 🚩 Главное следствие: охрана и дыра стоят на РАЗНЫХ путях данных

`run_forward_d1.py:124` объявляет `MIN_HISTORY_BARS = 250` с комментарием
«SMA200 фильтра + запас», и `_prepare_ticker` отказывается обрабатывать тикер с
меньшей историей. Но **фильтр эту охрану не проходит**:
`_check_structural_downtrend` читает свечи **своим** запросом
(`trading_orchestrator.py:410-414`). Это уже записано в докстринге
`run_forward_d1.py:186-190` — но записано как довод про отрицательный контроль,
а не как дыра в охране.

⇒ Охрана `MIN_HISTORY_BARS` защищает путь СИГНАЛА; путь ФИЛЬТРА защищён только
собственным `return None`, то есть **не защищён, а открыт**.

#### ФАКТ: сколько баров у боевого набора сегодня

Порог фильтра `sma_long = 200`, охрана прогона `MIN_HISTORY_BARS = 250`.

| | значение |
|---|---|
| минимум по набору | **OZON, 860 баров** |
| максимум | AFLT / FLOT / MTLR / SMLT, 951 |
| ближе 250 баров к порогу 200 (то есть < 450) | **НИ ОДНОЙ** |
| наименьший запас над порогом 200 | **660 баров** (OZON) |
| наименьший запас над `MIN_HISTORY_BARS` 250 | 610 баров (OZON) |

**⇒ Ф1 и Ф3 сегодня недостижимы на живом пути** — не потому, что закрыты, а
потому, что данных много. Это свойство данных, а не конструкции (правило 3 §8).
Достижимыми они станут при добавлении в набор новой бумаги с короткой историей —
а набор `FORWARD_TICKERS` объявлен растущим.

**Ф2, Ф4, Ф5, Ф7 от объёма данных не зависят и достижимы всегда.**

*Опись. Ничего не решено, в очередь не поставлено.*

### 7.10 Четыре записи по итогам

#### «IS с фильтром 4 / +547» — ОТОЗВАТЬ как результат

> Четыре сделки прошли в **199 сессиях, где фильтр выключен по построению**
> (`mask.fillna(False)`, все 12 бумаг достигают бара 200 только 2024-04-22).
> На честном окне — с 2024-04-22 — сделок с фильтром **НОЛЬ** (единственная
> возникшая, LKOH 2024-09-09, фильтром отсечена).
>
> ⇒ Пара «`IS с фильтром 4 / 50.0% / 1.01 / +547`» против «`IS без фильтра
> 5 / 60.0% / 1.71 / +43 310`» **не является A/B фильтра**: 53% окна обе ветви
> тождественны. Провенанс ставить рядом с КАЖДЫМ цитированием, как это уже
> сделано для `+74 055`.

#### Честное начало окна измерений для правила с окном N баров

> **Определение:** честное начало = дата, когда **ВСЕ** бумаги набора достигли N
> баров. Для фильтра даунтренда (`sma_long = 200`, набор 12) это **2024-04-22**.
>
> Прогрев обязан **исключаться из измерения явно**, а не входить молча. Сегодня
> он входит молча дважды: `mask.fillna(False)` не отличает прогрев от «нет
> даунтренда», а разрез не печатает, какая часть окна пришлась на прогрев.
>
> Замечание о форме: у окна уже есть приколоченное начало
> (`SAMPLE_START_2026_07`) и приколоченный конец (`--as-of`, долг №37). Честное
> начало — **третья** такая величина, и заводить её надо тем же способом:
> вычисляемой из набора и порога, а не литералом.

#### Темп накопления форварда — не константа планирования

> Частота сделок при **выключенном** фильтре различается между половинами в
> **5.4 раза**: IS 0.283 против OOS 1.538 сделки на бумагу в год. Фильтр в этом
> не участвует — он выключен в обеих цифрах. Значит различие есть **свойство
> рынка**, а не стратегии и не настройки.
>
> ⇒ Любой план вида «наберём N сделок за T» опирается на темп, который менялся в
> 5.4 раза внутри собственного опорного окна. Темп обязан цитироваться с
> указанием половины и режима, а прогноз сроков — как диапазон, а не число.

#### У боевого контура нет описывающих его чисел

> Обе опорные тройки посчитаны на **`MEASUREMENT_UNIVERSE_2026_07` — 12 бумагах**
> (`universe.py:50-53`, «НЕ РАСШИРЯТЬ… изменение делает невоспроизводимыми все
> прошлые PF»). Форвард идёт на **`FORWARD_TICKERS` — 20 бумагах**.
>
> ⇒ Ни одна цитируемая тройка не описывает то, что работает в бою. Перемер на 20
> **блокирован**: №41 (`DELETE`+`INSERT` разными транзакциями) и №42 (рваные ряды:
> у OZON 91 пропущенная сессия в OOS, у VKCO 23 в IS).
>
> Это не «надо перемерить», а **именованный разрыв между измеренным и
> работающим**, и он обязан стоять рядом с любой фразой «у нас PF 1.64».

### 7.12 ТЯЖЕСТЬ Ф1/Ф2/Ф4/Ф5/Ф6/Ф7 — измерена, две находки переклассифицированы

#### Вопрос 1: знает ли `trading_orchestrator` про `osc_range_moex_d1_fwd`

Ворот два, и они читают из РАЗНЫХ источников. Оба проверены значением.

**Ворота А — конфиг фильтра.** Источник:
```python
trading_orchestrator.py:73   STRATEGY_RULES_FILES = {"osc_range_moex": _RULES_DIR / "rules_osc_range.yaml"}
trading_orchestrator.py:370  if not (strategy_id or "").startswith(prefix): continue
```
Сопоставление **по префиксу**, не по равенству.
`"osc_range_moex_d1_fwd".startswith("osc_range_moex")` → **True**. **ЗНАЕТ.**

**Ворота Б — `belief_system`.** Запрос к боевой БД:

| strategy_id | confidence | total_trades | заведена |
|---|---|---|---|
| `osc_range_moex_d1_fwd` | **0.5000** | **0** | 2026-07-28 17:16:19 |

Строка **ЕСТЬ** (одна из 14, все засеяны одним заходом 28.07). **ЗНАЕТ.**

**⇒ Ф2 и Ф4 для боевой стратегии сегодня НЕ СРАБАТЫВАЮТ.** Ворота её узнают.

**Но настоящая поверхность Ф2 измерена и она шире.** Префикс в
`STRATEGY_RULES_FILES` ровно один. Из 14 стратегий в `belief_system` ему
соответствуют **5** (`osc_range_moex`, `…_d1`, `…_d1_fwd`, `…_h1`, `…_h4`) и **не
соответствуют 9**: `breakout_moex`, `funding_bybit`, `gap_moex`,
`momentum_bybit`, `trend_bybit`, `trend_moex`, `trend_moex_d1`, `trend_moex_h1`,
`trend_moex_h4`. Для этих девяти `_structural_filter_config` возвращает `{}` →
`:402` → `return None` → **фильтр не применяется вовсе, сигнал разрешён**.

> То есть ворота всегда разрешают **не для боевой стратегии, а для девяти
> остальных**, и молча: отсутствие фильтра ничем не отличается от пройденного
> фильтра. Сегодня это не стреляет только потому, что запущена одна стратегия из
> четырнадцати.

**Уточнение к Ф5 — механизм назван неверно, поверхность осталась.**
`MIN_STRATEGY_CONFIDENCE = Decimal("0.20")` (`:63`), а у боевой стратегии
`confidence = 0.5000` при `total_trades = 0`. Значение в БД **не NULL**, так что
ветка `or Decimal("0.5")` не задействована. Работает другое: **посевное значение
0.5 при нуле сделок проходит порог 0.2**. Класс тот же (отсутствие свидетельства
даёт разрешение), механизм — посев, а не NULL.

**Уточнение к Ф4 — дефект самостирающийся.** `_bootstrap_strategy` (`:453`)
делает `INSERT … ON CONFLICT DO NOTHING` сразу после разрешения. Значит после
первого же срабатывания строка появляется, и «неизвестная стратегия» больше
никогда не выглядит неизвестной. След остаётся только в логе
(`«Стратегия %s не найдена в belief_system — разрешаем с базовым confidence 0.5»`).

#### Вопрос 2: исполняется ли этот путь в живом прогоне — ДА, доказано двумя артефактами

**Артефакт 1 — строки в боевой БД `skipped_signals`:**

| когда | strategy_id | тикер | причина |
|---|---|---|---|
| 2026-07-30 13:40:41 UTC | **`osc_range_moex_d1_fwd`** | PHOR | `structural_downtrend` |
| **2026-08-01 08:38:54 UTC** = 11:38:54 МСК | **`osc_range_moex_d1_fwd`** | TATN | `structural_downtrend` |
| 2026-07-12 14:57:13 UTC | `osc_range_moex_d1` | GAZP | `structural_downtrend` |

Метка 11:38:54 МСК лежит **внутри прогона 01.08** (старт 11:38:44, финиш
11:38:56) — тот самый прогон-догон, вокруг которого шёл разбор №46.

**Артефакт 2 — `logs/forward_d1.log`:**
```
quantflow.evaluator: Сигнал отклонён: osc_range_moex_d1_fwd TATN — structural_downtrend
quantflow.orchestrator: TradingOrchestrator: все модули подключены        × 11
```

**⇒ Переклассификации Ф1–Ф5 не требуется: «живой» подтверждён исполнением, а не
только достижимостью.** Более того, запись отказа доказывает, что исполнение
**прошло мимо обеих охран и они не сработали**: до `:431` можно дойти, только
если `:402` нашёл конфиг и `:416` увидел ≥ 200 баров. Это здоровый исход — и он
же показывает, что ворота реально стоят на пути, а не в стороне от него.

#### Вопрос 3: Ф6 и Ф7 — ПЕРЕКЛАССИФИЦИРОВАНЫ, форвард их не зовёт

Из `RiskManager` форвард вызывает **только два обращения**:

```
run_forward_d1.py:676   self.risk.cfg.atr_stop_multiplier      (чтение конфига)
run_forward_d1.py:745   self.risk.calculate_position(...)      (сайзинг)
```

`register_open` / `register_close` / `trailing_stop` / `reconcile_with_broker`
зовутся **только** из `bot/main.py:147,263,344,387`,
`bot/gateway/trade_gateway.py:156`, `bot/ui/telegram_bot.py:695` и тестов. Ни
один из этих файлов форвард-прогон не исполняет.

Это **уже записано** в PROJECT_STATE (раздел 3, «Форвард не касается
`risk_state.json`», проверено 28.07) — я при составлении описи это не поднял.

> 🚩 **Почему в описи стояло «живой: ДА» — и это моя ошибка измерения.** Колонка
> «живой» считалась как **достижимость ФАЙЛА импортом** от `run_forward_d1.py`.
> `risk_manager.py` импортируется (`run_forward_d1.py:98`) и экземпляр создаётся
> (`:409`), поэтому файл в замыкание попал. Но **достижимость файла ≠ исполнение
> функции**. Для Ф1–Ф5 исполнение доказано артефактами; для Ф6/Ф7 его нет.
>
> **Ф6 и Ф7 переносятся в «путь живой торговли, сейчас не исполняется».** Тяжесть
> падает до отложенной: они выстрелят, когда пойдёт `main.py`, то есть после
> снятия запрета живой торговли (№60).

**Ф7 и долг №9 — ДВА дефекта на одном пути, и они складываются.**

| | №9 (TOCTOU) | Ф7 (молчаливый ноль) |
|---|---|---|
| место | `check_trade_allowed` (`risk_manager.py:127`) читает состояние в `with transaction()`, но проверки лимитов идут ПОСЛЕ выхода из блока; `register_open` (`:185`) открывает отдельную транзакцию | `register_close` (`:196`): `if data is None: return 0.0` |
| природа | конкурентность: между чтением и записью лок не удерживается | умолчание: нет записи об открытии ⇒ нулевой реализованный результат |
| цена | две позиции по тикеру либо превышение `max_open_positions` | дневной лимит потерь недосчитан |

**Это не один дефект.** Но они сцеплены в одну цепочку: отказ №9 порождает
ровно то состояние, в котором Ф7 возвращает `0.0` — позиция, о чьём открытии
записи нет. Тогда её закрытие даёт нулевой убыток, и **урон от №9 становится
невидимым для дневного лимита** вместо того, чтобы быть громким.

⇒ Формулировка для реестра: **№9 создаёт ущерб, Ф7 его прячет.** Чинить №9 без
Ф7 значит оставить сокрытие; чинить Ф7 без №9 — сделать ущерб видимым, но не
устранённым. Порядок — вместе, в ветке напарника (№9 ждёт его ответа).

#### Сводка тяжести после замера

| # | ворота узнают боевую стратегию | путь исполняется | тяжесть сегодня |
|---|---|---|---|
| Ф1 | — | ДА | **латентна**: 860 баров минимум против порога 200 |
| Ф2 | **ДА** (префикс совпал) | ДА | **латентна для боевой; открыта для 9 из 14 стратегий** |
| Ф3 | — | ДА | латентна, та же причина, что Ф1 |
| Ф4 | **ДА** (строка есть) | ДА | латентна и **самостирающаяся** |
| Ф5 | — | ДА | **действует**: посев 0.5 при 0 сделок проходит порог 0.2 |
| Ф6 | — | **НЕТ** | отложена до живой торговли |
| Ф7 | — | **НЕТ** | отложена; прячет ущерб №9 |

*Замер. Ничего не решено, в очередь не поставлено.*

### 7.13 Четыре записи по итогам

#### ГЕЙТ: расширение `FORWARD_TICKERS` имеет предусловие Ф1/Ф3

> **Предусловие:** до добавления бумаги в `FORWARD_TICKERS` — закрыть Ф1
> (`trading_orchestrator.py:416`) и Ф3 (`indicators.py:120,126`), либо доказать,
> что у новой бумаги ≥ 200 баров D1 **на момент добавления**.
>
> **Основание — измерено, а не предположено.** Сегодня Ф1 и Ф3 недостижимы
> только потому, что минимум по набору 860 баров при пороге 200. Это **свойство
> данных, а не защита** (правило 3 §8). Набор объявлен растущим
> (`universe.py`: «Единственный набор, который расширяется»), и первая же бумага
> с короткой историей **включает обе дыры молча**: фильтр перестанет
> применяться, а сигнал будет разрешён — без строки в логе и без записи в
> `skipped_signals`, потому что отказа не произойдёт.
>
> Охрана `MIN_HISTORY_BARS = 250` этого не ловит — см. следующую запись.

#### ДЫРА В АРХИТЕКТУРЕ: два пути к одним данным, охрана на одном

> **Факт.** `_prepare_ticker` (`run_forward_d1.py:532`) отказывается обрабатывать
> тикер с `len(rows) < MIN_HISTORY_BARS` (250, «SMA200 фильтра + запас»).
> `_check_structural_downtrend` (`trading_orchestrator.py:410-414`) читает свечи
> **своим** запросом и охраняется только собственным
> `if len(rows) < sma_long: return None`, то есть **разрешением**.
>
> Один и тот же вопрос «хватает ли истории» задаётся дважды, разным кодом, с
> разными порогами (250 и 200) и **противоположными исходами при отказе**:
> пропустить тикер против разрешить сделку.
>
> **Форма починки — ОБЩАЯ ТОЧКА ВХОДА К СВЕЧАМ, а не вторая копия охраны.**
> Вторая копия — это ровно долг №24: две копии одного факта разъезжаются молча, и
> здесь они уже разъехались (250 против 200). Нужен один читатель свечей с одним
> объявленным минимумом, к которому ходят оба пути; отказ этого читателя обязан
> быть отказом, а не умолчанием.
>
> Оговорка: докстринг `run_forward_d1.py:186-190` про этот второй путь уже знает,
> но описывает его как довод про отрицательный контроль, а не как дыру в охране.

#### ПРАВИЛО 14 (§8): область проверки задаётся предикатом достижимости, а не списком мест

> **Заведено 2026-08-02, четвёртый случай правила 8.** Особенность случая: он
> произошёл **с инструментом, сделанным против перечислений**.
>
> Опись молчаливых нулей строилась предикатом по AST — именно чтобы не полагаться
> на память. Но **область** предиката я задал списком каталогов (движок правил,
> индикаторы, риск, бэктест, живой прогон, загрузка). В список не попал
> `bot/learning/trading_orchestrator.py` — файл, содержащий ворота решения
> открыть позицию, — и 63 живых срабатывания уехали в «вне области».
>
> Предыдущие три случая: №24 (копии), №28 (перекрытые скрипты), №37 (окно
> выборки). Все три — про перечисленный список, оказавшийся короче реальности.
> Четвёртый показывает, что механическая проверка **наследует** дефект от границы,
> которую ей задали руками.
>
> **Формулировка:** область проверки обязана задаваться предикатом
> достижимости от точки входа («всё, что импортирует `run_forward_d1`»), а не
> перечислением мест. Если предикат области невозможен — это само по себе
> результат, и его надо записать, а не заменять списком.
>
> **Уточнение того же правила, найденное в этой же сессии:** достижимость
> **файла** и исполнение **функции** — разные предикаты. Колонка «живой» в описи
> считалась по первому, и из-за этого Ф6/Ф7 получили завышенную тяжесть.
> Исполнение доказывается артефактом (строка в БД, строка в логе), а не графом
> импортов.

#### Граница инструмента — записать в докстринг `tools/silent_zeros_probe.py`

> Предикат различает **наличие умолчания**, но **не направление ответа**.
> Разрешительный `False` фильтра и запретительный `False` условия для него
> неразличимы, поэтому **529 срабатываний — кандидаты, а не находки**;
> направление назначается разбором.
>
> Провалы, показанные фактом в этой же сессии: не пойман
> `return {"approved": True, …}` (разрешение непустой структурой, Ф4) и
> `or Decimal("0.5")` (нелитеральное умолчание, Ф5) — обе находки получены
> чтением. Отсутствующую проверку предикат не ловит по построению.
>
> Формулировку положить **в докстринг самого инструмента**, а не только сюда:
> иначе следующий его прогон предъявит ноль как замер (правило 12 §8).

### 7.15 ЗАМЕР В ОЧЕРЕДЬ НА 03.08 — сегодня НЕ делался

> **Двигалось ли доверие в `belief_system` хоть у одной стратегии хоть раз.**
>
> **Что известно сейчас, без замера.** Все **14** строк `belief_system` имеют
> `confidence = 0.5000` и `total_trades = 0`, и у всех `updated_at =
> 2026-07-28 17:16:19` — одна и та же секунда. То есть на снимке 02.08 доверие
> не двигалось **ни у одной** стратегии: таблица стоит ровно в том виде, в каком
> её засеяли.
>
> **Чего этот снимок не говорит.** Он показывает состояние, а не историю. Мог
> быть цикл, который двигал доверие и вернул его к 0.5; мог быть откат; могла
> быть перезаливка таблицы 28.07 поверх прежних значений. Снимок и историю
> смешивать нельзя — это правило 13 §8.
>
> **Метод замера.** Искать след ИЗМЕНЕНИЯ, а не текущее значение: строки в
> `belief_history`/аудите, если такая таблица есть; иначе — записи `BeliefUpdater`
> в `logs/forward_d1.log` за все прогоны с 12.07; иначе — признать, что история
> не сохраняется, и это само по себе результат.
>
> **Почему это важно, сформулировано до замера.** Механизм доверия — ворота
> `:210 if confidence < MIN_STRATEGY_CONFIDENCE` — стоит **на живом пути принятия
> решения** (исполнение доказано, §7.12). Если доверие не двигалось ни разу, то:
> * ворота проверены **только** в одном положении — «посевные 0.5 пропускают»;
> * ветка «доверие упало ниже 0.20 и сделка запрещена» **не исполнялась никогда**
>   и потому не проверена на живом пути вовсе;
> * `BeliefUpdater` подключается каждым прогоном (видно в логе), но
>   свидетельства, что он что-то меняет, нет.
>
> И главное: **обучение доверием — фундамент второго этапа ML.** Строить его
> поверх механизма, который ни разу не сдвинулся на боевых данных, значит
> повторить историю форварда: контур работает, а измеряет ли он что-нибудь —
> неизвестно (§2и, «ноль сделок не есть свидетельство о стратегии»).
>
> **Возможный исход «никогда» — не дефект сам по себе**, а прямое следствие
> нуля закрытых сделок форварда: двигать доверие нечем. Тогда результат замера
> формулируется как «механизм непроверен по причине отсутствия входных
> событий», и это отличается от «механизм сломан». Различить обязан замер, а не
> догадка.

### 7.16 ХЕНДОФФ на утро — отдельным файлом

Порядок утра, полная опись несведённых копий с номерами строк конфликтов,
список нерешённого и риск №31 — в **`docs/HANDOFF_2026-08-03.md`**.

Сюда не копируется намеренно: копия разошлась бы с оригиналом, а риск ровно
об этом (см. HANDOFF, раздел 3).

### 7.17 Пункт 11 и новый пункт 12 утреннего чеклиста

Уже внесено в боевой папке 02.08. Здесь — для полноты очереди: счёт по блоку A
**2 из 12**, пункт 11 (текст верен) засчитан как отдельное ИЗМЕРЕННОЕ
достижение, пункт 12 (сообщение получено человеком) — **не пройден**.
