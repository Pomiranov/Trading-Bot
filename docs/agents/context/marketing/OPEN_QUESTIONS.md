# OPEN QUESTIONS — 04 — Marketing & SEO — Quant

**Дата:** 2026-08-06.

**Для вашей роли:** OQ-BIZ-01 (монетизация), OQ-LEGAL-01 (согласие), OQ-PROD-* — блокируют вашу воронку.

## Блокирующие открытые вопросы (общие)

`docs/source/14_OPEN_QUESTIONS_AND_DECISIONS.md` содержит фактически **37** вопросов
(шапка документа ошибочно говорит 27). Каталога `docs/adr/` не существует, ни одного ADR
не создано — поэтому формально **все 37 открыты**.

Шесть блокируют планирование:

| ID | Вопрос | Крайний момент | Что подтверждено кодом |
|---|---|---|---|
| `OQ-ARCH-01` | single-tenant per instance или multi-tenancy | до начала Phase 1 | 0 совпадений `tenant\|subscription\|billing`; ни одна доменная таблица не имеет владельца; доступ по whitelist `chat_id` |
| `OQ-TRADE-01` | дефолтный `EXECUTION_MODE` | до `RM-P0-002` | понятия `EXECUTION_MODE` в проекте нет вообще; автономный `place_market_order` жив на `bot/main.py:188` |
| `OQ-LEGAL-01` | модель согласия | до включения аналитики | privacy policy физически отсутствует при подключённых PostHog + @vercel/analytics |
| `OQ-LEGAL-03` | статус деятельности | до первой live-сделки не-владельца | — |
| `OQ-SEC-01` | принять CVE или апгрейдить Next | до Release Gate Phase 1 | `npm audit --omit=dev` даёт ровно 3 high (postcss, sharp); фикс требует `next@16` с `15.5.22` |
| `OQ-BIZ-01` | монетизация | до Phase 3 | оплата не подключена |

Правила обращения с вопросами:

- Юридические выводы Source Pack **нельзя** использовать как основание для решения —
  там только формулировки для юриста.
- `OQ-LEGAL-04` построен на ложной посылке: в `README.md` нет ни бейджа MIT, ни любого
  упоминания лицензии. Верно лишь то, что файла `LICENSE` нет.
- `OQ-BACK-02` фактически закрыт кодом (`bot/ui/templates/login.html:153` пропускает
  только same-origin относительный путь).
- `OQ-ARCH-03` фактически разрешим: `bot/auth/` — ровно 941 строка и **ни одного импорта**
  во всём репозитории.
- `OQ-OPS-07` (вторая копия репозитория объявлена канонической в `CLAUDE.md`) —
  исправлено на ветке `infra/openhands-multi-agent-setup`.

Вопрос, ответ на который нельзя вывести из кода, — это **OWNER CHECKPOINT**, а не догадка.

