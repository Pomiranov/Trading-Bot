# ЗАГЛУШКА. Хендофф перенесён в боевую папку 2026-08-03

Действующий файл — **`docs/HANDOFF_2026-08-03.md` в `D:\Trading-Bot-Nik`**,
ветка `merge-learning-nik`, коммит `9e5ba75`.

**Почему перенесён.** Первые два шага утра — сверка телеграма с §2з и приёмка
блока A — выполняются в боевой папке. Точка входа, лежащая на **непринятой
ветке**, из места работы недостижима, а копия в двух местах разошлась бы за
сутки (риск №31, §9).

Файл здесь **не удалён** по той же причине, что и
`PROJECT_STATE_PENDING_2026-08-02.md`: удалённый восстанавливают из истории, не
зная, что он устарел. При мерже ветки — удалить.
