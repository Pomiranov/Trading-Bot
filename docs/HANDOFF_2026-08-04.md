# Хендофф на утро 2026-08-04

Составлен в ночь на 04.08, после коммитов `5d424b6`, `1ae87e0`, `578a4e6`.

**Этот файл — точка входа, а не хранилище.** Содержание живёт в
`docs/PROJECT_STATE.md` (раздел **2л** — вечер 03.08, §5 — кандидаты в долг, §8 —
правило 20, §9 — состояние) и в `docs/EXPERIMENTS.md` (попытки №2 и №3). **Тексты
сюда не копируются:** второй экземпляр расходится молча — это долг №24, и цена уже
измерена (§9, риск §3).

---

## 1. ПОРЯДОК УТРА. Отклоняться нельзя

### Шаг 1 — сверка сообщения сторожа с ЗАМОРОЖЕННЫМ текстом

Ожидаемый текст за слот **04.08 00:15** снят с кода `forward_healthcheck.py` и
заморожен **до прогона**:
`measurements/2026-08-03_blockA_freeze/02_TELEGRAM_EXPECTED.md`, SHA-256 в
`SHA256SUMS.txt` того же каталога.

> ⚠ В задании этот файл назван `04_TELEGRAM_FREEZE` — такого файла **нет**. Верное
> имя — выше. Названо, чтобы не искали второй экземпляр заморозки.

**Признаки проверять ПООТДЕЛЬНО**, а не «похоже на предсказанное». Ожидается **✅** —
и это **смена режима, первое ✅ за несколько суток**. Различающие признаки —
четыре строки, которых приходить **НЕ должно** (перечислены в замороженном файле,
раздел «СМЕНА РЕЖИМА»). Пришла хоть одна — **режим не сменился, это расхождение, а
не шум**.

Право отличаться имеют **только даты, время и число секунд**. Ветка «бары за 01–02.08
догнаны тем же прогоном» названа заранее и **законна** — тогда пункт 6 замороженного
списка неприменим, а не провален.

### Шаг 2 — живая проверка долга №48

Предсказание заморожено до 00:15:
`measurements/2026-08-03_blockA_freeze/03_PREDICTION_48_LIVE.md`.
**Ожидание: №48 — NO-OP.** Проверяется **бесплатно**, тем же текстом сторожа: ни
одной команды к БД, ни одного прогона руками. Расхождение означало бы, что граница
выражена **не от бара решения**.

Сверка **НЕ ВЫПОЛНЕНА** — это единственное её состояние на утро.

### Шаг 3 — три НЕ ЗАМЕРЕННЫХ пункта БЛОКА A

Счёт блока A: **8 пройдено из 13**, неприменимы **2**, **НЕ ЗАМЕРЕНЫ три** — пункты
**5, 8а, 8б**. До их замера они остаются именно «не замерены», а не «пройдены» и не
«неприменимы» (правило 18 §8).
Замороженный список — `measurements/2026-08-03_blockA_freeze/01_LIST_FROZEN.md`.

> 🚫 **ИСПРАВЛЕНО 04.08 (вечер): «все три требуют лога прогона 04.08» — НЕВЕРНО.**
> Установлено чтением самого замороженного текста, а не кода. Лога требует **один**
> пункт из трёх:
>
> | пункт | формулировка из заморозки | чем проверяется на самом деле |
> |---|---|---|
> | **5** | «бюджеты в логе потикерные, сводка без единого „свободно“» | **лог 04.08** — единственный из трёх |
> | **8а** | «`powercfg /waketimers`: один таймер или два» | **системная команда**, лог непричастен |
> | **8б** | «строк `origin='backtest'` — ноль» | **запрос к БД**, лог непричастен |
>
> Автор неверного утверждения — **Claude**; обнаружено **Claude Code** чтением
> замороженного текста 04.08. Цена ошибки: два пункта из трёх сутки ждали не того,
> и их «не замерено» несло **подобранную**, а не записанную причину — тот же класс,
> что правило 18 §8.
>
> ✅ **Все три ЗАМЕРЕНЫ вечером 04.08.** Счёт блока A стал **11 пройдено из 13**,
> неприменимы **2**, не замерено **0**. Разбор — `PROJECT_STATE.md` §9.

### Шаг 4 — ноль вхождений НОВОГО токена в боевом логе

Ротация выполнена 03.08, отпечаток значения в `.env` — `bbb1066b3dad2c9b`.
Проверять **починенной формой** (область — предикат «все объекты базы» плюс файлы;
печатать **число просмотренных объектов**: ноль просмотренных при нулевом результате
есть отказ). Разбор формы и её пределы — §5, кандидат в долг «проверка тремя
независимыми формами была одной формой».

⚠ **Область перемера — предикатом `git worktree list`, а не перечислением: деревьев
ТРИ, а не два** (§5, кандидат в долг про третье дерево). Пофайловый скан 03.08 третье
дерево не покрывал.

⚠ Починки `httpx` нет ⇒ ротация токена — одноразовая мера с периодом «до следующего
прогона, отправившего сообщение» (§5, «Следствие для ротации логов»).

### Шаг 5 — строки 1129–1130 `logs/forward_d1.log` на месте

Файл git-ignored и **единственный полный след прогонов с 12.07**. На эти строки
ссылаются **четыре документа** в `docs/` (`PROJECT_STATE.md`, `TOKEN_ROTATION.md`,
`HANDOFF_2026-08-03.md` и этот) плюс **шесть улик** в `measurements/` — область
получена предикатом `grep -rl 1129 --include=*.md`, а не перечислена по памяти.
Усечение файла — отдельное решение, не гигиена.
Проверка: строки на месте, номера не сдвинулись. Если сдвинулись — сначала обновить
ссылки, потом всё остальное.

### Шаг 6 — данные продвинулись

| что | ожидание |
|---|---|
| бары за московскую сессию **03.08** | появились |
| `forward_state` | продвинулся |
| `candles` 1d | **+20 строк** |

Расхождение здесь меняет чтение шага 1: «✅ у сторожа» при не продвинувшихся данных
означало бы, что сторож смотрит не туда.

---

## 2. ЧТО ЗАКРЫЛОСЬ ВЕЧЕРОМ 03.08 — только ссылки

* **долг №52** и **долг №48** (все пять гейтов) — закрыты;
* **заморозка `SIGNAL_WINDOW_BARS`** — снята по исчерпанию причины;
* **долг №50** — части 1, 2а, 2б, 3; множитель приколочен (`c268ab752b9be2d8`),
  авторасчёт из данных запрещён; часть 2в не сделана **по решению**; гейт **Г1
  переформулирован** в Г1а/Г1б, оба выполнены;
* **попытка №2** реестра — **ИСПОЛНЕНА**. 🚩 **О3 сработало: путь H4 закрыт числом**
  (темп 1.06 против 1.62 у D1). О2 опровергнуто: дневной фильтр на H4 **полезен**;
* **попытка №3** — **зафиксирована и НЕ ИСПОЛНЕНА**, попытка не расходована.

Разбор — `PROJECT_STATE.md` §2л. Улики — `measurements/2026-08-04_debt50/` и
`measurements/2026-08-04_signals_census/`.

---

## 3. ПЕРВЫМ В РАБОТУ ПОСЛЕ УТРА

**Счётчики четырёх путей отказа сигнала.** §5, кандидат в долг (номер за Ником);
очередь — `QuantFlow_THROUGHPUT_PLAN.md` §5; следствие для ML — `ML_ROADMAP.md`,
этап 2.

Порядок обязателен: **шаг 1** — счётчики на все четыре пути плюс «сигнал родился»
(приёмка: тождество сходится числом **и** обе тройки D1 не сдвинулись); **шаг 2** —
исход отвергнутого сигнала, в бэктесте вычислим, в живом контуре нельзя. Шаг 2 без
шага 1 бессмыслен.

---

## 4. НЕ ЗАМЕРЕНО — не путать с пройденным

* тест на 17 значений по **фактическим** полям движка на обоих ТФ (`_window_bars`,
  `_warmup_bars`) — **не написан**; замер выполнен, но держится на одном прогоне, а не
  на механизме;
* три пункта блока A (5, 8а, 8б);
* сверка живой проверки №48.
