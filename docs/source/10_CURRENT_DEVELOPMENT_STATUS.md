# 10 — Current Development Status

| Field | Value |
|---|---|
| **Дата аудита** | 2026-08-05 |
| **Путь к проекту** | `/Users/danila/Downloads/Trading-Bot-merge-learning-nik` |
| **Ветка** | `quant-site-approved-reference-redesign` |
| **Commit HEAD** | `a54a100b4d542f1d866b5f89336ce0703fea6ced` |
| **Статус документа** | ACTIVE — Source of Truth по статусу разработки |
| **Область аудита** | Фактический статус каждого крупного компонента на момент аудита |
| **Связанные документы** | [02](02_REPOSITORY_AND_SYSTEM_ARCHITECTURE_AUDIT.md) — [09](09_PRODUCT_UX_MARKETING_AND_MONETIZATION_AUDIT.md) (детали findings) · [11](11_MASTER_ROADMAP.md) (что делать) · [14](14_OPEN_QUESTIONS_AND_DECISIONS.md) |

**Шкала статусов:** `NOT STARTED` · `PROTOTYPE` · `PARTIALLY IMPLEMENTED` · `FUNCTIONAL` · `BETA READY` · `PRODUCTION READY` · `BLOCKED` · `DEPRECATED`

**Уровень уверенности оценки:** Высокий / Средний / Низкий — отражает, насколько статус подтверждён фактической проверкой, а не только чтением кода.

---

## Сводная таблица

| # | Компонент | Статус | Уверенность |
|---|---|---|---|
| 1 | Маркетинговый сайт | **BETA READY** | Высокая |
| 2 | Dashboard | **FUNCTIONAL** | Средняя |
| 3 | Backend / платформенный слой | **FUNCTIONAL** | Высокая |
| 4 | База данных | **FUNCTIONAL** | Высокая |
| 5 | Авторизация (операторы) | **BETA READY** | Высокая |
| 6 | Авторизация (клиенты) | **NOT STARTED** | Высокая |
| 7 | Telegram-бот | **FUNCTIONAL** | Средняя |
| 8 | Telegram Mini App | **BLOCKED** | Средняя |
| 9 | Торговое ядро | **PARTIALLY IMPLEMENTED** | Средняя |
| 10 | Risk management | **FUNCTIONAL** | Средняя |
| 11 | «AI/ML» (learning-система) | **FUNCTIONAL** (не ML) | Высокая |
| 12 | Backtesting | **FUNCTIONAL** | Средняя |
| 13 | Paper trading | **FUNCTIONAL** | Средняя |
| 14 | Брокер: Tinkoff | **FUNCTIONAL** | Средняя |
| 15 | Брокер: Finam | **PROTOTYPE** | Высокая |
| 16 | Брокер: Bybit | **PARTIALLY IMPLEMENTED** | Высокая |
| 17 | Подписки | **NOT STARTED** | Высокая |
| 18 | Платежи | **NOT STARTED** | Высокая |
| 19 | Уведомления | **FUNCTIONAL** | Средняя |
| 20 | Аналитика (продуктовая) | **PARTIALLY IMPLEMENTED** | Высокая |
| 21 | Тестирование | **PARTIALLY IMPLEMENTED** | Высокая |
| 22 | Безопасность | **FUNCTIONAL** | Высокая |
| 23 | Deployment | **PARTIALLY IMPLEMENTED** | Низкая |
| 24 | Monitoring / alerting | **NOT STARTED** | Высокая |
| 25 | CI/CD | **NOT STARTED** | Высокая |
| 26 | Документация | **PARTIALLY IMPLEMENTED** (противоречива) | Высокая |
| 27 | Onboarding | **NOT STARTED** | Высокая |
| 28 | Поддержка | **NOT STARTED** | Высокая |
| 29 | Backup / recovery | **NOT STARTED** | Высокая |
| 30 | Юридический контур | **NOT STARTED** | Высокая |

---

## 1. Маркетинговый сайт — BETA READY

1. **Реализовано:** Next.js 15.5.22 / React 19, App Router, 2 локали (238 ключей в паритете), 10 секций, дизайн-система с машинным гейтом, security-заголовки, SEO-метаданные (canonical, hreflang, OG, Twitter), sitemap, robots, форма заявки с honeypot и rate-limit, аналитика (PostHog + Vercel).
2. **Работает фактически:** все 6 статических гейтов exit 0; production build exit 0 (12 SSG-страниц); 0 ошибок в консоли; все запросы 200; **нет горизонтального overflow на 1440/1024/768/390/320**; мобильное меню с Escape и возвратом фокуса.
3. **Частично:** аналитика подключена, но без согласия и privacy policy ([W-08], [S-01]); фокус не переводится в мобильное меню ([W-04]).
4. **Отсутствует:** юридические страницы (404); structured data ([W-05]); рабочий E2E-гейт ([Q-03]).
5. **Заблокировано:** приём заявок — по умолчанию адрес нигде не сохраняется ([W-01]).
6. **Критические зависимости:** `NEXT_PUBLIC_SITE_URL`, `BETA_WEBHOOK_URL`, `NEXT_PUBLIC_POSTHOG_KEY`; юридическое заключение.
7. **Риски:** SEO нерабочий из-за placeholder-домена ([W-02]); 363 kB First Load ([W-03]); Safari/iOS не проверялся.
8. **Следующий шаг:** закрыть production-чеклист переменных, затем юридические страницы.
9. **Критерии завершения:** заявка сохраняется и подтверждена сквозным прогоном; `robots.txt`/`sitemap.xml` с реальным доменом; privacy/terms опубликованы; `qa:landing` зелёный.
10. **Уверенность:** **Высокая** — проверено на живом сайте, в production-сборке и всеми гейтами.

## 2. Dashboard — FUNCTIONAL

1. **Реализовано:** Flask app factory, 12 экранов SPA (vanilla ES-modules, 5 816 строк), 42 маршрута `/api/v2` (26 read + 16 действий), legacy `/api/v1`, SSE-realtime, серверные сессии с ролями, CSRF, идемпотентность мутаций, audit trail, deep links, клавиатурные шорткаты, режим read-only.
2. **Работает фактически:** приложение стартует (`read_only=True · schema_ok=True · engine_autostart=False`); **все** эндпоинты данных и действий отдают 401 без сессии; CSP с per-request nonce; 27 JS-тестов проходят; схема актуальна (v4); **mock-данных не найдено** — всё читается из БД.
3. **Частично:** английская локаль — заглушка ([D-03]); realtime только в legacy v1 ([D-04]); токены разошлись с сайтом — гейт падает ([Q-02]).
4. **Отсутствует:** клиентские сущности — подписка, профиль, подключение своего брокера, onboarding ([D-01]); режимы исполнения (advice-only / confirm) ([A-02]).
5. **Заблокировано:** production-запуск — только Werkzeug dev-сервер ([D-02]).
6. **Критические зависимости:** TimescaleDB; оператор, созданный через CLI; WSGI-сервер для production.
7. **Риски:** 72 контрактных теста аутентифицированных маршрутов пропускаются ([Q-04]); визуальная часть (UI-состояния, адаптивность, a11y) **не проверена**.
8. **Следующий шаг:** production WSGI (`waitress`), затем включить 72 теста в CI.
9. **Критерии завершения:** WSGI в production; 0 skipped-тестов; гейт токенов зелёный; визуальный аудит аутентифицированного UI проведён.
10. **Уверенность:** **Средняя** — API, безопасность и запуск проверены фактически; **аутентифицированный интерфейс визуально не проверялся** (нет учётных данных).

## 3. Backend / платформенный слой — FUNCTIONAL

1. **Реализовано:** `bot/qf_platform` — 37 файлов / 8 344 строк: 18 репозиториев, 20 сервисов, контракты (envelope + `ErrorCode`), DTO, `environment`, `bootstrap`, `migrate`, `schema`.
2. **Работает фактически:** контрактная дисциплина соблюдена (роут → один сервис → envelope); валидация всех query-параметров с явным отказом; разделение `DB_UNAVAILABLE` / `SCHEMA_OUT_OF_DATE`; `migrate --check` проходит.
3. **Частично:** идемпотентность и аудит покрывают только HTTP-путь, не цикл и не Telegram ([S-05]).
4. **Отсутствует:** `owner_id` на доменных таблицах ([B-01]); единый признак среды (`QF_ENV`).
5. **Заблокировано:** —
6. **Критические зависимости:** TimescaleDB, схема v4.
7. **Риски:** дублирующиеся слои — `bot/auth/` vs `bot/security/`, `/api/v1` vs `/api/v2` ([A-06]); `requirements.txt` без lock-файла.
8. **Следующий шаг:** вынести идемпотентность и аудит в `trade_gateway`, чтобы покрыть все три пути исполнения.
9. **Критерии завершения:** любая сделка независимо от интерфейса имеет запись в `audit_events`; один активный auth-слой; план удаления v1 зафиксирован.
10. **Уверенность:** **Высокая** — код прочитан полностью, контракт проверен пробами.

## 4. База данных — FUNCTIONAL

1. **Реализовано:** TimescaleDB (pg15) в Docker; ~20 таблиц; `trades` как гипертаблица по `opened_at`; индексы под фактические запросы; `SCHEMA_VERSION = 4`; миграции с транзакцией на statement и безопасным `--check`.
2. **Работает фактически:** `trading_db` Up 3 days; порт привязан к `127.0.0.1`; схема актуальна.
3. **Частично:** две библиотеки доступа (SQLAlchemy + asyncpg); `quantflow_schema.sql` — устаревший дубль.
4. **Отсутствует:** `owner_id` ([B-01]); **резервное копирование** ([B-06]).
5. **Заблокировано:** —
6. **Критические зависимости:** Docker; `DB_PASSWORD`.
7. **Риски:** **потеря тома = потеря всей торговой истории, `belief_system` и audit trail без возможности восстановления.** Усугубляется наличием необратимого `prune-equity`.
8. **Следующий шаг:** бэкап + **проверенное** восстановление.
9. **Критерии завершения:** автоматический бэкап работает; восстановление на чистом инстансе продемонстрировано; RPO/RTO зафиксированы.
10. **Уверенность:** **Высокая**.

## 5. Авторизация операторов — BETA READY

1. **Реализовано:** серверные сессии (`dashboard_sessions`), Argon2id с записью алгоритма в дайджест, `HttpOnly` + `SameSite=Strict`, `Secure` **по умолчанию**, double-submit CSRF, роли + `trading_authorized`, двухуровневая защита от подбора (burst 5/300 с + блокировка аккаунта 10 попыток/15 мин), fail-closed `before_request`.
2. **Работает фактически:** все защищённые маршруты → 401; `/` → 302 на `/login`; страница входа различает «БД недоступна» / «оператор не создан» / форму.
3. **Частично:** —
4. **Отсутствует:** 2FA; самостоятельная смена забытого пароля; управление операторами из UI (только CLI).
5. **Заблокировано:** —
6. **Критические зависимости:** БД; `argon2-cffi` (иначе fallback на scrypt).
7. **Риски:** мёртвые auth-модули (`bot/auth/`, `security/dashboard_auth.py`) — риск правки не того слоя ([S-06]).
8. **Следующий шаг:** удалить мёртвые слои; проверить валидацию `next` в `/login` (открытый редирект).
9. **Критерии завершения:** один активный auth-механизм; `CLAUDE.md` описывает его верно; `next` валидируется.
10. **Уверенность:** **Высокая** — проверено пробами и чтением кода.

## 6. Авторизация клиентов — NOT STARTED

1. **Реализовано:** ничего.
2. **Работает фактически:** —
3. **Частично:** —
4. **Отсутствует:** регистрация, клиентский аккаунт, `owner_id`, привязка Telegram к аккаунту, хранение брокерских ключей per-user, изоляция данных.
5. **Заблокировано:** решением по [OQ-ARCH-01](14_OPEN_QUESTIONS_AND_DECISIONS.md) — модель доступа не выбрана.
6. **Критические зависимости:** архитектурное решение (single-tenant-per-instance vs multi-tenant).
7. **Риски:** блокирует подписки, платежи, самообслуживание, открытую beta и публичный запуск. Самое дорогое изменение в проекте, если выбран multi-tenant.
8. **Следующий шаг:** **принять решение по [OQ-ARCH-01]** — не начинать реализацию до этого.
9. **Критерии завершения:** ADR зафиксирован; при multi-tenant — тест изоляции данных проходит.
10. **Уверенность:** **Высокая** — отсутствие подтверждено поиском по всему коду.

## 7. Telegram-бот — FUNCTIONAL

1. **Реализовано:** `bot/tg/` — 35 файлов / 4 792 строк на python-telegram-bot 22.8; 12 команд; ~60 callback-хендлеров; 3 FSM-conversation; fail-closed whitelist; rate-limiter в 15 хендлерах; дифференцированный error handler; диспетчер уведомлений; paper-trading и learning-разделы.
2. **Работает фактически:** *INFERRED из кода* — бот осознанно не запускался. Регистрация хендлеров, порядок middlewares и логика проверены чтением.
3. **Частично:** 6 подразделов настроек — заглушки «в разработке», включая `set_risk` и `set_security` ([T-05]); локализация только русская; FSM-состояние в памяти процесса (теряется при перезапуске).
4. **Отсутствует:** подтверждение/отклонение **автоматических** сделок ([T-01]); идемпотентность и audit trail на пути Telegram ([T-02]); **ни одного теста** ([Q-05]).
5. **Заблокировано:** —
6. **Критические зависимости:** `TELEGRAM_TOKEN`, `TELEGRAM_CHAT_ID`/`TELEGRAM_ALLOWED_IDS`; БД для статистики.
7. **Риски:** второй бот (`bot/ui/telegram_bot.py`, 1 036 строк) при одновременном запуске конфликтует за polling ([A-05]); `bot/reliability.py` (retry/circuit-breaker) к брокерским вызовам не применён.
8. **Следующий шаг:** слить боты, перенеся контур подтверждения сделок в БД-хранение.
9. **Критерии завершения:** одна реализация; подтверждение автоматических сделок работает и переживает перезапуск; тесты на `trade_confirm`.
10. **Уверенность:** **Средняя** — код прочитан целиком; рантайм не проверялся.

## 8. Telegram Mini App — BLOCKED

1. **Реализовано:** `bot/ui/static/miniapp/` — HTML + 4 JS-файла, включая игру «Quant Hunter» и клиент legacy-API; вынесен на отдельный маршрут `/miniapp` (раньше 67 kB грузились на каждой странице Dashboard); CSP `frame-ancestors https://web.telegram.org` для этих путей; инструкция подключения в `BOTFATHER.md`.
2. **Работает фактически:** статика отдаётся (префикс `/miniapp` публичен). **В браузере с активной сессией оператора** данные загружаются.
3. **Частично:** —
4. **Отсутствует:** валидация `initData` на сервере; рабочая аутентификация в WebView.
5. **Заблокировано:** клиент отправляет API-ключ из `sessionStorage`, но серверный валидатор (`bot/security/dashboard_auth.py`) **не подключён ни к чему** — доступ определяется только сессионной cookie, которой в Telegram WebView нет. Все вызовы данных (`QFApi.positions()`, `overview()`, `signals()`, SSE) → 401 ([T-03]).
6. **Критические зависимости:** публичный HTTPS-домен (требование Telegram); реализация проверки `initData`.
7. **Риски:** `initDataUnsafe` используется на клиенте — если по нему когда-либо начнут определять личность, это станет уязвимостью подмены.
8. **Следующий шаг:** реализовать проверку `initData` (HMAC-SHA256 на `WebAppData`) с выдачей серверной сессии; удалить мёртвый `dashboard_auth.py`.
9. **Критерии завершения:** Mini App внутри Telegram получает данные; подделанный `initData` отклоняется тестом.
10. **Уверенность:** **Средняя** — вывод построен на четырёх проверенных фактах, но без запуска внутри Telegram.

## 9. Торговое ядро — PARTIALLY IMPLEMENTED

1. **Реализовано:** `bot/main.py` (417 строк): цикл candles → indicators → rules → orchestrator → risk → брокер; реконсиляция позиций с брокером каждый цикл; PID-lock; graceful shutdown; зеркалирование сделок в paper и `trading_signals`; трейлинг-стоп; `--backtest` и `--bot-only`.
2. **Работает фактически:** *INFERRED* — цикл не запускался с реальным брокером. Логика прочитана полностью; загрузка правил подтверждена логом при старте Dashboard («основных=12, фильтров=3, выходов=3, тикерных=4, макро=3»).
3. **Частично:** только лонг (`Action.SELL` закрывает позицию, шортов нет); только рыночные заявки; воспроизводимость решения без версии правил ([B-03]).
4. **Отсутствует:** режимы исполнения (`advice` / `confirm` / `auto`) — есть только бинарное start/stop ([A-02]); автоматический kill switch ([A-03]); идемпотентность и audit trail ([S-05]); **тесты** ([Q-05]).
5. **Заблокировано:** переход к Live реальными деньгами — блокируется [A-02], [A-03], [S-05], [Q-05], [B-06], [Q-06].
6. **Критические зависимости:** MOEX ISS; Tinkoff API; БД; `rules.yaml`.
7. **Риски:** **торговое окно в наивном локальном времени, а лог заявляет UTC** ([A-04]) — часть сессии MOEX не покрывается, что искажает и исполнение, и всю накопленную статистику; `place_market_order` глотает исключения (компенсировано реконсиляцией, но не устранено); circuit-breaker не применён.
8. **Следующий шаг:** исправить таймзону ([A-04]) — самое дешёвое исправление с наибольшим эффектом на корректность; затем `EXECUTION_MODE`.
9. **Критерии завершения:** `EXECUTION_MODE=confirm` по умолчанию и работает; окно торговли привязано к таймзоне биржи с тестом; kill switch реализован; 5 приоритетных сценариев покрыты тестами.
10. **Уверенность:** **Средняя** — код прочитан полностью; реальное исполнение не проверялось осознанно.

## 10. Risk management — FUNCTIONAL

1. **Реализовано:** `bot/risk/` (373 строки): `calculate_position` (риск = `portfolio_value × max_position_pct`), `check_trade_allowed` (лимит открытых позиций, дубль по тикеру, дневной убыток, sanity размера), `trailing_stop`, `reconcile_with_broker`, `state_store` с advisory OS-lock (чтобы независимые копии не проверяли лимиты по неполной картине).
2. **Работает фактически:** проверки вызываются **до** обращения к брокеру (`bot/main.py:177` перед `:188`) — заявление сайта подтверждено; косвенно покрыто тестами `test_signals_risk_analytics.py` (35 тестов).
3. **Частично:** параметры только из `.env` — из UI не настраиваются (`set_risk` — заглушка, [T-05]), хотя сайт заявляет «Вы задаёте пределы».
4. **Отсутствует:** автоматический стоп торговли по просадке ([A-03]); лимиты per-user (нет клиентов).
5. **Заблокировано:** —
6. **Критические зависимости:** `RISK_*` переменные; корректный `portfolio_value` от брокера.
7. **Риски:** дневной лимит блокирует сделки по одной, но не останавливает цикл — при пробитии лимита цикл продолжает пытаться.
8. **Следующий шаг:** kill switch по просадке; вынести `RISK_*` в UI.
9. **Критерии завершения:** пробитие порога просадки останавливает движок (тест); лимиты редактируются из Dashboard с записью в audit.
10. **Уверенность:** **Средняя** — логика проверена чтением и частично тестами; поведение на реальных данных не наблюдалось.

## 11. «AI/ML» — FUNCTIONAL (машинного обучения нет)

1. **Реализовано:** `bot/learning/` — 9 модулей / 3 209 строк: `belief_updater` (win_rate, profit_factor, expectancy, sharpe → `confidence`), `trading_orchestrator` (порог 0.20), `hypothesis_engine` (стат-тесты на `scipy`), `decision_evaluator`, `memory_writer`, `knowledge_seeder`, `sandbox_learning_loop`, `feedback`.
2. **Работает фактически:** формула `new_confidence = current + (target - current) * 0.15` с клэмпом [0.05, 0.95] и минимумом 20 сделок; отклонённые сигналы пишутся в `skipped_signals` с причиной; таблицы `belief_system` и `hypotheses` существуют; ручной запуск цикла через `/api/v2/learning/run-cycle`.
3. **Частично:** статистика **смешивает** сделки, принятые по разным версиям правил ([B-03]) — из-за этого её нельзя интерпретировать как характеристику текущей конфигурации.
4. **Отсутствует:** машинное обучение любого вида. `grep` по `sklearn|torch|tensorflow|keras|xgboost|lightgbm|transformers|openai|anthropic` → **0**; по `bayes|posterior|prior|likelihood` → **0**. `scipy` используется для стат-тестов гипотез, не для моделей. **Тестов нет** ([Q-05]).
5. **Заблокировано:** —
6. **Критические зависимости:** закрытые сделки (минимум 20 на стратегию); БД.
7. **Риски:** **`README.md` называет это «байесовской системой убеждений» и «институциональной платформой» — оба утверждения неверны** ([X-01]). Сайт (`faq.a1`) описывает механику точно.
8. **Следующий шаг:** привести `README.md` в соответствие; ввести `rules_version` и решить, что делать с накопленной статистикой ([OQ-AI-01]).
9. **Критерии завершения:** формулировки в документации соответствуют коду; границы `confidence` покрыты тестами; статистика привязана к версии правил.
10. **Уверенность:** **Высокая** — формула, константы и отсутствие ML проверены прямым чтением и поиском.

## 12. Backtesting — FUNCTIONAL

1. **Реализовано:** `bot/backtest/` — 11 файлов / 2 136 строк; **комиссия 0.03 % на сторону** учитывается на входе и выходе; `_downtrend_gate`; `BacktestResult.summary()`; интеграция с learning-циклом; хранение прогонов в `backtest_runs`; запуск через `--backtest`, `POST /api/v2/backtest/run`, выгрузка через legacy-эндпоинт.
2. **Работает фактически:** *INFERRED* — прогон не выполнялся (требует загрузки истории с MOEX ISS). Учёт комиссии подтверждён чтением (`engine.py:249,372-375`).
3. **Частично:** проскальзывание в бэктесте не моделируется (в paper — моделируется).
4. **Отсутствует:** тесты; walk-forward валидация как отдельный режим (есть отдельный `run_forward_d1.py`, 652 строки).
5. **Заблокировано:** —
6. **Критические зависимости:** история свечей MOEX ISS.
7. **Риски:** результаты бэктеста не привязаны к версии правил ([B-03]) — прогон нельзя точно воспроизвести после правки `rules.yaml`.
8. **Следующий шаг:** привязать прогоны к `rules_version`; добавить проскальзывание.
9. **Критерии завершения:** прогон воспроизводим по `rules_version`; тест на известном наборе данных даёт стабильный результат.
10. **Уверенность:** **Средняя** — код прочитан; фактический прогон не выполнялся.

## 13. Paper trading — FUNCTIONAL

1. **Реализовано:** `bot/qf_platform/services/paper_trading_service.py` + `bot/engine/` (1 086 строк); отдельные таблицы `paper_accounts` / `paper_positions` / `paper_trades`; **проскальзывание 0.01 % в худшую для трейдера сторону**; комиссия на входе и выходе; проверка достаточности средств; `close_reason`; `trailing_stop_pct`; автономный цикл зеркалирует реальные сделки в paper.
2. **Работает фактически:** *INFERRED* — код прочитан; расчёты подтверждены (`:136,143,145,185-192`). Раздел доступен в Telegram (`/paper`) и Dashboard.
3. **Частично:** цена берётся из `_get_market_price` по кэшу свечей — не тиковая симуляция.
4. **Отсутствует:** тесты именно paper-сервиса; понятие «7 дней Sandbox» из тарифов как механизм ([P-01]).
5. **Заблокировано:** —
6. **Критические зависимости:** БД; котировки.
7. **Риски:** это ключевой механизм доверия («начните без риска»), и он не покрыт тестами.
8. **Следующий шаг:** тесты на комиссию/проскальзывание/расчёт PnL.
9. **Критерии завершения:** расчёт PnL с комиссией и проскальзыванием покрыт тестами.
10. **Уверенность:** **Средняя**.

## 14–16. Брокерские интеграции

| Брокер | Статус | Кратко |
|---|---|---|
| **Tinkoff** | **FUNCTIONAL** | Единственный зарегистрированный адаптер с торговлей. Sandbox по умолчанию (`TINKOFF_SANDBOX=true`). Только MOEX-акции, только лонг, только рыночные заявки. `place_market_order` глотает исключения — компенсировано реконсиляцией. **Уверенность: Средняя** (реальные вызовы не выполнялись) |
| **Finam** | **PROTOTYPE** | Скелет 118 строк; `finam-trade-api` **не** в `requirements.txt`; методы выбрасывают исключения. **Зарегистрирован в реестре**, поэтому виден в UI и меню Telegram как существующий выбор. **Уверенность: Высокая** |
| **Bybit** | **PARTIALLY IMPLEMENTED** | 202 строки; **не** наследует `BrokerAdapter`, **не** в реестре → недостижим из торговых путей. Используется сервисами только для чтения. Содержит недостижимый `place_market_order` / `cancel_order` — заряженный след ([B-02]). `BYBIT_*` отсутствуют в `.env.example`. **Уверенность: Высокая** |

Заявления сайта по всем трём (`faq.a9`) — **соответствуют факту**.

## 17–18. Подписки и платежи — NOT STARTED

1. **Реализовано:** ничего.
2–4. **Отсутствует:** всё — таблицы, провайдер, вебхуки, разграничение функций по тарифу, триал, срок Sandbox.
5. **Заблокировано:** [A-01] — без клиентских аккаунтов биллинг невозможен.
6. **Критические зависимости:** решение по модели доступа; выбор провайдера; юридический контур.
7. **Риски:** на сайте опубликованы конкретные цены (5 000 / 10 000 ₽) и неопределённое понятие «материалов» ([P-01]).
8. **Следующий шаг:** **не** внедрять провайдера. Пометить цены как ориентировочные и подтвердить готовность платить интервью (H2).
9. **Критерии завершения:** для Phase 3 — оплата работает и лимиты тарифов проверяются в коде; до тогда — пометка на сайте.
10. **Уверенность:** **Высокая** — отсутствие подтверждено поиском (0 совпадений).

## 19. Уведомления — FUNCTIONAL

1. **Реализовано:** `bot/tg/notifications/dispatcher.py` (385 строк): `notify_trade_open/close`, `notify_api_error`, `notify_risk_limit`, `notify_bot_started/stopped`; переключатели в меню; SSE-канал для Dashboard; `/api/internal/push` для событий из bot-процесса (защищён constant-time токеном + loopback по реальному TCP-peer).
2. **Работает фактически:** *INFERRED* для Telegram; SSE-эндпоинт проверен на принуждение auth (401).
3. **Частично:** очередей и повторов нет — fire-and-forget; при недоступности Telegram уведомление теряется.
4. **Отсутствует:** периодическая сводка в активном боте (есть только в legacy); email-канал; алертинг ([Q-06]).
5. **Заблокировано:** —
6. **Критические зависимости:** Telegram API.
7. **Риски:** **единственный канал оповещения — тот же Telegram, который может быть недоступен.** Для торгового сервиса это единая точка отказа оповещения.
8. **Следующий шаг:** второй независимый канал алертинга (внешний uptime-монитор).
9. **Критерии завершения:** сбой процесса вызывает алерт по каналу, не зависящему от Telegram.
10. **Уверенность:** **Средняя**.

## 20. Аналитика — PARTIALLY IMPLEMENTED

1. **Реализовано:** на **сайте** — PostHog (`identified_only`, ручной pageview), Vercel Analytics, Speed Insights, `lib/analytics/events.ts`, `scroll-analytics.tsx`.
2. **Работает фактически:** в dev не активна (подтверждено в console: «No requests will be sent to the server»).
3. **Частично:** подключена без согласия и privacy policy ([W-08], [S-01]).
4. **Отсутствует:** **продуктовая аналитика внутри продукта** — ни Dashboard, ни бот не отслеживают использование функций. Неизвестно, чем пользуются.
5. **Заблокировано:** юридическим контуром.
6. **Критические зависимости:** `NEXT_PUBLIC_POSTHOG_KEY`; privacy policy.
7. **Риски:** без данных воронки решения по [P-05], H1, H4, H8 принимать не на чем.
8. **Следующий шаг:** privacy policy → включить PostHog в production → собирать воронку.
9. **Критерии завершения:** воронка «визит → scroll → CTA → заявка» измеряется; известно топ-5 используемых функций продукта.
10. **Уверенность:** **Высокая**.

## 21. Тестирование — PARTIALLY IMPLEMENTED

1. **Реализовано:** 145 тест-функций в 7 файлах (`tests/dashboard_tests`, `platform_tests`, `security_tests`); 27 JS-тестов Dashboard на встроенном раннере Node; 6 контент-гейтов сайта; 2 гейта токенов.
2. **Работает фактически:** `pytest` → **161 passed, 72 skipped**, exit 0, 1.44 s; `format.test.mjs` → 27/27, exit 0; все 6 гейтов сайта exit 0.
3. **Частично:** **72 прогона (31 %) пропускаются** — все тесты аутентифицированных маршрутов ([Q-04]); внешне зелёный прогон не покрывает главный API-контракт.
4. **Отсутствует:** тесты `bot/tg/` (4 792 строки), `bot/main.py` (417), `bot/broker/` (1 375), `bot/learning/` (3 209), `bot/backtest/` (2 136) — **~12 000 строк без тестов** ([Q-05]); `mypy`; `pytest` даже не указан в `requirements.txt`.
5. **Заблокировано:** `qa:landing` (E2E) **падает** — Playwright не установлен ([Q-03]); `check-dashboard-tokens.mjs` **падает** — реальный дрейф токенов ([Q-02]).
6. **Критические зависимости:** тестовая БД + оператор для 72 тестов; Playwright для E2E.
7. **Риски:** два гейта красные, и без CI об этом никто не узнаёт.
8. **Следующий шаг:** CI ([Q-01]) — без него остальное не удержится.
9. **Критерии завершения:** 0 skipped в CI; оба падающих гейта зелёные; 5 приоритетных сценариев торгового ядра покрыты.
10. **Уверенность:** **Высокая** — все команды выполнены, exit code зафиксированы.

## 22. Безопасность — FUNCTIONAL

1. **Реализовано:** см. [07](07_SECURITY_PRIVACY_AND_COMPLIANCE_AUDIT.md) — серверные сессии, Argon2id, CSP с nonce (Dashboard), CSRF double-submit, двухуровневая защита от подбора, идемпотентность, audit trail, AES-256-GCM для секретов, маскирование PII и секретов, rate-limits, fail-closed доступ.
2. **Работает фактически:** **все** защищённые эндпоинты → 401; CSP с nonce в живом заголовке; секреты не в Git (`git check-ignore` + `git ls-files` подтверждают).
3. **Частично:** идемпотентность/аудит только на HTTP-пути ([S-05]); шифрование секретов **опционально** ([S-04]).
4. **Отсутствует:** CSP `script-src` на сайте ([S-02]); проверка `initData` ([T-03]); lock-файл Python-зависимостей; 2FA.
5. **Заблокировано:** —
6. **Критические зависимости:** `SECRETS_MASTER_KEY`; TLS в production (`QF_HTTPS=1` для HSTS).
7. **Риски:** **3 high-severity CVE** в npm-дереве (postcss, sharp), фикс требует мажорного апгрейда Next ([S-03]); мёртвые auth-модули ([S-06]); Python-зависимости не проверялись на уязвимости.
8. **Следующий шаг:** сделать `SECRETS_MASTER_KEY` обязательным в production; CSP на сайте через `Report-Only`.
9. **Критерии завершения:** production не стартует без мастер-ключа; CSP `script-src` активен; CVE закрыты или принятие риска зафиксировано с датой пересмотра.
10. **Уверенность:** **Высокая** для проверенного; **этот аудит не заменяет внешний security review перед работой с реальными деньгами.**

## 23. Deployment — PARTIALLY IMPLEMENTED

1. **Реализовано:** `start.sh` / `start.ps1` / `start.bat`; `docker-compose.yml` (TimescaleDB + Adminer, оба на loopback); `docs/WINDOWS_DEPLOYMENT.md` (265 строк) с запуском как Windows-служб через NSSM; `.claude/launch.json`; сайт готов к Vercel (`VERCEL_GIT_COMMIT_SHA` учтён).
2. **Работает фактически:** локальный запуск проверен для сайта, Dashboard и БД.
3. **Частично:** Dashboard только на Werkzeug dev-сервере ([D-02]); NSSM оборачивает тот же dev-сервер.
4. **Отсутствует:** production WSGI; TLS/реверс-прокси; единый признак среды (`QF_ENV`); lock-файл Python; Dockerfile для приложения (в compose только БД).
5. **Заблокировано:** —
6. **Критические зависимости:** Windows Server 2019 (заявленная цель); TLS-сертификат для Mini App (требование Telegram).
7. **Риски:** **фактическая production-среда не проверялась** — нет доступа. Все выводы по деплою основаны на скриптах и документации.
8. **Следующий шаг:** `waitress` + TLS-прокси; зафиксировать зависимости.
9. **Критерии завершения:** production не использует Werkzeug; HTTPS работает; HSTS активен; воспроизводимая установка зависимостей.
10. **Уверенность:** **Низкая** — среда недоступна, `start.ps1` не выполнялся.

## 24. Monitoring / alerting — NOT STARTED

1. **Реализовано:** health-checks (`/health`, `/api/v2/health`), `health_service` / `system_health_service` / `faults_service`, latency-probe собственных запросов, `correlation_id` в каждом ответе, `system_events`, структурное логирование с ротацией.
2. **Работает фактически:** `/health` отдаёт `{"db":true,"status":"ok"}` без утечек.
3. **Частично:** состояние видно только в Dashboard или через Telegram.
4. **Отсутствует:** `grep` по `sentry|prometheus|opentelemetry|statsd|datadog|grafana` → **0**. Нет агрегации ошибок, нет метрик, нет внешнего uptime-мониторинга, нет алертинга. (Примечательно: MCP-серверы Sentry и Grafana подключены к среде разработки — инструменты доступны, интеграций нет.)
5. **Заблокировано:** —
6. **Критические зависимости:** выбор стека наблюдаемости.
7. **Риски:** **остановка торговли может остаться незамеченной часами.** Для сервиса, управляющего деньгами, — блокер production.
8. **Следующий шаг:** внешний uptime-монитор на `/health` + Sentry (самые дешёвые шаги с наибольшим эффектом).
9. **Критерии завершения:** падение процесса даёт алерт ≤5 мин; необработанные исключения агрегируются; есть алерт «цикл не отчитывался N минут».
10. **Уверенность:** **Высокая**.

## 25. CI/CD — NOT STARTED

1–4. **Отсутствует полностью:** `.github/workflows` не существует; ни pre-commit, ни любой другой оркестратор.
5. **Заблокировано:** —
6. **Критические зависимости:** тестовая БД для 72 тестов; Playwright для E2E.
7. **Риски:** **прямое следствие уже наблюдается** — два гейта красные и это не было замечено ([Q-02], [Q-03]). Блокирует безопасный рефакторинг, которого проекту предстоит много.
8. **Следующий шаг:** один workflow с уже зелёными гейтами, затем добавить красные.
9. **Критерии завершения:** все 14 команд из [08 §1](08_QA_TESTING_PERFORMANCE_AND_RELIABILITY_AUDIT.md) выполняются на push/PR; красный CI блокирует merge.
10. **Уверенность:** **Высокая**.

## 26. Документация — PARTIALLY IMPLEMENTED (противоречива)

1. **Реализовано:** 44 Markdown-документа (`docs/` 15, `design/` 13, `website/docs/` 17), `README.md` (1 312 строк), `CLAUDE.md`. **Исключительная документация внутри кода** — docstrings объясняют *почему*, с описанием отвергнутых альтернатив и прошлых инцидентов.
2. **Работает фактически:** внутрикодовая документация — сильнейшая сторона проекта; неоднократно позволила этому аудиту понять намерение, а не только реализацию.
3. **Частично:** большинство документов относятся к завершённым итерациям редизайна.
4. **Отсутствует:** актуальное описание архитектуры (создаётся этим Source Pack).
5. **Заблокировано:** —
6. **Критические зависимости:** —
7. **Риски:** **`CLAUDE.md` содержит минимум 5 неверных утверждений** (корень проекта, ветка, `BOT_TOKEN` вместо `TELEGRAM_TOKEN`, «JWT-based» auth, aiogram-терминология), а `README.md` переоценивает продукт («институциональная», «байесовская»). Новый разработчик или AI-агент, доверившись им, пойдёт по ложному пути. Полный разбор — [13](13_STALE_DOCUMENTS_REGISTER.md).
8. **Следующий шаг:** исправить `CLAUDE.md` и `README.md`; принять этот Source Pack как источник истины.
9. **Критерии завершения:** `CLAUDE.md` описывает фактические корень, ветку, переменные и auth; `README.md` без оверклеймов; устаревшие документы помечены.
10. **Уверенность:** **Высокая** — все противоречия проверены против кода.

## 27–28. Onboarding и поддержка — NOT STARTED

- **Onboarding:** `grep -rniE "onboarding|wizard|getting.?started"` по `bot/` → нет. Ни в Dashboard, ни в боте (только `/start` + `/help`). Тарифный «Sandbox 7 дней» не существует как механизм.
- **Поддержка:** канала нет. `footer.linkContact` ведёт на `#access` (ту же форму). Ассистент на сайте: «Пока недоступен». Механизма обратной связи нет нигде.
- **Заблокировано:** отсутствием клиентских аккаунтов ([A-01]).
- **Следующий шаг:** реальный контакт в footer (XS, снимает и fallback для [W-01]); процедура ручного provisioning для beta.
- **Уверенность:** **Высокая**.

## 29. Backup / recovery — NOT STARTED

Разобрано в [B-06](06_BACKEND_DATA_AND_INTEGRATIONS_AUDIT.md). Кратко: том Docker без средств резервирования; `grep` по `backup|pg_dump|restore|pgbackrest|wal` → процедуры нет; единственная связанная функция — **необратимое удаление** `prune-equity`. Потеря тома = потеря торговой истории, `belief_system` и audit trail. **Блокер production.** Уверенность: **Высокая**.

## 30. Юридический контур — NOT STARTED

Разобрано в [S-01](07_SECURITY_PRIVACY_AND_COMPLIANCE_AUDIT.md). Кратко: `/ru/privacy`, `/ru/terms`, `/ru/legal` → **404**, при собираемом email и трёх активных аналитических сервисах. Есть корректный дисклеймер в footer, но одного его недостаточно. Требует **профильного юриста** — самая длинная внешняя зависимость, начинать раньше остальных. Уверенность: **Высокая** (в части факта отсутствия; юридических выводов не делается).

---

## Что реально работает сегодня: одним абзацем

Оператор с доступом может: запустить Dashboard и войти по паролю; запустить/остановить торговый движок; смотреть портфель, позиции, сделки, equity, просадку, сигналы, стратегии, гипотезы, риск-лимиты, события и audit trail на 12 экранах; закрыть позицию и исполнить сигнал с идемпотентностью и записью в журнал; запустить бэктест и выгрузить результат; запустить цикл обучения; управлять системными ключами; торговать вручную из Telegram с двухшаговым подтверждением и защитой от подмены данных; получать уведомления о сделках, ошибках и риск-лимитах; вести paper-торговлю с комиссией и проскальзыванием. Автономный цикл может сам сканировать 4 тикера MOEX по 25 правилам, проверять уверенность и риск и **исполнять сделки без подтверждения** — в песочнице по умолчанию.

**Чего не может никто:** зарегистрироваться, оплатить, подключить свой брокерский счёт, подтвердить автоматическую сделку, получить поддержку, прочитать privacy policy, восстановить данные из бэкапа и узнать о сбое без ручной проверки.

---

## Top Critical Findings

1. Клиентского слоя не существует ([A-01], [B-01], [D-01]) — блокирует подписки, платежи, onboarding и публичный запуск.
2. Автономное исполнение без подтверждения при обещании обратного на сайте ([A-02], [T-01], [P-03]).
3. Нет CI/CD, и два существующих гейта красные незамеченными ([Q-01], [Q-02], [Q-03]).
4. Нет бэкапа торговой истории и audit trail ([B-06]).
5. Нет мониторинга и алертинга ([Q-06]).
6. Юридический контур отсутствует ([S-01]).
7. Заявки на доступ по умолчанию не сохраняются ([W-01]).
8. Торговое окно в наивном локальном времени ([A-04]).
9. ~12 000 строк торговой логики без тестов ([Q-05]).
10. Документация противоречит коду в базовых вещах ([X-01], [X-02]).

## Recommended Next Steps

Порядок — по критерию «снимает блокер / стоит дёшево»:
1. Принять решение по модели доступа ([OQ-ARCH-01](14_OPEN_QUESTIONS_AND_DECISIONS.md)) — от него зависит объём почти всей дальнейшей работы.
2. Исправить документацию (RM-P0-004) — XS, устраняет ложные ориентиры для всех участников.
3. Завести CI на зелёных гейтах (RM-P0-003).
4. Закрыть production-чеклист переменных сайта (RM-P0-006, RM-P0-007).
5. Ввести `EXECUTION_MODE=confirm` по умолчанию (RM-P0-002).
6. Начать юридическую работу (RM-P1-011) — самая долгая внешняя зависимость.
7. Настроить бэкап с проверенным восстановлением (RM-P1-015).
8. Исправить таймзону торгового окна (RM-P1-006).

## Уровень уверенности документа

**Высокая** для 20 из 30 компонентов — статус подтверждён запуском, HTTP-пробами, выполненными командами или прямым поиском.
**Средняя** для 8 компонентов, где код прочитан полностью, но рантайм не наблюдался (торговое ядро, Telegram, брокеры, paper, backtest, risk, уведомления, Dashboard UI).
**Низкая** для deployment — production-среда недоступна.
