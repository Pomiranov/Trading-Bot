# Хендофф на утро 2026-08-03

Обновлён в ночь на 03.08 **после сведения записей**. Раздел «что подлежит
сведению» из первой редакции больше не актуален и заменён фактическим итогом.

Этот файл — **точка входа**, а не хранилище. Содержание живёт в
`docs/PROJECT_STATE.md`; здесь порядок и ссылки. Копировать сюда тексты нельзя —
по причине из раздела 3.

---

## 1. ПОРЯДОК УТРА. Отклоняться нельзя

### Шаг 1 — сверка телеграма с §2з, ДОСЛОВНО

Ожидаемый текст за слот 03.08 00:15 зафиксирован **до** прогона: `PROJECT_STATE.md`,
раздел 2з. Неизменяемая копия с SHA-256 —
`measurements/2026-08-03_prediction_freeze/`. Отличаться имеют право только даты,
время и число секунд.

**Проверять признаки Р1–Р6 ПООТДЕЛЬНО**, а не «похоже на предсказанное»:

| | что смотреть | расхождение означает |
|---|---|---|
| Р1 | 🚨 «Прогон отчитался успехом без сессии» **первой строкой** | пришло ✅ — разбор неверен, СТОП |
| Р2 | 🚨 «Журнал прогонов пересоздан после слота» **ОТСУТСТВУЕТ** | пришла второй раз — журнал пересоздаётся, **дефект**, СТОП |
| Р3 | ⚠ «Свечи не поступают» + `(3 дн. назад, порог 2)` | иное число — ошибка в возрасте либо свечи пришли |
| Р4 | `Обработано до: <b>2026-07-31</b>` | `2026-08-01`/`02` — бары появились, значит 02.08 их не было по задержке публикации |
| Р5 | **НЕТ** строк «пропустил бары» / «не объяснено журналом» / «выбросил бары» | есть — гонка чтения порождает ложные A3, приоритет П4 растёт |
| Р6 | сообщение **пришло**, код **0** | не пришло — П1 меняет характер, разбирать сеть |

⚠ **Обоснование Р4 в §2з известно НЕВЕРНЫМ** («биржа не торговала 01–02.08»;
замер по ISS его опроверг — MOEX торгует по выходным, 22 дня из 28). Замена
готова и лежит в разделе 2к. **Правка внесена НЕ БУДЕТ до конца шага 1:**
подогнанный под известный факт признак перестаёт быть предсказанием.
⚠ **Р6 доказывает только целость токена и конфига**, но НЕ причину отказа 02.08.

**Уже известно из журнала (это НЕ сверка):** прогон 03.08 состоялся —
`start 00:15:01.629406`, `finish 00:15:11.730217`, `rc 0`, — и **строки
`session` за 03.08 нет**. Это чтение журнала; сверка выполняется по пришедшему
сообщению. Пункт 11 и пункт 12 чеклиста 02.08 разошлись впервые, и различать их
теперь обязательно.

### Шаг 2 — приёмка блока A, ДВЕНАДЦАТЬ пунктов

Счёт на сейчас: **2 из 12**. Пройдены «прогон состоялся в свой слот» и «текст
вердикта совпал с §2з дословно». **Пункт 12 «сообщение получено человеком» —
ПРОВАЛЕН** 02.08. Засчитывать «с оговоркой» запрещено: этот ход дал ложное
«девять из девяти» 31.07.

### Шаг 3 — ротация `TELEGRAM_TOKEN`, ~2 минуты

Инструкция готова: **`docs/TOKEN_ROTATION.md`**. Заранее ничего не исполнялось.

**Зачем.** `logs/forward_d1.log` содержит **живой токен в открытом виде**
(2 вхождения, строки 1129–1130): `httpx` пишет полный URL на уровне INFO, минуя
маскировку `notify.py`. Логи **не ротируются вовсе** ⇒ токен копится без предела.

**Почему после сверки, а не до.** Правка `.env` до шага 1 делает исход **Р6**
неотличимым: не пришло — это отозванный токен, неподхваченный новый или всё та
же сеть? Один шаг, три объяснения.

**В репозиторий токен НЕ утекал** — проверено тремя формами поиска по 14 ссылкам
и 127 коммитам, включая недостижимые объекты (§5, кандидат в долг про httpx).

### Шаг 4 — и только теперь всё остальное

---

## 2. СВЕДЕНИЕ ВЫПОЛНЕНО в ночь на 03.08. Что где теперь

| | было | стало |
|---|---|---|
| **К1** боевой `docs/PROJECT_STATE.md` | не закоммичен, +549/−15 | **сохранён** коммитом `5c41bfb` («только документы»), затем **сведён** коммитом `9e5ba75` |
| **К2** `PROJECT_STATE_PENDING_2026-08-02.md` на ветке | 6 коммитов, 1174 строки | **заглушка со ссылкой**, коммит `5237efa`. Не удалена намеренно |
| **К3** `measurements/` в двух деревьях | расщеплён | **собран сюда**; оригиналы в ветке не удалены, дубли названы в `measurements/00_INDEX.md`, действующей считается копия здесь |

**Конфликты С1–С4 разрешены так:**

| | что было | итог |
|---|---|---|
| С1 `:2263` обоснование Р4 | «биржа не торговала 01–02.08» | **НЕ ТРОНУТО до сверки** — намеренно, см. шаг 1 |
| С2 «ложь по расписанию» | «приходить каждые выходные» | отозвано; П3 в прежней форме снят с очереди |
| С3 очередь «П3 + 0.7» | одним заходом | отозвана |
| С4 основание шага 0.7 | «каждые выходные и праздники» | переписано: «отличить редкий неторговый день от сломанной загрузки»; форма календаря — поденьная, от ISS, три состояния |

**Провенанс** заведён **одним блоком** в «ГДЕ МЫ СЕЙЧАС» (не 28 копиями у
каждого цитирования — это был бы долг №24). Отозвано «IS с фильтром 4 / +547».
Запрет на `30 / 60.0% / 1.20 / +74 055` проверен по всем 24 вхождениям и
доставлен в два места, где число стояло без пометки.

**НЕ сведён и не мержится до приёмки блока A — КОД:** `bot/notify.py`,
`bot/forward_healthcheck.py`, `tests/forward_tests/test_notify_retry.py`,
`tools/silent_zeros_probe.py`, `docs/REHEARSAL_SLOT_PLAN.md` — ветка
`worktree-notify-p2p1`, `5237efa`.

---

## 3. РИСК: знание в копиях. Класс №31

**Записан по факту.** 02.08 при составлении описи молчаливых нулей Ф6 и Ф7 были
помечены как лежащие на живом пути. Это неверно, и **правильный ответ уже был
записан в `PROJECT_STATE.md`, раздел 3** («Форвард не касается `risk_state.json`»,
проверено 28.07). Факт не был прочитан и был переоткрыт с неверной
классификацией; исправлено только после прямого вопроса человека.

Цена измерена: **один такой промах за сутки.** После сведения копий стало две
(боевой файл + заглушка), но риск не снят — он снимается только тем, что перед
записью нового факта ищут уже записанный.

Пример того, что дисциплина работает: 03.08 факт «логи не ротируются» **не был
заведён заново** — он уже стоял в §9, и туда добавлено только следствие.

---

## 4. ЧТО НЕ РЕШЕНО И ЖДЁТ НИКА

| # | предмет | что решить |
|---|---|---|
| **Н1** | форма №42 (рваные ряды) | вариант I / II / III; блокирует перемер троек на 20 бумагах |
| **Н2** | форма №44 (проскальзывание) | если М3 считает перцентили — **назначить популяцию сравнения**; один бар TATN стоит в 49.2-м перцентиле против всех баров и в 98.2-м против выходных |
| **Н3** | гейт Ф1/Ф3 на расширение `FORWARD_TICKERS` | закрыть дыры либо доказывать ≥200 баров у новой бумаги |
| **Н4** | Ф2 — фильтр по префиксу `strategy_id` | из 14 стратегий префиксу отвечают 5; для 9 фильтр не применяется вовсе |
| **Н5** | пакет Ф7 + №9 для партнёра | чинить вместе: №9 создаёт ущерб, Ф7 его прячет |
| **Н6** | роль БД с правами администратора | `trader` имеет `rolsuper = t`; блокирует репетиционный слот |
| **Н7** | **форма починки httpx-утечки** | «только тест» / «тест + сторож» / «тест + отдельная задача». Тест исполняется когда его запустили, а утечка копится непрерывно |
| **Н8** | **ротация логов** — отдельный предмет в очереди | форма обязана сохранять историю, а не усекать: `forward_d1.log` — единственный полный след прогонов с 12.07 |

Кандидаты в долг **без номеров** (номер выделяет Ник): httpx-утечка токена, роль
БД, `session_date`, `IS_END`, «молчаливая смерть сторожа» — все в §5.

---

## 5. Замер в очереди

**Двигалось ли доверие в `belief_system` хоть у одной стратегии хоть раз.**
§9, «СОСТОЯНИЕ НА 2026-08-03». Метод и обоснование записаны до замера; известно
без него, что все 14 строк имеют `confidence 0.5000`, `total_trades 0` и один и
тот же `updated_at` — но это **снимок, а не история**.

---

## 6. Состояние на момент записи

* боевая папка: ветка `merge-learning-nik`, `HEAD = 9e5ba75`, синхронна с
  `origin`, рабочее дерево чисто; **правок кода за сессию — ноль файлов**;
* ветка `worktree-notify-p2p1` = `5237efa`, синхронна, **не мержена**;
* тесты: **230 из 230** зелёные, 20 новых показаны падавшими до правки;
* настройки машины не менялись; `.env` не трогался.
