# Хендофф на утро 2026-08-03

Написан в ночь на 03.08, **до** ночного прогона: на момент записи
`NextRunTime` обеих задач ещё в будущем (`D1 03.08 00:15:15`,
`Healthcheck 03.08 09:00`), `LastTaskResult` сторожа всё ещё **1** от 02.08,
состояние сторожа всё ещё от 01.08 11:38:46. Улика цела.

Этот файл — **точка входа**, а не хранилище. Содержание живёт в
`docs/PROJECT_STATE_PENDING_2026-08-02.md`; здесь только порядок и ссылки.
Копировать сюда тексты нельзя — ровно по причине из раздела 3 ниже.

---

## 1. ПОРЯДОК УТРА. Отклоняться нельзя

### Шаг 1 — сверка телеграма с §2з, ДОСЛОВНО

Ожидаемый текст за слот 03.08 00:15 зафиксирован **до** прогона в боевом
`docs/PROJECT_STATE.md`, раздел 2з. Отличаться имеют право только даты, время и
число секунд.

**Проверять признаки Р1–Р6 ПООТДЕЛЬНО**, а не «похоже на предсказанное»:

| | что смотреть | расхождение означает |
|---|---|---|
| Р1 | 🚨 «Прогон отчитался успехом без сессии» **первой строкой** | пришло ✅ — разбор неверен, СТОП |
| Р2 | 🚨 «Журнал прогонов пересоздан после слота» **ОТСУТСТВУЕТ** | пришла второй раз — журнал пересоздаётся, **дефект**, СТОП |
| Р3 | ⚠ «Свечи не поступают» + `(3 дн. назад, порог 2)` | иное число — ошибка в возрасте либо свечи пришли |
| Р4 | `Обработано до: <b>2026-07-31</b>` | `2026-08-01`/`02` — бары появились, значит 02.08 их не было по задержке публикации |
| Р5 | **НЕТ** строк «пропустил бары» / «не объяснено журналом» / «выбросил бары» | есть — гонка чтения порождает ложные A3, приоритет П4 растёт |
| Р6 | сообщение **пришло**, код **0** | не пришло — П1 меняет характер, разбирать сеть |

⚠ **Р4 в боевом документе записан с НЕВЕРНЫМ обоснованием** («биржа не торговала
01–02.08»). Замена — PENDING §7.1, правка 1. Сам признак верен, неверна причина.
⚠ **Р6 доказывает только целость токена и конфига**, но НЕ причину отказа 02.08.

### Шаг 2 — приёмка блока A, ДВЕНАДЦАТЬ пунктов

Счёт на сейчас: **2 из 12**. Пройдены: «прогон состоялся в свой слот» (доказано
положительно) и «текст вердикта совпал с §2з дословно».
**Пункт 12 «сообщение получено человеком» — ПРОВАЛЕН** 02.08.

Засчитывать «с оговоркой» запрещено — это ход, давший ложное «девять из девяти»
31.07.

### Шаг 3 — и только теперь всё остальное

Первая работа после приёмки — **сведение трёх копий знания** (раздел 3).
Не правки кода. Основание — там же.

---

## 2. ЧТО ПОДЛЕЖИТ СВЕДЕНИЮ. Три несведённые копии

| # | где | состояние | объём |
|---|---|---|---|
| **К1** | `D:\Trading-Bot-Nik` → `docs/PROJECT_STATE.md` | **НЕ закоммичен**, лежит в рабочем дереве боевой папки | +549 / −15 строк |
| **К2** | ветка `worktree-notify-p2p1` → `docs/PROJECT_STATE_PENDING_2026-08-02.md` | закоммичен и запушен, **6 коммитов** | 1174 строки |
| **К3** | `measurements/` в двух местах: `2026-08-02_blockA/` (боевая папка, **не отслеживается git**, 16 файлов, 12.3 МБ) и `2026-08-02_blockA_part2/README.md` (в ветке, закоммичен) | расщеплён между двумя рабочими деревьями | — |

Ветка целиком: 8 файлов, +2290 / −21.
Коммиты: `1bf96a5`, `48226f9`, `3558ee5`, `c664c40`, `b7d29f8`, `2bbd4fe`.

### Конфликтующие места — поимённо, с номерами строк К1

Все четыре — К2 **отзывает** написанное в К1. При сведении К2 старше по времени
и опирается на замеры; К1 писался до них.

| # | К1, строка | что там стоит | чем заменяется |
|---|---|---|---|
| **С1** | `PROJECT_STATE.md:2263` | Р4: «биржа не торговала 01–02.08, состояние не двигалось» | PENDING §7.1, правка 1 — MOEX торгует по выходным (22 дня из 28), причина неверна |
| **С2** | `:6608-6611` | «будет приходить **каждые выходные и каждый биржевой праздник**… ложью по расписанию» | PENDING §1 — **ОТОЗВАНО**: тревога раз в месяц, сторож был прав, П3 гасил бы правдивый редкий сигнал |
| **С3** | `:7537-7540` | «1б. П3 + 0.7 одним заходом», основание «ложь по расписанию» | PENDING §1 — П3 в прежней форме **снят с очереди**; 0.7 остаётся с другим основанием |
| **С4** | `:7743-7744` | 0.7 «приходит каждые выходные и праздники» | PENDING §1 и §7.9 — основание «отличить редкий неторговый день от сломанной загрузки»; форма календаря **поденьная и от ISS** |

### Места К1, к которым К2 добавляет провенанс (не конфликт, но обязательно)

| К1, строка | что стоит | что приписать |
|---|---|---|
| `:529-532`, `:570-573` | таблицы A/B: «IS с фильтром 4 / 50.0% / 1.03 / +1 756» и «новая 4 / 50.0% / 1.01 / **+547**» | PENDING §7.10 — **отозвать как результат**: 4 сделки прошли в 199 сессиях, где фильтр выключен по построению; на честном окне с 2024-04-22 сделок с фильтром **ноль** |
| везде, где цитируется `18 / 72.2% / 1.64` | опорная тройка | PENDING §7.4(а) — «OOS-проверкой не является», два основания |

### Что К2 добавляет заново (конфликтов нет)

Правило 14 §8 (идёт после правила 13, оно уже в К1); результат замера №41
(ноль дыр); кандидаты в долг — роль БД с правами администратора и «дату сессии
только через `session_date`»; опись молчаливых нулей Ф1–Ф9; гейт на расширение
`FORWARD_TICKERS`; дыра «два пути к свечам»; план репетиционного слота.

### Порядок сведения (предлагаемый, решает Ник)

1. К1 закоммитить **как есть**, без правок — иначе история потеряется;
2. поверх применить С1–С4 и провенансы отдельным коммитом «отзывы по замерам
   02.08», чтобы отзыв был виден как отзыв;
3. К2 влить в `PROJECT_STATE.md`, `PROJECT_STATE_PENDING_*.md` удалить —
   иначе появится четвёртая копия;
4. К3 решить отдельно: 12.3 МБ минидампов в git не идут (`.gitignore` уже
   закрывает `*.dmp`), а `00_MANIFEST.md` и текстовые замеры — идут.

**Ветку `worktree-notify-p2p1` НЕ мержить до конца шага 2.**

---

## 3. РИСК: знание в трёх несведённых копиях. Класс №31

**Записан по факту, случившемуся сегодня, а не из осторожности.**

02.08 при составлении описи молчаливых нулей я пометил Ф6 и Ф7
(`risk_manager.py`) как лежащие на живом пути. Это неверно, и **в проекте уже
был записан правильный ответ** — `PROJECT_STATE.md:3344-3359`, «Форвард не
касается `risk_state.json`», проверено 28.07:

> «Из `RiskManager` форвард вызывает **только** `calculate_position` … и читает
> `self.risk.cfg.atr_stop_multiplier`. `check_trade_allowed` / `register_open` /
> `register_close` … вызываются только из `main.py`, `ui/telegram_bot.py`,
> `gateway/trade_gateway.py`; в форвардном пути их нет.»

Факт **не был прочитан** и был **переоткрыт с неверной классификацией**.
Исправлено только на следующем заходе и только потому, что Ник задал прямой
вопрос.

**Механизм риска.** Знание разложено по трём копиям (К1/К2/К3), ни одна не
является полной, и поиск по одной даёт ложное «этого нет». Чем дольше копии
живут раздельно, тем чаще будет повторяться сегодняшний случай: **уже
установленный факт переоткрывается заново и с ошибкой**.

**Класс — №31** (предикат по копиям): две копии одного знания расходятся молча,
и расхождение обнаруживается не проверкой, а случайно.

**⇒ Сведение — ПЕРВАЯ работа после приёмки блока A.** Не после правок кода, не
после следующего замера. Обоснование — не аккуратность, а измеренная цена:
один такой промах за сутки, найденный только вопросом человека.

---

## 4. ЧТО НЕ РЕШЕНО И ЖДЁТ НИКА

Ничего из этого сегодня не решалось и в очередь не ставилось.

| # | предмет | что именно решить | почему блокирует |
|---|---|---|---|
| **Н1** | **форма №42** (рваные ряды) | вариант I / II / III; цена варианта III пересчитана — 13 из 18 (72%) с фильтром и 18 из 29 (62%) без | блокирует перемер троек на 20 бумагах |
| **Н2** | **форма №44** (проскальзывание) | если М3 считает перцентили — **назначить популяцию сравнения**; один и тот же бар TATN 28.02 стоит в 49.2-м перцентиле против всех баров и в 98.2-м против выходных (PENDING §7.0-бис) | без неё любая тройка с касаниями тонких баров остаётся предварительной; блокирует шаг 2 «решение по выходным» |
| **Н3** | **Ф1 / Ф3** — гейт на расширение `FORWARD_TICKERS` | закрыть дыры либо доказывать ≥200 баров у каждой новой бумаги (PENDING §7.13) | набор объявлен растущим; первая короткая бумага включает обе дыры молча |
| **Н4** | **Ф2** — фильтр применяется по префиксу `strategy_id` | из 14 стратегий префиксу соответствуют 5; для 9 остальных фильтр не применяется вовсе | сегодня не стреляет только потому, что запущена одна стратегия из четырнадцати |
| **Н5** | **пакет Ф7 + №9** — для партнёра | чинить **вместе**: №9 (TOCTOU) создаёт ущерб, Ф7 (`register_close → 0.0`) его прячет от дневного лимита | код напарника, №9 ждёт его ответа с 27.07 |
| **Н6** | **роль БД с правами администратора** | `trader` = `DB_USER` имеет `rolsuper/rolcreatedb/rolcreaterole = t`; предложены 4 шага, четвёртый (тест-предикат) дёшев и может идти первым | блокирует репетиционный слот: разделение «боевая БД / клон» по имени базы не гарантия, пока роль суперпользовательская |

Плюс кандидаты в долг **без номеров** (номер выделяет Ник): «молчаливая смерть
сторожа» (К1, §5), «дату сессии только через `session_date`» (PENDING §7.4в),
«роль БД» (PENDING §5).

---

## 5. Замер, поставленный в очередь на завтра

**Двигалось ли доверие в `belief_system` хоть у одной стратегии хоть раз.**
Подробности и почему это важно — PENDING §7.15. Сегодня **не делался**
намеренно.

---

## 6. Состояние на момент записи

* боевая папка: `HEAD = 6dc3d8b`, не сдвигался; изменён только
  `docs/PROJECT_STATE.md`, плюс не отслеживаемый `measurements/2026-08-02_blockA/`;
* ветка `worktree-notify-p2p1` = `2bbd4fe`, запушена, синхронна с `origin`;
* весь набор тестов зелёный: **230 из 230**, из них 20 новых показаны
  падавшими до правки;
* правок кода в боевой папке не делалось; настройки машины не менялись.
