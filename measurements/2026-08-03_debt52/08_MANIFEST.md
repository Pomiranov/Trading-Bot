# Долг №52: заглядывание в дневном гейте убрано. Улика 2026-08-03

## Режим

`SIGNAL_WINDOW_BARS` не тронут (заморожен до приёмки блока 0.5, №48). Прогонов с
`--learn` не было; `run_osc_oos_debug.py` работает с `orchestrator=None`, ни одного
INSERT в `trades`. `.gitignore` не правился. Пересчёт окон под H4 (№50) в задачу не
входил и не делался.

Правки кода — две: `bot/backtest/engine.py::_downtrend_gate` (условный `.shift(1)`)
и новый тест `tests/forward_tests/test_downtrend_gate_causality.py`.

## Артефакты

| файл | что внутри |
|---|---|
| `00_PREDICTION.md` | мои ожидания, коммит `2990396` **до** правки |
| `01_SCOPE.txt` | часть 2: предикат по всем конфигурациям правил и стратегиям из данных |
| `02_TEST_BEFORE.txt` | часть 1: тест **ПАДАЕТ** на нынешнем коде |
| `03_TEST_AFTER.txt` | Г1: тот же тест зелёный после правки |
| `04_RUN_ON.txt` | прогон плеча С фильтром, `--as-of 2026-07-29` |
| `05_RUN_OFF.txt` | прогон плеча БЕЗ фильтра |
| `06_COMPARE.txt` | Г2 и Г3: сверка до/после по архивным и новым CSV |
| `07_TESTS_FULL.txt` | Г4: полный прогон тестов |
| `SHA256SUMS.txt` | SHA-256 перечисленного |

**Гейт-CSV в git не идут** (`measurements/**/*.csv` в `.gitignore` — конвенция:
данные не версионируются), но лежат рядом и опознаваемы по SHA-256:

```
6cb04bc8e74bf8e5709b3448676bcdd62c10c644a9c3e17047912b0b3507cc5c  osc_debug_post25_on__run-2026-07-31__last-session-2026-07-29.csv
ab7a535450b18ea8dec098b05ed27f2a66e0c8e1f62fbe062e66979d0893797b  osc_debug_post25_off__run-2026-07-31__last-session-2026-07-29.csv
051adf507b2fa9e8f1a32fccbceea0fba500d0cb7686ccd5cc6bc6b40adc6991  osc_debug_debt52after_on__run-2026-08-03__last-session-2026-07-29.csv
ab7a535450b18ea8dec098b05ed27f2a66e0c8e1f62fbe062e66979d0893797b  osc_debug_debt52after_off__run-2026-08-03__last-session-2026-07-29.csv
```

🚩 **Плечо БЕЗ фильтра совпало БАЙТ В БАЙТ с архивным** (`ab7a5354…` = `ab7a5354…`).
Это сильнейший контроль, какой здесь возможен: он доказывает сразу, что данные не
дрейфовали с 31.07 **и** что правка того пути не касалась. Значит изменение
H4-плеча с фильтром вызвано **правкой**, а не данными.

## Часть 1 — дефект воспроизведён, тест показан ПАДАЮЩИМ

Синтетика по примеру из тела долга: дневной признак `04.01 = False`,
`05.01 = True` (close 99), H4-бары 04:00/08:00/12:00 несут закрытие 105 предыдущего
дня, 20:00 — 99 своего.

```
2 failed, 3 passed
FAILED test_intraday_bar_takes_previous_session_not_its_own
FAILED test_no_intraday_bar_sees_its_own_session   AssertionError: 12 != 0 : 12 из 1996 баров видят свою сессию
```

Бар `05.01 04:00` получал `True` — значение, посчитанное по цене `20:00` того же
дня, то есть **16 часов будущего**. Три зелёных до правки — не случайность: они
проверяют, что синтетика нетривиальна, что D1 не сдвинут и что край серии попадает
в прогрев; такими они обязаны быть в обоих состояниях.

## Часть 2 — область ПРЕДИКАТОМ. Утверждение долга подтверждено

45 файлов `*.y*ml` обходом дерева → 8 конфигураций правил (признак: верхний уровень
— отображение и есть ключ `rules`) → гейт читается проектным
`RulesEngine.structural_downtrend_filter`:

| конфигурация | `filters:` | дневной гейт |
|---|---|---|
| `knowledge/rules/rules_osc_range.yaml` | **отображение** | **ВКЛЮЧЁН** |
| `knowledge/rules/rules.yaml` | список | выключен |
| `knowledge/rules.yaml` | список | выключен |
| `knowledge/rules/rules_wrd_moex.yaml` | список | выключен |

Плюс четыре их копии в worktree `debt-25-37-28`; копия `rules_osc_range.yaml`
байт-идентична боевой (`git hash-object` даёт один и тот же
`669b3dd154c0ef45eaa2eb4a9e4e994622359511`). **Третьей конфигурации с включённым
гейтом нет** — останавливаться не пришлось.

**Стратегии ИЗ ДАННЫХ** (`SELECT strategy_id, timeframe, count(*) FROM trades`):

```
osc_range_moex      H1    15      trend_moex        H1     153
osc_range_moex_d1   D1    34      trend_moex_d1     D1     272
osc_range_moex_h1   H1   560      trend_moex_h1     H1    5024
osc_range_moex_h4   H4   171      trend_moex_h4     H4    1303
```

⚠ **Поправка к описи поражения в теле долга.** Долг называет «строки
`osc_range_moex_h4` (171) и `osc_range_moex_h1` (560)». Данные подтверждают эти два
числа точно и добавляют **третий** набор строк, в долге не названный:
`osc_range_moex` на `H1` — **15** строк. Итого не-D1 строк `osc_range` не 731, а
**746**. Были ли те 15 посчитаны при включённом гейте, из `trades` не
устанавливается — их отпечаток правил здесь не сверялся.

`trend_moex*` на H4/H1 не задет: у `rules.yaml` гейт выключен — теперь это замер, а
не чтение.

## Г1 — тест зелёный, и он же был показан падающим

```
5 passed in 1.04s
```

## Г2 — D1 НЕ СДВИНУЛСЯ. Сверка до копейки

Все двенадцать строк D1 совпали «до» и «после» **точно**:

| плечо / разрез | до | после |
|---|---|---|
| D1 с фильтром IS | 4 / 50.0% / 1.009 / +547.26 | то же |
| **D1 с фильтром OOS** | **18 / 72.2% / 1.638 / +127 747.97** | **то же** |
| D1 с фильтром ВСЁ | 22 / 68.2% / 1.491 / +128 295.23 | то же |
| D1 без фильтра IS | 5 / 60.0% / 1.708 / +43 310.36 | то же |
| **D1 без фильтра OOS** | **29 / 58.6% / 1.141 / +52 937.30** | **то же** |
| D1 без фильтра ВСЁ | 34 / 58.8% / 1.220 / +96 247.66 | то же |

Обе опорные тройки воспроизведены. По плечу без фильтра опубликованное `PF 1.14`
дано с двумя знаками, посчитанное — `1.141`; `n`, `WR` и `PnL` совпадают до
копейки. Автоматическая сверка в `06_COMPARE.txt` пометила это как расхождение —
это **дефект моего допуска** (0.0005 против опубликованной точности 0.01), а не
расхождение чисел. Решающий критерий Г2 — «до == после» — выполнен точно.

Подтверждает и то, что П1 угадан: сдвиг сделан условным, безусловный провалил бы
этот гейт.

## Г3 — ПЕРЕМЕР H4. ПРЕДСКАЗАНИЕ ДОЛГА №52 ОПРОВЕРГНУТО

| плечо / разрез | до правки | после правки |
|---|---|---|
| H4 с фильтром IS | 35 / 42.9% / 0.743 / −88 194.22 | 35 / 42.9% / **0.740** / −89 485.27 |
| **H4 с фильтром OOS** | 59 / 32.2% / **0.364** / −357 958.92 | 56 / 28.6% / **0.335** / −374 554.86 |
| **H4 с фильтром ВСЁ** | 94 / 36.2% / **0.508** / −446 153.14 | 91 / 34.1% / **0.489** / −464 040.13 |
| H4 без фильтра IS | 57 / 50.9% / 1.446 / +194 216.77 | **без изменений** |
| H4 без фильтра OOS | 114 / 38.6% / 0.730 / −225 461.66 | **без изменений** |
| H4 без фильтра ВСЁ | 171 / 42.7% / 0.975 / −31 244.89 | **без изменений** |

**Предсказание долга требовало РОСТА `PF` плеча с фильтром. `PF` УПАЛ:**

```
OOS : 0.364 -> 0.335   НЕ ВЫРОС
ВСЁ : 0.508 -> 0.489   НЕ ВЫРОС
```

Согласно заданию объяснение здесь не даётся. Расхождение с предсказанием
зафиксировано; поиск второго дефекта заводится, первый кандидат — **долг №53**
(бар D1 за сессию `2025-04-10` испорчен у всех 12 бумаг, и ISS отдаёт его таким и
сегодня).

Плечо без фильтра осталось прежним — как и требовалось: там гейт возвращает `None`
до ресемпла, заглядывания нет по построению.

**П4 подтверждён:** число сделок с фильтром изменилось (59 → 56, 94 → 91), значит
сдвиг доехал до раздачи значений.

## Г4 — тесты целиком, с областью рядом с числом

**273 passed**, команда `.venv/Scripts/python.exe -m pytest tests/ -q` из
`D:/Trading-Bot-Nik`, дерево `merge-learning-nik`. Разбивка:
`tests/forward_tests` 215 + `tests/platform_tests` 39 + `tests/security_tests` 19 =
273. Независимая форма — статический счёт `def test_` по `tests/` даёт **273**, то
же число. До этой задачи (коммит `2990396`) было **268**; прирост **+5** — ровно
пять новых тестов этого файла.

## Часть 5 — ЧТО ЭТИ ЧИСЛА НЕ ГОВОРЯТ

1. **Это `osc_range` с D1-настройками на барах H4.** Пересчёт окон — долг №50, он
   заблокирован. Формулировки «H4 убыточен» и «на H4 фильтр вреден» изъяты из
   документов 01.08 и здесь не возвращаются;
2. **H4 существует только у 12 бумаг из 20.** У восьми новых (`MTLR, SMLT, OZON,
   AFLT, VKCO, MTSS, FLOT, PHOR`) строк 1h/4h ноль. Оба плеча посчитаны на одних и
   тех же 12 — набор `MEASUREMENT_UNIVERSE_2026_07`, отпечаток `753d9f90fef9401e`,
   напечатан прогоном;
3. **Ряд 1h/4h не обновляется с 2026-07-11** (долг №7). Конец окна назван прогоном,
   а не предположен: запрошено «по сессию 2026-07-29», фактически у H4 последняя
   закрытая сессия — **2026-07-11**, у D1 — **2026-07-29**. То есть H4-числа стоят
   на окне короче D1-числа на 18 сессий, и это не сравнение двух таймфреймов на
   одном окне.
