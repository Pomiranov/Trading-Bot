# ЗАГЛУШКА. Содержание сведено в `docs/PROJECT_STATE.md` 2026-08-03

**Файл НЕ УДАЛЁН намеренно.** Образец — решение по документам развёртывания
(`PROJECT_STATE.md`, §9): удалённый файл через месяц восстанавливают из истории,
не зная, что он устарел, и дубль возвращается. Заглушка со ссылкой этого не даёт.

## Куда переехало

Всё содержание — в **`docs/PROJECT_STATE.md`** на ветке `merge-learning-nik`,
коммиты **`5c41bfb`** («только документы») и **`9e5ba75`** («сведение»).

| что было здесь | где теперь |
|---|---|
| отзыв «ложь по расписанию», снятие П3 с очереди, новое основание шага 0.7 | §5 «Заведено 02.08» и §9, порядок работ, шаги 1б и 0.7 |
| арифметика выходных 22 из 28, доля выходных по годам | **раздел 2к** |
| замер дыр №41 — ноль | раздел 2к |
| разложение пустоты IS, граница 2024-04-22, честное начало окна, темп не константа | раздел 2к |
| предусловие формы №44 (перцентиль — свойство пары «бар + популяция») | раздел 2к |
| опись молчаливых нулей Ф1–Ф9 с измеренной тяжестью | раздел 2к |
| дыра «два пути к свечам», гейт на `FORWARD_TICKERS`, сцепка Ф7+№9, три литерала `IS_END` | раздел 2к |
| правило 14 | §8 |
| провенанс тройки и отзыв «IS с фильтром 4 / +547» | «ГДЕ МЫ СЕЙЧАС», блок «ПРОВЕНАНС ЦИТИРУЕМЫХ ЧИСЕЛ» |
| кандидаты в долг: httpx-токен, роль БД, `session_date`, `IS_END` | §5 «Заведено 02.08», все с пометкой «номер выделяет Ник» |
| риск №31, замер про `belief_system` в очередь | §9, «СОСТОЯНИЕ НА 2026-08-03» |
| хендофф на утро | `docs/HANDOFF_2026-08-03.md`, **перенесён в боевую папку** — утренняя работа идёт там, а не на непринятой ветке |

## Что осталось на этой ветке и почему

**Код — не сведён и не мержится до приёмки блока A:**
`bot/notify.py`, `bot/forward_healthcheck.py`,
`tests/forward_tests/test_notify_retry.py`, `tools/silent_zeros_probe.py`,
`docs/REHEARSAL_SLOT_PLAN.md`.

**Копии в `measurements/2026-08-02_blockA_part2/`** оставлены здесь
намеренно: они закоммичены в этой ветке, и удалять их до мержа значит
переписывать её состояние. Дубль объявлен в
`measurements/00_INDEX.md` боевой папки; **действующей считается копия там**.

## Что делать при мерже ветки

1. этот файл удалить — к тому моменту он выполнит свою работу;
2. `docs/HANDOFF_2026-08-03.md` здесь удалить (он уже в боевой папке);
3. дубли `2026-08-02_blockA_part2/` схлопнуть в одну копию;
4. сверить `.gitignore`: в боевой папке маски **уже сужены** — общая
   `measurements/**/*.json` съедала замороженную улику, а общая `*.log` —
   замороженные логи. Здесь лежит прежняя, широкая редакция; при мерже брать
   версию боевой папки.
