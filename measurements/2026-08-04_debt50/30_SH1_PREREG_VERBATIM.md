# Ш1: выписка из пре-регистрации ДОСЛОВНО. Прогон НЕ выполнялся

Прочитано 2026-08-03, вечер, из `docs/EXPERIMENTS.md`, «Попытка №1». Ни один параметр
не взят из задания и не из памяти.

**Три даты раздельно:** дата работы — **2026-08-03**; московская торговая сессия
последнего закрытого бара D1 — **2026-08-03**; внутренняя метка бара —
**2026-08-02 21:00+00**. Метка каталога `2026-08-04` датой замера не является.

## Что ЗАПИСАНО — дословно

**Окно и `--as-of`** (строки 104-108):
> начало — `SAMPLE_START_2026_07` = **2023-07-12** (обязательный аргумент, долг №37);
> конец — **`--as-of 2026-07-01`**, оба конца приколочены; почему именно этот конец —
> «держит **оба** таймфрейма в эпохе **Tinkoff** (стык поставщиков `2026-07-02`,
> вычислен по коду) — сравнение не смешивает таймфреймы с поставщиками».

**Набор бумаг** (строка 109):
> «набор | 12 бумаг, `MEASUREMENT_UNIVERSE_2026_07`, отпечаток **`753d9f90fef9401e`**»

**Таймфреймы** (строка 111): «`4h` и `1d` (`run_osc_oos_debug.py:44`)».
**Поставщик** (строка 110): «Tinkoff Invest API (`data/tinkoff_loader.py`), не ISS».

**Три строки таблицы** (строки 45-49), дословно:
> | **D1** | 1, боевые правила | D1-окна | база сравнения на новом окне |
> | **H4 (D1-окна)** | 1, те же правила | D1-окна | **КОНТРОЛЬ** |
> | **H4 (пересчитанные)** | 2, копия правил | ×4.3 | предмет замера |
>
> Строка «D1 с пересчитанными окнами» в таблицу не входит.

**Обязательная колонка** (строка 57), дословно:
> «**Обязательная колонка: «сделок в год на бумагу»** = `n / (бумаг × окно_в_годах)`.
> Без неё таблица покажет качество и скроет темп, а спор именно про темп.»

**Записанное ожидание** (строки 117-121), дословно:
> «**`PF` H4-плеча ВЫРАСТЕТ** относительно контрольной строки (`0.98` на полном
> окне), но **останется НИЖЕ D1**. **Число сделок `n` РЕЗКО УПАДЁТ** — с порядка сотни
> к порядку десятков. **«Сделок в год на бумагу» ПАДАЕТ.**»

**Что считается опровержением** (строка 127):
> «**`PF` упал ИЛИ `n` не изменился.**»

## 🚫 ОСТАНОВКА 1: полный состав колонок НЕ ЗАПИСАН

Предикатом: `grep -i 'колонк' docs/EXPERIMENTS.md` даёт **одно** попадание — строку 57
про обязательную колонку. Метрических колонок (`n`, `WR`, `PF`, `PnL`, средняя сделка,
удержание, темп) досье **не перечисляет**. Таблица «строка / прогон / окна / роль»
определяет СТРОКИ, а не колонки.

⇒ по правилу Ш1 — **«не записано»**, и выбрать разумное нельзя: замер, чьи колонки
выбраны после чтения чисел, ничего не доказывает.

## 🚫 ОСТАНОВКА 2: плечи по фильтру записаны как «ТОЛЬКО БЕЗ ФИЛЬТРА»

Строка 95, таблица «Что НЕ трогается — и почему», дословно:
> «| **плечо С ФИЛЬТРОМ** | долг №52 не починен: дневной гейт раздаётся внутридневным
> барам через `ffill`, то есть **заглядывает до 16 часов вперёд**. Замер идёт **только
> без фильтра** — там `_downtrend_gate` возвращает `None` и заглядывания нет по
> построению |»

Задание требует считать каждую строку **и с фильтром, и без него**. Основание задания
верное — долг №52 закрыт 03.08, заглядывания больше нет, — но это **изменение
пре-registered условия**, а не его прочтение.

И реестр сам говорит, что с этим делать (строки 129-133), дословно:
> «**Это один объявленный пересчёт. Одна попытка.** Второй прогон с другими
> множителями — **нарушение**, и требует **новой записи в этом реестре** с новым
> номером, новой датой фиксации и новым прогнозом.»

⇒ прогон с двумя плечами вместо одного — не попытка №1, а **другой замер**. Заводить
новую запись в реестре (новый номер, новая дата фиксации, новый прогноз) — **акт
человека**, не мой: иначе автор пре-регистрации и автор её изменения совпадут, и форма
перестанет быть дисциплиной.

## Что НЕ сделано и почему

**Прогон не выполнялся ни в каком виде.** Копия правил H4 в улику не кладётся: без
решения по колонкам и плечам она была бы черновиком замера, который не состоится в
объявленных условиях.

Задание разрешает остановку прямо: «Останется бюджет — НЕ добирай, скажи и остановись».
Здесь остановка не по бюджету, а **по правилу Ш1**, и это его прямое срабатывание —
первое за три отложенных захода.

## Запреты соблюдены

Боевая БД не писалась (`trades` не менялся, прогонов не было). Константа множителя
(`c268ab752b9be2d8`) не тронута. Боевой `rules_osc_range.yaml` не тронут, секция
`indicators:` в него не добавлялась. `.env`, `.gitignore`, `forward_runs.jsonl`,
`forward_healthcheck_state.json`, `MIN_HISTORY_BARS`, окна фильтра — не тронуты.
Переменных окружения не осталось.
